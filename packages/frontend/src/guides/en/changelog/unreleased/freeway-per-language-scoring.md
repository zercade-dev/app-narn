Multi-language projects now spend fewer free requests: languages that share the same free model are batched together into one request instead of one request per language.
- Applies automatically to free-model (Freeway) runs; no settings needed.
- Groundwork also landed for per-language quality scores, arriving with the next data update.
