Russian can now be chosen in Settings — v1.66.0 shipped the translation but left it out of the language menu, so nobody could actually switch to it.

- Open Settings and pick Русский from the language list, the same way as Español or Français.
- The translation itself is unchanged from v1.66.0; only the menu entry was missing.
