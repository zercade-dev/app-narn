The Freeway panel now names exactly which connection has bad credentials, and lets you clear the warning with one click.

- A bad-credentials warning now shows which instance is affected and which vault entry to fix, so two keys for the same provider are no longer indistinguishable.
- A new "Try again" button clears the warning directly from the panel once you've saved the right key — no more re-saving keys to see if it worked.
