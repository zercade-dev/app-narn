Updating a provider's API key now fully clears its "bad credentials" warning in the Freeway panel.

- Previously, some providers kept showing a bad-credentials warning even after you saved a working new key.
- Saving a new key now clears the warning for every configured connection to that provider, not just some of them.
- Providers whose keys you didn't touch are unaffected.
