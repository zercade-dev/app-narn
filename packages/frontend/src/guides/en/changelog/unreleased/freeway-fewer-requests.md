NARN Freeway now packs more strings into each translation request on larger runs, so those runs finish sooner.

- Smaller runs keep the more cautious request size that helps translations come back cleanly.
