## v1.1.2 — 2026-07-01

Choose exactly how many entries are sent per AI request with the new custom batch size option.

* Custom batch size: choose exactly how many entries are sent per AI request, in every run dialog.
* Fixed category generation failing on long outputs.
