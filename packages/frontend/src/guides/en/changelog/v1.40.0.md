## v1.40.0 — 2026-07-17

Stage details is now a proper side-by-side workspace.

- The source text sits on the left and the selected language's translation on the right, row by row, so you can translate without scrolling back and forth.
- Character-limit controls only appear when a limit is actually set — add one with the new "+ Add limit" action.
