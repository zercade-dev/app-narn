## v1.8.0 — 2026-07-02

Rate-limited and error-prone translation batches now retry as a whole instead of breaking apart into single requests.

* Smoother recovery when a translation provider has a hiccup: rate-limited batches now wait and retry whole instead of breaking apart into many single requests, and temporary provider errors get one full retry before any splitting — fewer wasted API calls and faster runs.
