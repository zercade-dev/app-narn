## v1.4.3 — 2026-07-02

Restoring a backup now fully replaces project data instead of mixing it with what's already there.

* Restoring a backup now fully replaces project data instead of mixing it with existing data.
* Relink and retranslate data is no longer lost during backup and restore.
