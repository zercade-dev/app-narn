## v1.67.1 — 2026-08-15

NARN Freeway now recovers on its own when you fix a provider key, and uses the provider setup you actually see in Global Config.

- A rejected API key no longer switches a free provider off for good — updating the key in the vault brings it straight back.
- A bad key on one provider setup no longer takes down your other setups for the same provider.
- Freeway now runs through the provider setup shown in Global Config, and, when it isn't the default setup, shows which one is serving.

NARN Freeway now works with the providers you set up as named instances, and its panel starts collapsed.

- Freeway now works when your provider is set up as a named instance (for example your default Groq or Google setup), which is how the web app configures providers.
- The "Enable it" shortcut now scrolls to the control that actually turns a module back on.
- Mistral and Cerebras were removed from the free-provider list — they were never available outside a local one-click setup.
- The NARN Freeway panel is collapsed until you open it, and only loads its status then.
