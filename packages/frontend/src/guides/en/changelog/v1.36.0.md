## v1.36.0 — 2026-07-17

Global Config is much easier to scan when you have many provider setups: each module now shows as a compact row with its status at a glance, and expands only when you want to edit it. Long explanations moved into small info hints, so the page stays clean even with a dozen configured providers.
