# NARN terminology — German (`de`)

Your locale's rendering of every term in the shared lexicon. **This file is yours alone** —
no other locale's file is touched by your work, and nobody else writes rows here.

Read it alongside two other files:

- [`../terminology.md`](../terminology.md) — what each term *means*, its part of speech,
  an example key, and what it must not be confused with. It is **frozen** for the duration
  of the backfill: you read it, you never edit it. See the freeze notice at its top.
- [`../style/de.md`](../style/de.md) — how German is written here: register, punctuation,
  capitalization, plural and agreement rules. That one is yours to write too, for this
  locale only.

Fill a row when you meet the term, in the same change that introduces the wording — so
this file records decisions actually taken, never predictions. Use **Notes** for anything
the next translator would otherwise have to rediscover: a declension that forced a
different word, a term you deliberately left in English, an acronym you expanded, a
candidate you rejected and why.

Terms are in the order they appear in `../terminology.md`. Do not add, remove or reorder
rows: a term the lexicon lacks goes in the additive queue in [`README.md`](README.md).

An empty Rendering means **no batch has met that term yet** — not that it was skipped.
Batch 1 (`config`) filled the rows below; the rest are for later batches.

| Term | Rendering | Notes |
| --- | --- | --- |
| project | Projekt | n., neuter. Verb is *erstellen*, never *öffnen*. |
| workspace | Workspace | Loan kept on purpose: it must never share a word with *Projekt*, and every German native candidate (*Arbeitsbereich*, *Umgebung*) reads as a project-sized or environment-sized scope. Attributive as a hyphenated compound — `config:workspaceSettingsTitle`, `config:batchGroupingDefaultOption`. |
| entry | Eintrag | n., masc., pl. *Einträge*. The counting unit of the whole app. Do not reuse for a translation-memory record without a qualifier — `config:tm.clearAllSuccess_one` writes it out in full. **English *string* splits two ways and both readings were verified at the call site — do not harmonize them.** `config:routing.simpleHint` ("Every string in this project") really does mean entries and takes *Eintrag*; `config:pseudoTestHelpBody` ("hardcoded strings") means literal text baked into the game, is not an entry at all, and takes "hartcodierte Texte". The `logs` namespace narrates in English *strings* where the rest of the UI counts *entries* — a deliberate English inconsistency `../english-review-notes.md` records and leaves alone, whose recommended canonical term is *entry*. Batch 5 shipped that reading: every narrated one is an entry and takes *Eintrag* (`logs:translation.failedNoRoute_one`, `logs:orphan.detected_other`, `logs:translation.queued`). **This is not German silently harmonizing English.** German has no second noun to split them with: *Zeichenkette* is a programmer word for a run of characters and names a different object entirely, so a locale that kept the English distinction would invent one the product does not have. The log entry itself is the qualified compound "Log-Eintrag" (`console:empty`), which is what lets the same noun carry both without collision. **House rule for this column, since this row is the first to need it:** the citation guard treats a quoted span as a checkable German citation whenever it sits *immediately* next to a backtick key, so an English gloss must be kept out of that adjacency — wrap it in parentheses, as both glosses above are, or set it in italics. Drop the parentheses and the English words get tested against the German corpus and fail. That is not hypothetical: the first draft of this sentence demonstrated the bad form inline, and the lexicon citation check failed on it. **A second trap sits next to it** — a backtick span containing a space is treated as a citation too, so a shell command in backticks fails the same way; the guard's own header calls that path unexercised, and it is not any more. Name the check in plain prose here, and keep backticks for key and file references only. |
| source text | Quelltext | n., masc. Shares the *Quell-* root with *Quellsprache*; that is fine, the heads differ. Not *Originaltext*. |
| translation | Übersetzung | n., fem. Verb *übersetzen*. |
| source label | Herkunftslabel | n., neuter, pl. *Herkunftslabels*. *Quellenlabel* was rejected: English already shortens the column heading to *Sources*, and a German shortening to *Quellen* would be indistinguishable from *Quelltext*/*Quellsprache* — exactly the collision the shared lexicon warns about. `config:routing.labelSources` therefore shortens to "Herkunft" instead, and `config:routing.anySource` follows it. Batch 5 ships the full plural at `generation:fieldSources` "Herkunftslabels", where the checkbox names the field rather than the column. |
| achievement | Errungenschaft | n., fem. Steam's German term; *Erfolg* (Xbox) was rejected because `config:health.successRate` already uses *Erfolg* for a different thing, and *Trophäe* is PlayStation-only. The genitive compound is long, so the labels use a *von*/*der*-phrase instead: `config:lqa.achievementDescriptionBytesLabel`. |
| inline tag | Inline-Tag | n., neuter, pl. *Inline-Tags*. Hyphenated mixed compound. |
| placeholder | Platzhalter | n., masc. The in-text token sense only (the double-brace tokens). The input-hint sense of the English word is a different thing and takes no noun in German — those strings are written as infinitive phrases instead. |
| translator context | Übersetzerkontext | n., masc. `strings:compare.editContext`. Distinct from the model's *Kontextfenster* (`config:models.confidenceReason.prompt-near-context`) and from the CSV column, which `config:includeContext` names "Kontextspalte". Where English shortens the term to bare "Context" the German shortens the same way — `strings:compare.addContext`, `strings:compare.emptyContextFilter`. |
| source language | Quellsprache | n., fem. Pairs with *Zielsprache*. |
| target language | Zielsprache | n., fem. |
| reference language | Referenzsprache | n., fem. `strings:compare.translateUseReferenceNone`. Shortened to "Referenz" where the toolbar label stands alone (`strings:compare.reference`), the same shortening English makes. Never *Vergleichssprache* — *Vergleich* is the Compare tab's surface name. |
| writable language | bearbeitbare Sprache | Adjective + noun; the adjective is the part that is fixed, and it is fixed against the **read-only** rendering this locale already ships. `collab:sharing.columnLanguages` is "Bearbeitbare Sprachen" (a table column header, 21 chars — see the length note in `../style/de.md`), and the three lock messages reuse the adjective: `collab:locks.compareTargetsScoped`, `reviewLanguagesScoped`, `glossaryEditScoped`. Elimination proof: *beschreibbar* is the literal antonym of *schreibgeschützt* (`glossary:readOnly`, `strings:contextMenu.readOnly`) and was rejected because German *beschreiben* means both *write on* and *describe*, so "beschreibbare Sprachen" reads as *describable languages*; *freigegeben* is banned by construction, since *freigeben* is the **approve** term; *zugewiesen*, *erlaubt* and *verfügbar* are the shared lexicon's own list of adjectives not to alternate between. *Bearbeitbar* survives and means the thing — the collaborator may edit those languages and nothing else. English's own *read-only* counterpart is a different construction and stays one: `collab:locks.readOnlyLanguage` renders it as access, "nur Lesezugriff", not as an adjective on the language. |
| Pseudo Test | Pseudo Test | Proper noun, kept as-is and unhyphenated when standing alone (`config:pseudoTestHelpAria`). In a German compound it takes hyphens throughout — `config:pseudoExportHint` writes "Pseudo-Test-Text" — which is orthography, not a second name. The language code `pseudo-test` is never translated. |
| run | Durchlauf | n., masc., pl. *Durchläufe*. Never a verb. **A run is *gestartet*, not *ausgeführt*, and a translation is not what gets run**: `quality:empty` says "Einen Übersetzungsdurchlauf starten", because "eine Übersetzung ausführen" reads as executing a piece of text (corrected 2026-08-11, round-3 review M5). Bare *Lauf* was rejected: on its own it reads as running/jogging, and *Durchlauf* is the ordinary German word for one execution of a process. **The compound head is `-durchlauf` / `-durchläufe`, never a shortened `-lauf`** — `config:importModeFullReplaceHint` "KI-Durchläufen", `config:batchGroupingDescription` "Übersetzungs-, Bewertungs- und Quelltext-Review-Durchläufe". A shortened compound reads as a different word from the standalone noun, which is how one term becomes two. Batch 5 repeats it in the log stream at `logs:translation.runQueued` and `logs:translation.queueStartFailed`, and `batch:cancelRun` is "Durchlauf abbrechen". `batch:savedRulesNotice` uses the bare plural as the sentence subject — "Durchläufe verwenden die gespeicherten Routing-Regeln" — which is two plural noun phrases around a transitive verb and therefore a word-order case: the intended reading *is* the subject-first default, so no passive is owed. |
| Activity | Aktivität | n., fem. The tab label `strings:tabs` (runs) and the guide topic `strings:guide.topicActivity`. The **page title expands** and must not be shortened to match: `strings:runs.title` is "Übersetzungsaktivität", mirroring English's own "Activity" / "Translation Activity" split. Not *Verlauf* or *Protokoll*: the first is the version history, and the second reads as a record of what happened rather than a list of runs. **Corrected 2026-08-12 (batch 5), left visible:** this cell used to name *Protokoll* as the word this locale uses for the live server log, which was never true of anything this locale ships — the live log is the loan *Log* (`console:searchPlaceholder`, and batch 2's `strings:runs.judgeLogsTitle` before it), and *Protokoll* appears in no German value in any batch. The rejection stands on the reading of the word, which is what it always should have rested on. |
| batch | Batch | n., masc./neuter — treated as masc. here (*ein Batch*, pl. *Batches*). Loan, per the don't-over-Germanize rule; *Stapel* reads as a physical pile. `logs:translation.batchFailed` ships the masculine singular — "Ein Batch ist fehlgeschlagen" — and moves both of its numbers behind labels, since neither token is `count`. |
| batch grouping | Batch-Gruppierung | n., fem. Distinct from *Batch-Modus* (`config:module.batchMode`), which is the other setting. |
| AI review | KI-Review | n., neuter, pl. *KI-Reviews*. Deliberately **not** built on *Prüfung*, which is reserved for the deterministic LQA checks. *Review* is ordinary German developer vocabulary (Code-Review). Attested bare at `strings:runs.judgeBadge` and `strings:guide.topicAiReview`; the two run-type labels keep English's own parenthetical shape at `strings:runs.typeTranslationAiReview` and `strings:runs.typeSourceAiReview`. The two **tab** surface names are a different shape — see `../style/de.md`. |
| judge | bewerten / Bewertung | Verb, evaluative sense; never *richten*/*Richter*, and no noun "der Judge". **One rendering, both parts of speech: wherever English uses *judge* as a noun or an attributive naming the pass, it is the deverbal *Bewertung*** — `config:requestTimeoutDescription` and `config:maxOutputTokensDescription` list it among the AI call types, `config:batchGroupingDescription` compounds it as "Bewertungs-…-Durchläufe". It had briefly drifted to a second rendering ("KI-Review der Übersetzungen") in the third of those; that is the *surface* name of the Translation AI review tab, which batch 3 owns, and it must not double as the term. **Batch 3 also uses the verb on the three Translation-AI controls** — `review:translationAi.runReview` "Letzten Durchlauf bewerten", `reviewAll`, `reReview` — **because those three buttons start the judge**: they reach the run-scoped and project-wide judge routes in `stores/run-store.ts`, i.e. M25, so they inherit this row's verb. **Corrected 2026-08-11 (round-3 re-review, I1):** this sentence previously justified the choice with *German has no idiomatic verb for review*, which is **false** — *begutachten*, *überprüfen*, *durchsehen*, *sichten* and *beurteilen* all exist and were never considered. The conclusion did not depend on it; the call site did. The retraction, and the three-way verb rule it produced, are in `../style/de.md` — read that rule before rendering an English *review* anywhere. **Batch 5 uses the deverbal noun for the score rather than the verb**: `logs:judge.done` repeats `strings:runs.judgeScoreLabel`'s "Punktzahl" and labels the verdict token, because that token interpolates a raw English *pass* / *fail* straight from the engine — see the paragraph in `../style/de.md`. |
| source review | Quelltext-Review | n., neuter. Built from *Quelltext* + *Review* so it is visibly the same system as *KI-Review* applied to the source. This is the **term**, used wherever English writes the lowercase "source review" in prose — `config:requestTimeoutDescription`, `strings:row.ignoredTooltip`, `strings:runs.sourceReviewDetailsShow`, `strings:order.custom`, and batch 3's `review:sourceAi.startSuccess`. The **tab** is named "Quelltext-KI-Review" (`strings:tabs`, review-source-ai, and `review:sourceAi.configTitle`), carrying across English's own split between the prose term "source review" and the surface "Source AI review"; that is faithfulness to each key's own English, not drift. **The verb is *untersuchen***, and the citation is per **sentence**, not per key: `review:sourceAi.scopeNeverReviewed`, `scopeNoneHint` and the **second** sentence of `configHint` render English *review*; `emptyHint` renders English *checks*. Not *prüfen*, which is the human-review/LQA root, and not *bewerten*, which is the judge. It also pairs with *Befund*, the outcome of an examination. **`configHint`'s first sentence is *durchsehen***, not *untersuchen* — its English is *Check the source text itself*, and two consecutive *untersuchen* where English varied read badly; see the *durchsehen* row in `../style/de.md`. **Corrected 2026-08-11 (round-3 re-review, M8):** this cell previously cited `configHint` flatly as an *untersuchen* site, which opening the key falsifies. |
| finding | Befund | n., masc., pl. *Befunde*. `strings:runs.judgeAllFindingsTitle`, `strings:runs.judgeViewAll`, and batch 3's `review:sourceAi.findingsTitle` / `allFindingsTitle` / `loadingFindings`. Elimination proof: *Beanstandung* is taken by *issue*, *Fehler* by *error*, *Meldung* by the message strings, *Hinweis* and *Anmerkung* are the lexicon's banned hint/remark readings, *Ergebnis* is the banned *result*. *Befund* survives, and it means the thing: the outcome of an examination, reported rather than adjudicated — which is exactly an AI opinion about the source text. Batch 3's five `review:sourceAi.finding*` type labels are the same term, and they reuse the five renderings `strings:runs.aiReviewCheck*` already shipped (Tippfehler, Grammatik, Terminologie, Klarheit, Bedenklich). **Batch 5 uses it where English says *issues*, and that is not a contradiction of the *issue* row.** `logs:sourceReview.done` counts `m.findings` — the source review's own finding records — so it ships as "Quelltext-Review — Befunde: {{findings}}.", the same label-colon-number device `strings:runs.judgeAllFindingsCount` already uses. The *issue* row sends loose prose *about* what a source review looks for to *Auffälligkeiten*; it does not send a count of the records themselves there. |
| suggestion | Vorschlag | n., masc., pl. *Vorschläge*. `strings:runs.judgeSuggestionLabel`, and batch 3's `category:reviewTitle` "Vorschläge prüfen" (identical to `strings:runs.reviewSuggestions`) and `glossary:generateNoSuggestions`. One word across the judge panel, the glossary generator and the category generator. The action on it is *anwenden* — `strings:runs.judgeApply` — including at `strings:runs.judgeApproveAll`, which says "Approve all suggestions" in English for the same apply action; that is the known English copy bug and German does **not** mirror it with *freigeben*. The refuse action is *ablehnen*, see *discard*. The participial adjective is attested at `review:sourceAi.suggestion` "Vorgeschlagener Quelltext". |
| discard | Verwerfen / Ablehnen | **Two senses, two words, and the split is now complete.** Sense 1, the unsaved-edits button beside Save, is "Verwerfen" (`config:discard`, `strings:editor.discard`) — it destroys nothing the user made. Sense 2, refusing something the app offered, is "Ablehnen" (`strings:runs.judgeDiscard`, and batch 3's `glossary:generateDiscard` "Ergebnis ablehnen"); batch 6 shipped `colorText:assistant.discard` in that same sense and with that same word, beside `assistant.apply` "Anwenden" on the assistant's proposal card — refusing something the model offered, which is precisely sense 2. Batch 5's `logs:judge.suggestionDiscarded` is the log line for that same action and repeats `strings:runs.judgeDiscardSuccess` verbatim plus English's own full stop: "Vorschlag abgelehnt." *Ablehnen* is free: the lexicon bans *cancel*, *delete*, *undo* and *clear* for this term, and none of them is *ablehnen*. `config:discardUntranslatable` is a **third** sense the frozen lexicon does not cover — omitting entries from an export, which destroys nothing — and is written as "weglassen". It is **queued** in [`README.md`](README.md) for the between-waves resolution, because thirteen other languages meet that same key in their own batch 1 and this file is not one any of them reads. |
| needs review | zu prüfen | Verb phrase, so it carries no gender. The *reviewed* counterpart is the participle "geprüft" (`config:reviewProgressCount`) and the progress heading is "Prüffortschritt" (`config:reviewProgress`). Batch 2 reused both rather than coining a third: the filter label is `strings:filters.needsReview`, the lowercase row badge `strings:compare.cellNeedsReviewBadge`, and the quoted status tokens `strings:contextMenu.clearNeedsReview` and `strings:contextMenu.clearReviewed`. **German needs no gender rule for the "reviewed" family**: "geprüft" is a predicative participle and is invariant in every one of the three roles the shared lexicon separates for es/fr, including the explicit-antecedent case `strings:compare.cellMarkReviewedAria`. |
| flag | Zurückstellen / Zurückgestellt | Verb on the row button (`review:flag`), participle on the filter chip (`review:filterFlagged`), the toast (`review:flaggedToast`) and the empty state (`review:emptyFlaggedTitle`, `emptyFlaggedHint`). **A disposition, not an alarm**: it sets the translation aside to come back to later and *clears* needs-review, so it may not be *markieren*, which is the verb that **sets** that state (`strings:compare.flagAllNeedsReview`). Elimination proof: *markieren* and *kennzeichnen* are that verb; *melden* is the banned *report/denounce*; *Lesezeichen* is the banned *bookmark*; *mit Stern versehen* is the banned *star*. *Zurückstellen* survives and means the thing — German for setting something aside to deal with later. **Known near-neighbour, checked rather than assumed:** *Zurücksetzen* is **revert** (`strings:runs.revert`, see `../style/de.md`). The two are never on screen together — revert is a control in the Activity run list, this is a button in the Manual review queue, two different tabs — and they name unrelated actions, so the two-character difference is not a co-rendering hazard. Do not collapse them. English's *other*, loose uses of "flagged" are not this term, and there are **two** of them — both take the ordinary *markiert*, and both are listed here so a later fix round does not "restore" the disposition word into either: `glossary:flaggedTitle` (a glossary term missing a translation) and `review:sourceAi.runSummary` (entries the source review marked as carrying findings). The second one lives in this same namespace file, three keys from `review:filterFlagged` "Zurückgestellt", which is exactly why it is written down. |
| Review (the sidebar group) | Review | n., neuter, uninflected. `strings:guide.groupReview`; batch 4 shipped the identical word at `sidebar:groups.review`, where it is the heading directly over the four tab labels of the group. The umbrella is deliberately the loan rather than *Prüfung* or *Bewertung*, each of which is already spent on one of the group's four members (LQA checks; the judge). The loan is as vague in German developer usage as "Review" is in English, which is exactly the property an umbrella needs here. |
| review queue | Prüfwarteschlange | n., fem. `review:title`, `review:emptyTitle`, `review:filterLabel`. Built on the same *prüf-* root as **needs review** ("zu prüfen") and *geprüft*, so a reader sees one feature and not two — which is what the shared lexicon asks for. Not the noun *Prüfung*, which `../style/de.md` reserves for the deterministic LQA check; the compounding stem is free, and `config:reviewProgress` "Prüffortschritt" already ships it for exactly this human-review surface. The compound is the ordinary German pattern (*Druckwarteschlange*). It is the **page title** of the Manual review tab, whose tab label is "Manuelles Review" — English splits the same way ("Manual review" / "Review queue"), so this is not drift. |
| module | Modul | n., neuter, pl. *Module*. Never *Plugin*. |
| module instance | Modulinstanz | n., fem. Shortened to *Instanz* once *Modul* is established in the sentence, which is most of `config:instances.*`. The id itself is never translated; `config:instances.slugLabel` is "Instanz-ID" and the identifier fragment stays *Slug*. `logs:module.instanceRegistered` follows English, which calls the object a *configuration* there rather than an instance, and puts a real noun in front of the token so the article can decline: "Die Konfiguration {{instance}} wurde hinzugefügt." |
| provider | Anbieter | n., masc. Used only for the outside company/service. `config:routing.simplePlaceholder` labels a module-instance picker *Provider* in English — a known English defect — and is rendered with this word as written, without dragging *Modul* toward it anywhere else. **Batch 6 owns the other two keys of that defect** and treats them identically: `colorText:assistant.instanceLabel` and `stage-details:chatInstanceLabel` are both "Anbieter", as are `assistant.pickInstance` "Anbieter auswählen…" and the four assistant status lines that name it. The pull is real and was resisted per key: the same two namespaces render English *Module* as "Modul" three keys away (`stage-details:module`, `modulePlaceholder`, `orphans:relink.aiModuleLabel` "KI-Modul"), so both words are on screen in one popover — which is the English defect made visible, not a German drift. Do not harmonize them; the copy fix belongs to English. |
| model | Modell | n., neuter. *Vorlage* carries *template*, so the usual German model/template collision does not arise. |
| prompt | Prompt | n., masc. Loan. Capitalized as a German noun even where the English badge is lowercase (`config:routing.promptBadge`). |
| reasoning effort | Reasoning-Aufwand | n., masc. The provider's own parameter; German AI tooling keeps *Reasoning*. |
| routing rule | Routing-Regel | n., fem., pl. *Routing-Regeln*. *Routing* is the established German loan for content routing too, and hyphenation keeps it from reading as network routing on its own. |
| rule group | Regelgruppe | n., fem. Distinct from *Kategorie* and from *Batch-Gruppierung*. |
| credential vault | Tresor | n., masc. Bitwarden's German term for the same object. Carries all four phrasings: `config:credentialsVaultLockedChip`, `config:credentialsVaultLocked`, `config:credentialsUnlockButton`, `config:credentialsMissing`. **The full form is Zugangsdaten-Tresor, and batch 4 is where it had to be settled.** Batch 1 never needed it: every English `config` string shortens to *vault* (*Vault locked*, *Unlock vault*), while `vault` is the namespace where English writes *credential vault* out in full. It is the *credential* rendering plus the head, hyphenated per the mixed-compound rule: `vault:statusLabel` "Zugangsdaten-Tresor", and the three dialog titles `vault:unlockTitle`, `vault:createTitle`, `vault:editorTitle` build on it. The short *Tresor* stays wherever English shortens too (`vault:nameLabel`, `vault:goToVault`, `vault:changePasswordTitle`), so the two forms track English's own split rather than being interchangeable. Batch 5 is all short form, because all of its English is: `logs:action.unlockVault` repeats `config:credentialsUnlockButton` byte for byte, `console:vaultLocked` is "Tresor gesperrt" and `logs:vault.unlocked` is "Tresor entsperrt." |
| credential | Zugangsdaten | n., plural-only in German (there is no natural singular), which suits every string — English uses the plural throughout. *Anmeldeinformationen* was rejected as a 21-character unbreakable compound. Vault key names are never translated, and that includes the **schematic** one: `vault:keyPlaceholder` is the placeholder in the key picker and ships as `KEY_NAME`, unchanged, because it models the shape of a real vault key (`OPENAI_API_KEY`) and the field never accepts a German word. It is a single token, so it is below the identical-value guard's three-word threshold for a spaced script and reds nothing here. |
| LQA | LQA | Kept as the industry acronym; German localization practice uses it untranslated. Attributive with a hyphen: `config:lqa.title`, `config:health.lqaRetries`. |
| quality gate | Gate | n., neuter. The full form is *Quality-Gate*; `config:overflowRatioDescription` says "LQA-Gate" and `config:lqa.description` uses the bare "Gate" once quality checks have been named, mirroring English. *Tor*, *Pforte* and *Schranke* all read physically and are banned. |
| check | Prüfung | n., fem., pl. *Prüfungen*; verb *prüfen*. Covers "Qualitätsprüfungen" and "LQA-Prüfungen" (`config:lqa.title`, `config:lqa.description`) and every individual check name. Reserved against *AI review*. Batch 3 reuses it for the literal English "Checks" of the source-review toggles — `review:sourceAi.checksLabel` "Prüfungen", matching `strings:runs.aiReviewChecksLabel`, and `review:sourceAi.noCheckHint` — which the shared lexicon explicitly permits: the ban is on rendering the *term* **AI review** with this word, not on translating an English "check" as one. The `quality:checkLabels.*` names repeat the `config:lqa.checks` renderings **except where English itself differs**: `quality:checkLabels.tag-equality` (whose English is the bare *Tag equality*) ships as "Tag-Gleichheit", not the config name "Gleichheit der Inline-Tags", whose own English carries "Inline". Batch 5 ships the compound in the log stream wherever English writes *quality check* — `logs:lqa.passed` is "Qualitätsprüfung bestanden." and `logs:lqa.failed` its negated twin — reusing `config:lqa.description`'s own noun and the *bestehen* pass/fail lexeme rather than coining a log-only word. |
| issue | Beanstandung | n., fem. One verdict an LQA check files. *Fehler* is reserved for *error*, *Problem* reads as a crash, and *Meldung* collides with the `{{message}}` strings. `config:lqa.description`, `strings:row.lqaIssues_other`, and batch 3's `review:lqaTitle` "LQA-Beanstandungen", `quality:mostCommonIssues.title` "Häufigste Beanstandungen" and `quality:bySource.title`. **English also uses "issues" loosely for "problems in the text", and that is not this term**: `review:sourceAi.configHint` and `review:sourceAi.noFindings` are about the source review, whose outputs are *Befunde*, so they take the neutral *Auffälligkeiten* rather than the LQA verdict word. |
| severity | Schweregrad | n., masc. The two values are "Blockierend" and "Warnung" (`config:lqa.severityBlocking`, `config:lqa.severityWarning`); they belong to *check*, not here. Batch 5's `console:filter_warn` renders English's abbreviated log level *Warn* with the same full word, "Warnung": German has no idiomatic four-letter clipping, the two never name different things, and the filter strip is CSS-uppercased anyway. |
| notification severity | Info / Warnung / Kritisch | The three value labels at `account:notificationsSeverity.info`, `warning` and `critical`. "Warnung" is **not** a fresh decision — it is fixed by *severity* above (`config:lqa.severityWarning`) and is repeated here verbatim, which is the whole point of the shared lexicon separating this row from that one. *Kritisch* is free: the LQA pair is "Blockierend"/"Warnung", so nothing was spent on it, and the shared lexicon's banned readings (*Alarm*, *Gefahr*, *Fehler*, *Priorität*) are all avoided. |
| assertion | Zusicherung | n., fem. A genuinely fourth word: *Bedingung* is routing conditions, *Prüfung* is the LQA checks, *Regel* is routing rules. `config:lqa.regexAddAssertion`. |
| pattern | Muster | n., neuter. Regex sense only. Free of the *Vorlage* (template) collision by construction. |
| overflow | Überlauf | n., masc. "Überlauf-Verhältnis" for the ratio (`config:overflowRatioLabel`), "Längenüberlauf" for the check name (`config:lqa.checks.overflow.name`, repeated at `quality:checkLabels.overflow`). Batch 3's `review:overflowIssue` opens with the bare noun and keeps English's × sign: "Überlauf: Die Übersetzung ist {{ratio}}× so lang wie der Quelltext." — the comparative frame is count-neutral, which the bare ratio token requires. |
| length limit | Längenlimit | n., neuter. A hard cap, deliberately not built on *Überlauf*. *Begrenzung* was rejected as the banned *limitation* reading. `config:routing.labelMaxLength`, `config:lqa.checks.length-limit.name`. |
| pass rate | Bestehensquote | n., fem. `quality:columns.passRate`, `quality:byLanguage.title`, and the three legend tiers `quality:legend.tier*`. Built on *bestehen*, which is already the whole product's pass/fail lexeme — `strings:runs.judgeVerdictPass` "Bestanden" / `judgeVerdictFail` "Nicht bestanden", `strings:row.lqaPassed` "LQA bestanden", `strings:filters.lqaFailed` — so the dashboard names the same outcome as the row badges. **Not built on *Erfolg***, which `config:health.successRate` ("Erfolg {{rate}} %") has already spent on the *other* metric, provider request success; that is the trap the shared lexicon names. *Quote* rather than *Rate* is the ordinary German for a share expressed as a percentage (*Fehlerquote*, *Bestehensquote*). The overall stat is "Bestehensquote gesamt" (`quality:overallStat.passRateLabel`) — the dashboard idiom, not a second term. |
| glossary | Glossar | n., neuter. Batch 5 compounds it natively, without a hyphen, in the generation log lines: "Glossargenerierung" (`logs:glossaryGen.failed`) and "Glossardurchlauf" (`logs:glossaryGen.done`), matching `strings:row.ignoredTooltip`, which already ships the pair Glossar- und Kategoriegenerierung. |
| glossary term | Glossarbegriff | n., masc., pl. *Glossarbegriffe*. Never *Eintrag*. Shortened to bare *Begriff* inside the Glossary tab, exactly as English shortens to "term" — `glossary:confirmDeleteTitle` "Begriff löschen", `glossary:totalTerms` "Begriffe gesamt:", `glossary:emptyTermsTitle`. The table's Source column is the term's own source-language form, so it is `glossary:colSource` "Quellbegriff" and **not** *Quelltext*, which is an entry's source text (`review:sourceText`); the two are different objects and the pre-flight's same-English/different-rendering report lists the pair for that reason. |
| constant | konstant | **Adjective, not a noun.** Three of its four uses are adjectival — the two lowercase state badges `review:glossaryConstant` and `review:sourceAi.glossaryConstant` ("konstant", lowercase because the surrounding chips are) and the bulk action `glossary:bulkMarkConstant` "Als konstant markieren ({{count}})" — so the noun *Konstante* would have to be bent into all three, and it also reads as a mathematical constant. The column header `glossary:colConstant` is "Konstant", capitalized only as the header's first word. `glossary:bulkClearConstant` names the flag as a compound — "Konstant-Markierung entfernen ({{count}})" — parallel to its sibling "Als konstant markieren ({{count}})" and built on the shape `strings:bulk.clearNewFlag` "Neu-Markierung entfernen" already shipped for the same clear-a-flag operation. **Corrected 2026-08-11 (round-3 review, M3):** it first shipped as „Konstant“ entfernen, quoting and capitalizing a flag English neither quotes nor capitalizes, while the button beside it used the bare adjective — two presentations of one flag on two adjacent buttons. Never *schreibgeschützt*, which is the **read-only** flag two columns away (`glossary:readOnly`) and means the row cannot be edited, not that the text is never translated. |
| match | Treffer | n., masc.; verb *übereinstimmen*. `config:lqa.checks.glossary-adherence.description` uses the noun, `config:lqa.regexModeMustMatch` the verb. Never the word chosen for *glossary term*. Batch 3 ships the Matches panel and its family: `glossary:matchesPanel` "Treffer", `matchResultsCount`, `matchNoResults`, and the compound "Glossartreffer" where English writes "glossary matches" (`glossary:confirmEnableApplyTitle`). **A third lexeme of this family is licensed and recorded rather than left to look like drift:** `review:glossaryTermsTitle` is "Passende Begriffe" for English's "Matching terms", because English itself splits the participle from the noun there (Matching terms / Matches) and the noun *Treffer* cannot be made attributive without a heavier compound. *Übereinstimmende Begriffe* is equally correct if a later batch prefers it; what is not allowed is a fourth word. |
| translation memory | Translation Memory | Neuter, uninflected. The established German CAT-tool term (Trados, memoQ) and it sidesteps *Speicher*, which reads as RAM. In a German compound it takes hyphens: `config:tm.policyUpdateFailed`. Batch 5's two log lines keep the loan uninflected and reuse the verbs already shipped in `config:tm.*`: `logs:tm.cleared` is "Translation Memory geleert." after `config:tm.clearAll`, and `logs:tm.variantDeleted` is "Ein Eintrag im Translation Memory wurde entfernt." |
| approve | freigeben | Verb; the only route into the Translation Memory (`config:tm.browserEmpty`, which uses the nominalized infinitive; `strings:bulk.approveSelected`). Batch 3 ships it as the review queue's own action — `review:approve` "Freigeben", `review:approveAll`, `review:approveUnchangedPassing` — verified at the call site: `ReviewTab.tsx` posts to `/projects/:id/approve`, the TM route. Distinct from *anwenden* (apply), *als geprüft markieren* (mark reviewed) and *speichern* (save). **The source review's "Approve" is a different action and takes a different word.** `review:sourceAi.approve` only records that a reviewer has dealt with an entry: `run-store.ts` sends a PATCH to that run's own source-review entry route with an `approved` flag, and the route performs **two** writes: the run's own finding record, **and** a mirror onto the entry's own `sourceReview` field — guarded so an older run's state is not clobbered. What it never touches is the thing that matters here: **never Translation Memory** (a different route entirely, the one the review queue posts to) and **never the source text**, which is what its own `applyReminder` tells the user. So it is "Bestätigen" / "Bestätigt", per the rule that a word English reuses across two controls does not get one rendering. Rendering it *freigeben* would tell the reader the source text had gone into Translation Memory. **Corrected twice, both left visible (2026-08-11, round-3 review M1 and re-review M1):** this cell first claimed the action *writes nothing*, which was false; the repair then named only the run record and missed the entry mirror. The *Bestätigen* / *freigeben* split never rested on either — it rests on the two negatives. If a later batch ships only an inflected form (*freigibst*), **change this cell to the attested form and keep the dictionary form here in Notes — never reword the string.** See the citation-guard note in `../style/de.md`. |
| category | Kategorie | n., fem., pl. *Kategorien*. |
| tone | Tonfall | n., masc., pl. *Tonfälle*. Bare *Ton* reads acoustically; *Stil* and *Stimme* read as the model's writing style. |
| orphan | Waise | n., fem., pl. *Waisen* — the Orphans tab title. The adjective is *verwaist* (`config:orphanedCount`, `config:importModeFullReplaceHint`), and it is invariant after a number, which is why the count strings need no plural family. Batch 5's `logs:orphan.linked` and `logs:orphan.deleted` use the adjective on the entry rather than the noun — "Ein verwaister Eintrag" — because English writes *an orphaned string*, an adjective on the entry, not the tab's noun. **Batch 6 owns the namespace and keeps that same split, per key, from each key's own English.** The noun carries the surface and everything that names one of them as a thing: `orphans:title` "Waisen" (the surface name), `confirmDelete.title` "Waise löschen?", `relink.title`, `actions.bulkDelete` and every `toast.*`. The adjective carries the two places English uses it on an entry: `orphans:empty` "Keine verwaisten Einträge" (English *No orphaned records*, where *record* is on the **entry** row's own Not-list, so the head is *Eintrag*) and `confirmDelete.body` "der verwaiste Eintrag". Neither the noun nor the adjective inflects for number after a numeral in a way the count strings need, which is why nothing here grew a plural family. **One toast takes neither form, and that is the round-6 review's Critical:** `orphans:toast.deleteError` fires from the bulk-delete path as well as the single-row one, so any German noun there commits to a number the string cannot know. It ships as "Löschen fehlgeschlagen" — no noun at all — and the counting section of `../style/de.md` carries the rule and the sibling asymmetry it creates. The **verb** for relinking governs *mit* + dative, not *auf*: `orphans:subtitle` reads "ihre Übersetzungen mit einem anderen Eintrag neu verknüpfen" (corrected in the same round, M1 — English's *relink onto* is not a complement German licenses, and the neighbouring `relink.description` keeps *auf* only because its verb is *verschieben*). |
| relink | neu verknüpfen | Verb. `config:importModeFullReplaceHint` ships the participle "verknüpft". One verb for the row button, the dialog title, the confirm step and the import warning. **The deverbal noun is *Neuverknüpfung*** (fem.), settled by batch 2 at `strings:runs.typeRelinkRetranslate` where a run-type label cannot take an infinitive. Batch 6 shipped it at `orphans:relink.confirmTitle` — English *Confirm relink* — as "Neuverknüpfung bestätigen", which satisfies the confirm-dialog row's infinitive shape and this row's noun at once, and repeats it at `relink.confirmDescription`, `toast.candidatesError` and `toast.aiRetranslateUnavailable`. The **verb** carries the controls: `orphans:actions.relink` and `relink.confirmSubmit` are both "Neu verknüpfen" (English *Relink* in both places), `relink.title` is "Waise neu verknüpfen", and `emptyTitle` uses the participle, "Alles ist verknüpft" — where English's own *Everything's linked* drops the *re-*, so the German drops the *neu-* with it. Note that the plain verb *verknüpfen* is **not** reserved by this row — it is free for linking an achievement to its counterpart (`strings:achievement.linkButton`, `strings:achievement.dialogTitle`), which is a different operation; only the *neu-* form names relinking. **Batch 5 uses the term where English uses the bare verb, and the call site is the reason.** `logs:orphan.linked` reads *Linked an orphaned string* in English, but the event is emitted by `relinkOrphan` in `M11-orphan-manager.ts` — the operation the UI calls Relink — so the German is "Ein verwaister Eintrag wurde neu verknüpft." The bare *verknüpfen* is free here precisely because this row leaves it free for achievement linking, which is why using it would have named the wrong operation. |
| backup | Backup | n., neuter, pl. *Backups*. Loan, per the don't-over-Germanize rule; *Sicherungskopie* is not what practitioners say. The surface name is the bare word (`config:importSnapshotNote`, `config:maxBackupsLabel`); batch 5 ships the plural at `logs:backup.pruneFailed` and the bare noun at `logs:backup.restored` "Backup wiederhergestellt." **Batch 6 owns the namespace, and the loan held under pressure.** `backup:toastRestoreSuccess` is byte-identical to that `logs` line, English and German alike; the page title expands from the tab label to "Backup und Wiederherstellung" (`backup:title`, English *Backup and Restore*, the Activity/Legal shape); the compound is hyphenated per the mixed-compound rule at `backup:previewLoading` "Backup-Details" and `deleteConfirmBody` "Backup-Datei"; and the plural is the loan's own, "Gespeicherte Backups", "Manuelle Backups". **The one place the round-1 style guide had prescribed *Sicherung* was `backup:creating`, and that prescription was wrong against this row** — it ships "Backup wird erstellt…". The retraction is in `../style/de.md`'s punctuation section, where it lived. **The verb is *erstellen* and never *sichern***, because the shared lexicon's "the verb is *create a backup*, never *to backup*" is the same instruction and because *sichern* is already argued away in the **claim** row. |
| snapshot | Snapshot | n., masc./neuter — treated as masc. (*ein Snapshot*, "Sicherheits-Snapshot" in `config:importSnapshotNote`). `config:templatesDescription` uses the plural loosely for saved configurations, exactly as English does. **Batch 6 met English's *other* name for the same object and did not coin a second German one.** `backup:itemPreImport` and `itemPreRetranslate` label an automatic pre-operation backup a *Restore point*, which the shared lexicon records as English's own alternative name for a snapshot; both ship as "Snapshot von kurz vor …", reusing this row's noun and `config:importSnapshotNote`'s own "Kurz vor diesem Import" phrasing rather than adding a *Wiederherstellungspunkt* that would name one object twice. The **trigger group headings** above those rows name the operation, not the object — `backup:triggerPreImport` "Automatisch — vor dem CSV-Import" and its two siblings — so the noun is not repeated there. |
| template | Vorlage | n., fem. *Modell* is taken by the AI model and *Muster* by the regex pattern, so neither is available. `config:templatesTitle`, `config:saveAsTemplate`. |
| collaborator | Mitwirkende | Substantivized adjective, declined (*ein Mitwirkender*, *die Mitwirkenden*). `strings:runs.manualRecordingPaused` ships the **nominative** plural after a *kein*-word — "keine aktiven Mitwirkenden", a verbless fragment where the *-en* is weak declension after the determiner, not a case ending — and `strings:runs.projectTotalCollaborators` ships the bare plural inside brackets. *Mitarbeiter* was rejected as "employee", *Teilnehmer* is the banned *participant*, *Bearbeiter* is the banned *editor*. **Batch 4 must not build *member* on this root**: the obvious contrast word is "Mitglied", which is free. And do not build it on *Zusammenarbeit* either — that is the Sharing surface name (see `../style/de.md`), not a person. Batch 4 kept both rules: *member* is the separate **Mitglied** row below, and the collaborator strings take the dative/genitive plural — `collab:sharing.auditToggleHelp` and `collab:invites.nicknameRequiredBody` both write *von Mitwirkenden*, `collab:errors.project_full` writes *für Mitwirkende*. **Where the English is singular the German goes plural or drops the noun**, because the substantivized adjective has no gender-neutral singular: English's "a collaborator" in `collab:sharing.auditToggleUnsharedHint` is "mit Mitwirkenden", and "as a collaborator" in `collab:join.description` is carried by the verb *mitwirken* instead. |
| member | Mitglied | n., neuter, pl. *Mitglieder*. The word batch 2 reserved for it, taken as written. `collab:sharing.membersTitle`, `columnMember`, `removeConfirmTitle`, `memberRemoved`, and `collab:errors.already_member`. Wider than *Mitwirkende*, exactly as English is wider than *collaborator*: the owner is a Mitglied and not a Mitwirkender, and the two words sit in adjacent strings on the Sharing tab (`membersTitle` "Mitglieder" over a table whose sibling card invites *Mitwirkende*), which is why they must not share a root. The former member is `collab:activity.formerMember` "Ehemaliges Mitglied". |
| nickname | Nickname | n., masc., pl. *Nicknames*. The established German loan for a public handle; *Spitzname* reads as an affectionate name friends use, not an identity other people look you up by, and *Benutzername* is the shared lexicon's named trap. Byte-identical to English and deliberately so — it is one word, which is below the identical-value guard's three-word threshold, so nothing is being dodged. `collab:nickname.title` (both the section heading and the field label), `collab:invites.codeDialogTitle`'s sibling hints, and every `collab:errors.nickname_*`. |
| claim | beanspruchen | Verb; verbal noun in the progress label, participle in the toast — `collab:nickname.claimButton` "Nickname beanspruchen", `claiming` "Wird beansprucht…", `claimSuccess` "Nickname beansprucht.", `immutableHint` "Ein einmal beanspruchter Nickname lässt sich nicht mehr ändern." Elimination proof: *reservieren* is banned as *reserve* **and** would collide head-on with `collab:errors.nickname_reserved` "Dieser Name ist reserviert."; *registrieren* is the banned *register*; *nehmen* and *belegen* are the banned *take*/*occupy*; *anfordern* is the banned *request*; *sichern* is free of the lexicon's bans but reads as *back up* beside this product's *Backup* strings; *festlegen* is available and is used for English's own *set* in `collab:nickname.description`, but as the term it would lose the take-it-before-someone-else-does sense that the reserved-name error contrasts with. *Beanspruchen* survives and means the thing: to assert a permanent claim to something for yourself. |
| invite | Einladung | n., fem., pl. *Einladungen*; the code itself is the compound *Einladungscode* (masc.), `collab:invites.codeDialogTitle` and `collab:join.codePlaceholder`. **The four statuses are one set, all adjectival:** `collab:invites.status.pending` "Ausstehend", `redeemed` "Eingelöst", `revoked` "Zurückgezogen", `expired` "Abgelaufen". **The revoke verb is *zurückziehen*, not *widerrufen*, and the reason is a co-render, not a preference:** the status cell and the revoke button are two adjacent columns of one table (`InvitesSection.tsx` renders `invites.status.*` in the Status cell and the `invites.revoke` button in the Actions cell of a pending row), so English's *Revoked* / *Revoke* pair is on screen at once — and German *widerrufen* is identical in the participle and the infinitive, which would collapse a distinction English keeps in the one place a reader compares them. *Zurückziehen* / *zurückgezogen* differ, are one lexeme, and are the ordinary German for withdrawing an invitation. Near-neighbours checked rather than assumed: *Zurückstellen* is **flag** (Manual review queue) and *Zurücksetzen* is **revert** (Activity run list); neither shares a tab with this one. |
| recording | Aufzeichnung | n., fem. `strings:runs.manualRecordingPaused`. Names the capture being on or off, not the captured rows. Deliberately **not** built on *Eintrag* or *Aufzeichnungen als Einträge* — this app counts entries constantly, and the lexicon's warning is exactly that collision. *Protokollierung* was rejected because it would be a second word for the *Logging* this locale ships at `strings:runs.aiReviewVerbose`, next to the *Logs* of `console:exportLogs` (re-verified in batch 5, which corrected the original reason — *Protokoll* is not this locale's word for the live server log and never was; see the note in the **Activity** row). **Batch 4 ships the verb and keeps the feature name separate**, exactly as English does: the capture is *aufzeichnen* (`collab:sharing.auditToggleCheckboxLabel` "Manuelle Bearbeitungen aufzeichnen", `collab:sharing.auditToggleHelp`), while English's *audit* — the feature the toggle switches on — is the loan *Audit* (`collab:sharing.auditToggleLabel` "Audit manueller Bearbeitungen"). Two English words, two German words; do not collapse them, and do not reach for *Protokoll* for the feature either. |
| stage | Level | n., neuter, uninflected in the plural (*die Level*). A playable level in the game, never a phase of a process. `strings:tabs` (stage-details) is "Level-Details" and `strings:runs.typeStageDetailsTranslation` repeats it. German *Stage* is not gaming vocabulary here (it reads as a podium), while *Level* is what German players and German game UIs actually say; *Phase*, *Etappe* and *Stufe* are the process readings the shared lexicon bans. Batch 5 repeats it three times in the log stream (`logs:stageDetails.queued`, `done`, `failed`), and batch 6 shipped the owed page title: `stage-details:title` is "Level-Details". The term also compounds in that batch — `stage-details:chatToggle` "Level-Details-Chat umschalten", `fields.stageDescription.label` "Level-Beschreibung", `fields.gameplayDetails.label` "Gameplay-Details" — and it declines: the noun is neuter, so the genitive singular is *des Levels* (`fields.name.placeholder` "Name des Levels"). **The one place *Level* does NOT go is `fields.gameplayDetails.label`**, whose English head is *gameplay*, not *stage*; that stays the separate loan. Its sibling `fields.gameplayDetails.placeholder` renders English's own *the stage* and therefore **does** carry the term — "Wie sich das Level spielt". **Corrected 2026-08-12 (round-6 review, I1), left visible:** this sentence first scoped the exception to the glob `fields.gameplayDetails.*`, an absolute over a two-key subtree proved on one key and falsified by the next line of the same JSON file. The exception is real; only its scope was invented, and a later fix round reading this row first — which is what a translator opens first — would have stripped a correct *Level* out of the placeholder. **The test is the English head of the key in front of you, never the key's prefix.** |
| Text Styler | Text-Styler | Proper noun, masc. `strings:tabs` (color-text); batch 4 shipped `sidebar:colorText` identically and batch 6 shipped the owed tool title at `colorText:title`. The hyphen is **not** a device for dodging the identical-to-English guard: a two-part compound noun written with a space is wrong German orthography, so "Text-Styler" is the only correct way to write this name in German and it would be the rendering even if the guard did not exist. A fully German coinage (*Textgestaltung*) was rejected — the lexicon calls this a product surface name, and German UIs keep such names. |
| element | Hydro / Pyro / Anemo / Electro / Dendro / Cryo / Geo | **Left untranslated, which is what the shared lexicon's own rule asks for** — it says to use the names the game itself ships in the target language and to leave them if it ships them untranslated, and it bans invented or literal renderings of the Greek roots. German gaming usage keeps these coinages as they are, and this repo carries no evidence about any particular game's German build, so inventing a set here would be exactly the "literal translation of the Greek root" the row rules out. The one that tempts a change is *Electro*, which German orthography would spell *Elektro*: it is a proper noun of the game's own making, not a German word, so it keeps its spelling. Each is a single token and therefore below the identical-value guard's three-word threshold, so nothing is being dodged. The **group heading** is translated normally — `colorText:groupElements` is "Elemente" — and so are the **four** non-element swatch groups around it: "Dunkler Hintergrund", "Heller Hintergrund", "Qualität" (see `../style/de.md` for why that one repeats a tab label and is licensed) and "Meine Farben". **Corrected 2026-08-12 (round-6 review, M3), left visible:** this said "the two … it sits beside" and named only the two background groups. `components/color-text/palettes.ts` defines four groups and `PaletteSection.tsx` renders a fifth (`groupCustom`), so the count was wrong **and** it omitted the two the round actually had to argue about; *elements* also sits between `light-bg` and `quality` in the array, so *beside* was wrong on the adjacency reading too. The instruction — translate the headings, leave the element names — never depended on the number. **`colorText:swatches.key1` / `key2` are on the same list in `../english-review-notes.md` and are kept for the same reason**: *Key 1* / *Key 2* name a game's keyword-highlight colours, not a keyboard key and not a vault key, and there is no established German form to use instead. |
| theme | Theme | n., neuter, pl. *Themes*. Loan, per the don't-over-Germanize rule and per this product's register, which already keeps *Backup*, *Prompt*, *Routing*, *Workspace* and *Review*. *Design* is the mainstream German consumer word (Windows, Android) and was the runner-up; it was not taken because it reads as visual design in general rather than one of four selectable skins, and because it would be the one fully German word on a settings page built out of loans. *Darstellung* is spent on `settings:appearance` and *Modus* on `settings:mode`, so neither was available anyway. `settings:theme` is both the visible label and the radio group's `aria-label`. **The four theme NAMES are byte-identical duplicates** — `settings:themes.*.name` and `welcome:themeChooser.names.*` match exactly: "Klassisch", "Pixel", "Techno", "Minimal". Only the first is translated; the other three are the same word in German. **Batch 6 shipped the second half by copying the four values out of the shipped `settings` file rather than re-deriving them**, which is the only method that can be relied on: nothing mechanical compares the two sets, the shared lexicon names this the app's highest-risk duplication, and Spanish is on record shipping "Tecno"/"Techno" and "Minimal"/"Minimalista" across the pair. The **descriptions** are a different matter and are NOT duplicates: `settings:themes.*.description` and `welcome:themeChooser.taglines.*` have different English (*The classic narn look.* against *The original NARN look.*; *Chunky 8-bit console vibes.* against *Chunky retro pixels.*), so each is translated from its own English and the brand keeps whichever casing its own source string carries. `welcome:themeChooser.keepDefault` names one of the four inside a button — "Techno behalten" — and the name is the same string again. |
| guide | Guide | n., masc. Loan, kept as the surface name; *Anleitung*, *Handbuch* and *Dokumentation* are all defensible German but the shared lexicon bans alternating between them, and *Guide* is what the sidebar item is: batch 4 shipped `sidebar:guide`. `config:pseudoTestHelpLink` names it in prose. **Batch 6 needed the plural and the loan takes an English one**: `welcome:guidesHeading` is "Guides" (a heading over the six guide-group names, which come from `strings:guide.group*`) and `welcome:browseGuides` is "Guides durchsehen" — a navigation button whose English is a verb phrase, so a bare infinitive, and the verb is the *durchsehen* row in `../style/de.md` rather than a new one. "Guides" is byte-identical to English and is one token, below the identical-value guard's three-word threshold, so nothing is being dodged; the German plural of *der Guide* is *die Guides*. `welcome:intro` names them once more in prose with the same word and the same verb. |
| release | Release | n., neuter (*das Release*), pl. *Releases*. Loan, per the don't-over-Germanize rule and per the family this locale already keeps (*Backup*, *Prompt*, *Routing*, *Workspace*, *Review*, *Guide*, *Changelog*, *Theme*, *Log*, *Batch*, *Snapshot*). `common:changelogShowOlder` is the only key that renders it. **Elimination proof.** The shared lexicon bans four candidates outright — *Version* (that is the number, which this app never translates), *Update*, *Build* and *Eintrag*, the last because that noun is the content entry and this app counts those constantly. Of what is left: *Veröffentlichung* names the **act** of publishing rather than the published thing; *Ausgabe* reads as an edition of a periodical **and** is already spent in this locale on output (`strings:runs.usageTokens`, `colorText:copyOutput`), so it is a live collision, not a preference; *Fassung* names a wording or draft of a text. *Release* survives, and it means the thing: one published version of the app as a shipped artefact, which is what German developers call *ein Release*. |
| changelog | Changelog | n., neuter (*das Changelog*). Loan, kept as the page's name. *Änderungsprotokoll* is the obvious German compound and was rejected on the reading of its head, not on length: *Protokoll* is a transcript or a network protocol, so *Änderungs-* over that head reads as a running record of changes as they happen — a log of what the server did — rather than as the release notes this page actually holds. *Verlauf*, *Neuigkeiten* and *Versionshinweise* are the shared lexicon's own banned readings. **Corrected 2026-08-12 (round-5 review, I1), left visible:** this cell used to reject the compound on a *collision*, citing the **Activity** row four rows above as reserving *Protokoll* for the live server log — the very claim that row retracted in the same commit, so the ground was a citation of a retraction of itself. The rejection stands; only its ground was borrowed from a reservation that never existed. `../style/de.md`'s Changelog surface row points here, and batch 6 is the batch that read it. `sidebar:changelog` (batch 4) and `common:changelogShowOlder` (batch 6) name the same page; the second ships as "Ältere Releases anzeigen ({{count}})" — see the **release** row for why that noun is a loan too. Its sibling `common:changelogEntryError` is the one key where English calls a release "a changelog entry"; German writes "Dieser Changelog-Eintrag …", using *Eintrag* under a disambiguating head exactly as `console:empty`'s "Log-Eintrag" does. That is faithfulness to this key's own English, not the **release** row's banned *Eintrag* rendering — the ban binds what you call a release where English says *release*, and `changelogShowOlder` obeys it. |
