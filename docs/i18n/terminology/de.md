# NARN terminology — German (`de`)

Your locale's rendering of every term in the shared lexicon. **This file is yours alone** —
no other locale's file is touched by your work, and nobody else writes rows here.

Read it alongside two other files:

- [`../terminology.md`](../terminology.md) — what each term *means*, its part of speech,
  an example key, and what it must not be confused with. It is **frozen** for the duration
  of the backfill: you read it, you never edit it. See the freeze notice at its top.
- [`../style/de.md`](../style/de.md) — how German is written here: register, punctuation,
  capitalization, plural and agreement rules. That one is yours to write too, for this
  locale only.

Fill a row when you meet the term, in the same change that introduces the wording — so
this file records decisions actually taken, never predictions. Use **Notes** for anything
the next translator would otherwise have to rediscover: a declension that forced a
different word, a term you deliberately left in English, an acronym you expanded, a
candidate you rejected and why.

Terms are in the order they appear in `../terminology.md`. Do not add, remove or reorder
rows: a term the lexicon lacks goes in the additive queue in [`README.md`](README.md).

An empty Rendering means **no batch has met that term yet** — not that it was skipped.
Batch 1 (`config`) filled the rows below; the rest are for later batches.

| Term | Rendering | Notes |
| --- | --- | --- |
| project | Projekt | n., neuter. Verb is *erstellen*, never *öffnen*. |
| workspace | Workspace | Loan kept on purpose: it must never share a word with *Projekt*, and every German native candidate (*Arbeitsbereich*, *Umgebung*) reads as a project-sized or environment-sized scope. Attributive as a hyphenated compound — `config:workspaceSettingsTitle`, `config:batchGroupingDefaultOption`. |
| entry | Eintrag | n., masc., pl. *Einträge*. The counting unit of the whole app. Do not reuse for a translation-memory record without a qualifier — `config:tm.clearAllSuccess_one` writes it out in full. **English *string* splits two ways and both readings were verified at the call site — do not harmonize them.** `config:routing.simpleHint` ("Every string in this project") really does mean entries and takes *Eintrag*; `config:pseudoTestHelpBody` ("hardcoded strings") means literal text baked into the game, is not an entry at all, and takes "hartcodierte Texte". The `logs` namespace's narrated *strings* are entries and take *Eintrag*, per the canonical-term rule. **House rule for this column, since this row is the first to need it:** the citation guard treats a quoted span as a checkable German citation whenever it sits *immediately* next to a backtick key, so an English gloss must be kept out of that adjacency — wrap it in parentheses, as both glosses above are, or set it in italics. Drop the parentheses and the English words get tested against the German corpus and fail. That is not hypothetical: the first draft of this sentence demonstrated the bad form inline, and the lexicon citation check failed on it. **A second trap sits next to it** — a backtick span containing a space is treated as a citation too, so a shell command in backticks fails the same way; the guard's own header calls that path unexercised, and it is not any more. Name the check in plain prose here, and keep backticks for key and file references only. |
| source text | Quelltext | n., masc. Shares the *Quell-* root with *Quellsprache*; that is fine, the heads differ. Not *Originaltext*. |
| translation | Übersetzung | n., fem. Verb *übersetzen*. |
| source label | Herkunftslabel | n., neuter, pl. *Herkunftslabels*. *Quellenlabel* was rejected: English already shortens the column heading to *Sources*, and a German shortening to *Quellen* would be indistinguishable from *Quelltext*/*Quellsprache* — exactly the collision the shared lexicon warns about. `config:routing.labelSources` therefore shortens to "Herkunft" instead, and `config:routing.anySource` follows it. |
| achievement | Errungenschaft | n., fem. Steam's German term; *Erfolg* (Xbox) was rejected because `config:health.successRate` already uses *Erfolg* for a different thing, and *Trophäe* is PlayStation-only. The genitive compound is long, so the labels use a *von*/*der*-phrase instead: `config:lqa.achievementDescriptionBytesLabel`. |
| inline tag | Inline-Tag | n., neuter, pl. *Inline-Tags*. Hyphenated mixed compound. |
| placeholder | Platzhalter | n., masc. The in-text token sense only (the double-brace tokens). The input-hint sense of the English word is a different thing and takes no noun in German — those strings are written as infinitive phrases instead. |
| translator context | Übersetzerkontext | n., masc. `strings:compare.editContext`. Distinct from the model's *Kontextfenster* (`config:models.confidenceReason.prompt-near-context`) and from the CSV column, which `config:includeContext` names "Kontextspalte". Where English shortens the term to bare "Context" the German shortens the same way — `strings:compare.addContext`, `strings:compare.emptyContextFilter`. |
| source language | Quellsprache | n., fem. Pairs with *Zielsprache*. |
| target language | Zielsprache | n., fem. |
| reference language | Referenzsprache | n., fem. `strings:compare.translateUseReferenceNone`. Shortened to "Referenz" where the toolbar label stands alone (`strings:compare.reference`), the same shortening English makes. Never *Vergleichssprache* — *Vergleich* is the Compare tab's surface name. |
| writable language |  |  |
| Pseudo Test | Pseudo Test | Proper noun, kept as-is and unhyphenated when standing alone (`config:pseudoTestHelpAria`). In a German compound it takes hyphens throughout — `config:pseudoExportHint` writes "Pseudo-Test-Text" — which is orthography, not a second name. The language code `pseudo-test` is never translated. |
| run | Durchlauf | n., masc., pl. *Durchläufe*. Never a verb. Bare *Lauf* was rejected: on its own it reads as running/jogging, and *Durchlauf* is the ordinary German word for one execution of a process. **The compound head is `-durchlauf` / `-durchläufe`, never a shortened `-lauf`** — `config:importModeFullReplaceHint` "KI-Durchläufen", `config:batchGroupingDescription` "Übersetzungs-, Bewertungs- und Quelltext-Review-Durchläufe". A shortened compound reads as a different word from the standalone noun, which is how one term becomes two. |
| Activity | Aktivität | n., fem. The tab label `strings:tabs` (runs) and the guide topic `strings:guide.topicActivity`. The **page title expands** and must not be shortened to match: `strings:runs.title` is "Übersetzungsaktivität", mirroring English's own "Activity" / "Translation Activity" split. Not *Verlauf* or *Protokoll* — the former is the version history and the latter is the live server log. |
| batch | Batch | n., masc./neuter — treated as masc. here (*ein Batch*, pl. *Batches*). Loan, per the don't-over-Germanize rule; *Stapel* reads as a physical pile. |
| batch grouping | Batch-Gruppierung | n., fem. Distinct from *Batch-Modus* (`config:module.batchMode`), which is the other setting. |
| AI review | KI-Review | n., neuter, pl. *KI-Reviews*. Deliberately **not** built on *Prüfung*, which is reserved for the deterministic LQA checks. *Review* is ordinary German developer vocabulary (Code-Review). Attested bare at `strings:runs.judgeBadge` and `strings:guide.topicAiReview`; the two run-type labels keep English's own parenthetical shape at `strings:runs.typeTranslationAiReview` and `strings:runs.typeSourceAiReview`. The two **tab** surface names are a different shape — see `../style/de.md`. |
| judge | bewerten / Bewertung | Verb, evaluative sense; never *richten*/*Richter*, and no noun "der Judge". **One rendering, both parts of speech: wherever English uses *judge* as a noun or an attributive naming the pass, it is the deverbal *Bewertung*** — `config:requestTimeoutDescription` and `config:maxOutputTokensDescription` list it among the AI call types, `config:batchGroupingDescription` compounds it as "Bewertungs-…-Durchläufe". It had briefly drifted to a second rendering ("KI-Review der Übersetzungen") in the third of those; that is the *surface* name of the Translation AI review tab, which batch 3 owns, and it must not double as the term. **Batch 3 also uses the verb on the three Translation-AI controls** — `review:translationAi.runReview` "Letzten Durchlauf bewerten", `reviewAll`, `reReview` — because German has no idiomatic verb for *review* and the engine behind those buttons is the judge; English's own `emptyHintRun` describes the button, in English, as one that *will judge* the run. The reasoning and the rejected alternatives are in `../style/de.md`. |
| source review | Quelltext-Review | n., neuter. Built from *Quelltext* + *Review* so it is visibly the same system as *KI-Review* applied to the source. This is the **term**, used wherever English writes the lowercase "source review" in prose — `config:requestTimeoutDescription`, `strings:row.ignoredTooltip`, `strings:runs.sourceReviewDetailsShow`, `strings:order.custom`, and batch 3's `review:sourceAi.startSuccess`. The **tab** is named "Quelltext-KI-Review" (`strings:tabs`, review-source-ai, and `review:sourceAi.configTitle`), carrying across English's own split between the prose term "source review" and the surface "Source AI review"; that is faithfulness to each key's own English, not drift. **The verb is *untersuchen*** (`review:sourceAi.configHint`, `emptyHint`, `scopeNoneHint`): not *prüfen*, which is the human-review/LQA root, and not *bewerten*, which is the judge. It also pairs with *Befund*, the outcome of an examination. |
| finding | Befund | n., masc., pl. *Befunde*. `strings:runs.judgeAllFindingsTitle`, `strings:runs.judgeViewAll`, and batch 3's `review:sourceAi.findingsTitle` / `allFindingsTitle` / `loadingFindings`. Elimination proof: *Beanstandung* is taken by *issue*, *Fehler* by *error*, *Meldung* by the message strings, *Hinweis* and *Anmerkung* are the lexicon's banned hint/remark readings, *Ergebnis* is the banned *result*. *Befund* survives, and it means the thing: the outcome of an examination, reported rather than adjudicated — which is exactly an AI opinion about the source text. Batch 3's five `review:sourceAi.finding*` type labels are the same term, and they reuse the five renderings `strings:runs.aiReviewCheck*` already shipped (Tippfehler, Grammatik, Terminologie, Klarheit, Bedenklich). |
| suggestion | Vorschlag | n., masc., pl. *Vorschläge*. `strings:runs.judgeSuggestionLabel`, and batch 3's `category:reviewTitle` "Vorschläge prüfen" (identical to `strings:runs.reviewSuggestions`) and `glossary:generateNoSuggestions`. One word across the judge panel, the glossary generator and the category generator. The action on it is *anwenden* — `strings:runs.judgeApply` — including at `strings:runs.judgeApproveAll`, which says "Approve all suggestions" in English for the same apply action; that is the known English copy bug and German does **not** mirror it with *freigeben*. The refuse action is *ablehnen*, see *discard*. The participial adjective is attested at `review:sourceAi.suggestion` "Vorgeschlagener Quelltext". |
| discard | Verwerfen / Ablehnen | **Two senses, two words, and the split is now complete.** Sense 1, the unsaved-edits button beside Save, is "Verwerfen" (`config:discard`, `strings:editor.discard`) — it destroys nothing the user made. Sense 2, refusing something the app offered, is "Ablehnen" (`strings:runs.judgeDiscard`, and batch 3's `glossary:generateDiscard` "Ergebnis ablehnen"); batch 6's `colorText:assistant.discard` is the same sense and takes the same word. *Ablehnen* is free: the lexicon bans *cancel*, *delete*, *undo* and *clear* for this term, and none of them is *ablehnen*. `config:discardUntranslatable` is a **third** sense the frozen lexicon does not cover — omitting entries from an export, which destroys nothing — and is written as "weglassen". It is **queued** in [`README.md`](README.md) for the between-waves resolution, because thirteen other languages meet that same key in their own batch 1 and this file is not one any of them reads. |
| needs review | zu prüfen | Verb phrase, so it carries no gender. The *reviewed* counterpart is the participle "geprüft" (`config:reviewProgressCount`) and the progress heading is "Prüffortschritt" (`config:reviewProgress`). Batch 2 reused both rather than coining a third: the filter label is `strings:filters.needsReview`, the lowercase row badge `strings:compare.cellNeedsReviewBadge`, and the quoted status tokens `strings:contextMenu.clearNeedsReview` and `strings:contextMenu.clearReviewed`. **German needs no gender rule for the "reviewed" family**: "geprüft" is a predicative participle and is invariant in every one of the three roles the shared lexicon separates for es/fr, including the explicit-antecedent case `strings:compare.cellMarkReviewedAria`. |
| flag | Zurückstellen / Zurückgestellt | Verb on the row button (`review:flag`), participle on the filter chip (`review:filterFlagged`), the toast (`review:flaggedToast`) and the empty state (`review:emptyFlaggedTitle`, `emptyFlaggedHint`). **A disposition, not an alarm**: it sets the translation aside to come back to later and *clears* needs-review, so it may not be *markieren*, which is the verb that **sets** that state (`strings:compare.flagAllNeedsReview`). Elimination proof: *markieren* and *kennzeichnen* are that verb; *melden* is the banned *report/denounce*; *Lesezeichen* is the banned *bookmark*; *mit Stern versehen* is the banned *star*. *Zurückstellen* survives and means the thing — German for setting something aside to deal with later. **Known near-neighbour, checked rather than assumed:** *Zurücksetzen* is **revert** (`strings:runs.revert`, see `../style/de.md`). The two are never on screen together — revert is a control in the Activity run list, this is a button in the Manual review queue, two different tabs — and they name unrelated actions, so the two-character difference is not a co-rendering hazard. Do not collapse them. English's *other*, loose use of "flagged" is not this term: `glossary:flaggedTitle` marks an incomplete glossary term and takes the ordinary *markiert*. |
| Review (the sidebar group) | Review | n., neuter, uninflected. `strings:guide.groupReview`; batch 4's `sidebar:groups.review` owes the identical word. The umbrella is deliberately the loan rather than *Prüfung* or *Bewertung*, each of which is already spent on one of the group's four members (LQA checks; the judge). The loan is as vague in German developer usage as "Review" is in English, which is exactly the property an umbrella needs here. |
| review queue | Prüfwarteschlange | n., fem. `review:title`, `review:emptyTitle`, `review:filterLabel`. Built on the same *prüf-* root as **needs review** ("zu prüfen") and *geprüft*, so a reader sees one feature and not two — which is what the shared lexicon asks for. Not the noun *Prüfung*, which `../style/de.md` reserves for the deterministic LQA check; the compounding stem is free, and `config:reviewProgress` "Prüffortschritt" already ships it for exactly this human-review surface. The compound is the ordinary German pattern (*Druckwarteschlange*). It is the **page title** of the Manual review tab, whose tab label is "Manuelles Review" — English splits the same way ("Manual review" / "Review queue"), so this is not drift. |
| module | Modul | n., neuter, pl. *Module*. Never *Plugin*. |
| module instance | Modulinstanz | n., fem. Shortened to *Instanz* once *Modul* is established in the sentence, which is most of `config:instances.*`. The id itself is never translated; `config:instances.slugLabel` is "Instanz-ID" and the identifier fragment stays *Slug*. |
| provider | Anbieter | n., masc. Used only for the outside company/service. `config:routing.simplePlaceholder` labels a module-instance picker *Provider* in English — a known English defect — and is rendered with this word as written, without dragging *Modul* toward it anywhere else. |
| model | Modell | n., neuter. *Vorlage* carries *template*, so the usual German model/template collision does not arise. |
| prompt | Prompt | n., masc. Loan. Capitalized as a German noun even where the English badge is lowercase (`config:routing.promptBadge`). |
| reasoning effort | Reasoning-Aufwand | n., masc. The provider's own parameter; German AI tooling keeps *Reasoning*. |
| routing rule | Routing-Regel | n., fem., pl. *Routing-Regeln*. *Routing* is the established German loan for content routing too, and hyphenation keeps it from reading as network routing on its own. |
| rule group | Regelgruppe | n., fem. Distinct from *Kategorie* and from *Batch-Gruppierung*. |
| credential vault | Tresor | n., masc. Bitwarden's German term for the same object. Carries all four phrasings: `config:credentialsVaultLockedChip`, `config:credentialsVaultLocked`, `config:credentialsUnlockButton`, `config:credentialsMissing`. |
| credential | Zugangsdaten | n., plural-only in German (there is no natural singular), which suits every string — English uses the plural throughout. *Anmeldeinformationen* was rejected as a 21-character unbreakable compound. Vault key names are never translated. |
| LQA | LQA | Kept as the industry acronym; German localization practice uses it untranslated. Attributive with a hyphen: `config:lqa.title`, `config:health.lqaRetries`. |
| quality gate | Gate | n., neuter. The full form is *Quality-Gate*; `config:overflowRatioDescription` says "LQA-Gate" and `config:lqa.description` uses the bare "Gate" once quality checks have been named, mirroring English. *Tor*, *Pforte* and *Schranke* all read physically and are banned. |
| check | Prüfung | n., fem., pl. *Prüfungen*; verb *prüfen*. Covers "Qualitätsprüfungen" and "LQA-Prüfungen" (`config:lqa.title`, `config:lqa.description`) and every individual check name. Reserved against *AI review*. Batch 3 reuses it for the literal English "Checks" of the source-review toggles — `review:sourceAi.checksLabel` "Prüfungen", matching `strings:runs.aiReviewChecksLabel`, and `review:sourceAi.noCheckHint` — which the shared lexicon explicitly permits: the ban is on rendering the *term* **AI review** with this word, not on translating an English "check" as one. The `quality:checkLabels.*` names repeat the `config:lqa.checks` renderings **except where English itself differs**: `quality:checkLabels.tag-equality` (whose English is the bare *Tag equality*) ships as "Tag-Gleichheit", not the config name "Gleichheit der Inline-Tags", whose own English carries "Inline". |
| issue | Beanstandung | n., fem. One verdict an LQA check files. *Fehler* is reserved for *error*, *Problem* reads as a crash, and *Meldung* collides with the `{{message}}` strings. `config:lqa.description`, `strings:row.lqaIssues_other`, and batch 3's `review:lqaTitle` "LQA-Beanstandungen", `quality:mostCommonIssues.title` "Häufigste Beanstandungen" and `quality:bySource.title`. **English also uses "issues" loosely for "problems in the text", and that is not this term**: `review:sourceAi.configHint` and `review:sourceAi.noFindings` are about the source review, whose outputs are *Befunde*, so they take the neutral *Auffälligkeiten* rather than the LQA verdict word. |
| severity | Schweregrad | n., masc. The two values are "Blockierend" and "Warnung" (`config:lqa.severityBlocking`, `config:lqa.severityWarning`); they belong to *check*, not here. |
| notification severity |  | Not met in `config`, but "Warnung" is already fixed by *severity* above and must render identically there. |
| assertion | Zusicherung | n., fem. A genuinely fourth word: *Bedingung* is routing conditions, *Prüfung* is the LQA checks, *Regel* is routing rules. `config:lqa.regexAddAssertion`. |
| pattern | Muster | n., neuter. Regex sense only. Free of the *Vorlage* (template) collision by construction. |
| overflow | Überlauf | n., masc. "Überlauf-Verhältnis" for the ratio (`config:overflowRatioLabel`), "Längenüberlauf" for the check name (`config:lqa.checks.overflow.name`, repeated at `quality:checkLabels.overflow`). Batch 3's `review:overflowIssue` opens with the bare noun and keeps English's × sign: "Überlauf: Die Übersetzung ist {{ratio}}× so lang wie der Quelltext." — the comparative frame is count-neutral, which the bare ratio token requires. |
| length limit | Längenlimit | n., neuter. A hard cap, deliberately not built on *Überlauf*. *Begrenzung* was rejected as the banned *limitation* reading. `config:routing.labelMaxLength`, `config:lqa.checks.length-limit.name`. |
| pass rate | Bestehensquote | n., fem. `quality:columns.passRate`, `quality:byLanguage.title`, and the three legend tiers `quality:legend.tier*`. Built on *bestehen*, which is already the whole product's pass/fail lexeme — `strings:runs.judgeVerdictPass` "Bestanden" / `judgeVerdictFail` "Nicht bestanden", `strings:row.lqaPassed` "LQA bestanden", `strings:filters.lqaFailed` — so the dashboard names the same outcome as the row badges. **Not built on *Erfolg***, which `config:health.successRate` ("Erfolg {{rate}} %") has already spent on the *other* metric, provider request success; that is the trap the shared lexicon names. *Quote* rather than *Rate* is the ordinary German for a share expressed as a percentage (*Fehlerquote*, *Bestehensquote*). The overall stat is "Bestehensquote gesamt" (`quality:overallStat.passRateLabel`) — the dashboard idiom, not a second term. |
| glossary | Glossar | n., neuter. |
| glossary term | Glossarbegriff | n., masc., pl. *Glossarbegriffe*. Never *Eintrag*. Shortened to bare *Begriff* inside the Glossary tab, exactly as English shortens to "term" — `glossary:confirmDeleteTitle` "Begriff löschen", `glossary:totalTerms` "Begriffe gesamt:", `glossary:emptyTermsTitle`. The table's Source column is the term's own source-language form, so it is `glossary:colSource` "Quellbegriff" and **not** *Quelltext*, which is an entry's source text (`review:sourceText`); the two are different objects and the pre-flight's same-English/different-rendering report lists the pair for that reason. |
| constant | konstant | **Adjective, not a noun.** Three of its four uses are adjectival — the two lowercase state badges `review:glossaryConstant` and `review:sourceAi.glossaryConstant` ("konstant", lowercase because the surrounding chips are) and the bulk action `glossary:bulkMarkConstant` "Als konstant markieren ({{count}})" — so the noun *Konstante* would have to be bent into all three, and it also reads as a mathematical constant. The column header `glossary:colConstant` is "Konstant", capitalized only as the header's first word. `glossary:bulkClearConstant` quotes the flag by name: „Konstant“ entfernen. Never *schreibgeschützt*, which is the **read-only** flag two columns away (`glossary:readOnly`) and means the row cannot be edited, not that the text is never translated. |
| match | Treffer | n., masc.; verb *übereinstimmen*. `config:lqa.checks.glossary-adherence.description` uses the noun, `config:lqa.regexModeMustMatch` the verb. Never the word chosen for *glossary term*. Batch 3 ships the Matches panel and its family: `glossary:matchesPanel` "Treffer", `matchResultsCount`, `matchNoResults`, and the compound "Glossartreffer" where English writes "glossary matches" (`glossary:confirmEnableApplyTitle`). |
| translation memory | Translation Memory | Neuter, uninflected. The established German CAT-tool term (Trados, memoQ) and it sidesteps *Speicher*, which reads as RAM. In a German compound it takes hyphens: `config:tm.policyUpdateFailed`. |
| approve | freigeben | Verb; the only route into the Translation Memory (`config:tm.browserEmpty`, which uses the nominalized infinitive; `strings:bulk.approveSelected`). Batch 3 ships it as the review queue's own action — `review:approve` "Freigeben", `review:approveAll`, `review:approveUnchangedPassing` — verified at the call site: `ReviewTab.tsx` posts to `/projects/:id/approve`, the TM route. Distinct from *anwenden* (apply), *als geprüft markieren* (mark reviewed) and *speichern* (save). **The source review's "Approve" is a different action and takes a different word.** `review:sourceAi.approve` only records that a reviewer has dealt with an entry — its own `applyReminder` says, in English, that approving *only records your review*, and it never writes anything — so it is "Bestätigen" / "Bestätigt", per the rule that a word English reuses across two controls does not get one rendering. Rendering it *freigeben* would tell the reader the source text had gone into Translation Memory. If a later batch ships only an inflected form (*freigibst*), **change this cell to the attested form and keep the dictionary form here in Notes — never reword the string.** See the citation-guard note in `../style/de.md`. |
| category | Kategorie | n., fem., pl. *Kategorien*. |
| tone | Tonfall | n., masc., pl. *Tonfälle*. Bare *Ton* reads acoustically; *Stil* and *Stimme* read as the model's writing style. |
| orphan | Waise | n., fem., pl. *Waisen* — the Orphans tab title. The adjective is *verwaist* (`config:orphanedCount`, `config:importModeFullReplaceHint`), and it is invariant after a number, which is why the count strings need no plural family. |
| relink | neu verknüpfen | Verb. `config:importModeFullReplaceHint` ships the participle "verknüpft". One verb for the row button, the dialog title, the confirm step and the import warning. **The deverbal noun is *Neuverknüpfung*** (fem.), settled by batch 2 at `strings:runs.typeRelinkRetranslate` where a run-type label cannot take an infinitive; batch 6's `orphans:relink.confirmTitle` ("Confirm relink") is the same noun. Note that the plain verb *verknüpfen* is **not** reserved by this row — it is free for linking an achievement to its counterpart (`strings:achievement.linkButton`, `strings:achievement.dialogTitle`), which is a different operation; only the *neu-* form names relinking. |
| backup | Backup | n., neuter, pl. *Backups*. Loan, per the don't-over-Germanize rule; *Sicherungskopie* is not what practitioners say. The surface name is the bare word (`config:importSnapshotNote`, `config:maxBackupsLabel`). |
| snapshot | Snapshot | n., masc./neuter — treated as masc. (*ein Snapshot*, "Sicherheits-Snapshot" in `config:importSnapshotNote`). `config:templatesDescription` uses the plural loosely for saved configurations, exactly as English does. |
| template | Vorlage | n., fem. *Modell* is taken by the AI model and *Muster* by the regex pattern, so neither is available. `config:templatesTitle`, `config:saveAsTemplate`. |
| collaborator | Mitwirkende | Substantivized adjective, declined (*ein Mitwirkender*, *die Mitwirkenden*). `strings:runs.manualRecordingPaused` ships the **nominative** plural after a *kein*-word — "keine aktiven Mitwirkenden", a verbless fragment where the *-en* is weak declension after the determiner, not a case ending — and `strings:runs.projectTotalCollaborators` ships the bare plural inside brackets. *Mitarbeiter* was rejected as "employee", *Teilnehmer* is the banned *participant*, *Bearbeiter* is the banned *editor*. **Batch 4 must not build *member* on this root**: the obvious contrast word is "Mitglied", which is free. And do not build it on *Zusammenarbeit* either — that is the Sharing surface name (see `../style/de.md`), not a person. |
| member |  | Not met in `strings`. Reserved against *collaborator* above: "Mitglied" is the intended word and nothing has taken it. |
| nickname |  |  |
| claim |  |  |
| invite |  |  |
| recording | Aufzeichnung | n., fem. `strings:runs.manualRecordingPaused`. Names the capture being on or off, not the captured rows. Deliberately **not** built on *Eintrag* or *Aufzeichnungen als Einträge* — this app counts entries constantly, and the lexicon's warning is exactly that collision. *Protokollierung* was rejected because *Protokoll* reads as the live server log. |
| stage | Level | n., neuter, uninflected in the plural (*die Level*). A playable level in the game, never a phase of a process. `strings:tabs` (stage-details) is "Level-Details" and `strings:runs.typeStageDetailsTranslation` repeats it. German *Stage* is not gaming vocabulary here (it reads as a podium), while *Level* is what German players and German game UIs actually say; *Phase*, *Etappe* and *Stufe* are the process readings the shared lexicon bans. Batch 6's `stage-details:title` owes "Level-Details" verbatim. |
| Text Styler | Text-Styler | Proper noun, masc. `strings:tabs` (color-text); batch 6's `colorText:title` and batch 4's `sidebar:colorText` owe the identical rendering. The hyphen is **not** a device for dodging the identical-to-English guard: a two-part compound noun written with a space is wrong German orthography, so "Text-Styler" is the only correct way to write this name in German and it would be the rendering even if the guard did not exist. A fully German coinage (*Textgestaltung*) was rejected — the lexicon calls this a product surface name, and German UIs keep such names. |
| element |  |  |
| theme |  |  |
| guide | Guide | n., masc. Loan, kept as the surface name; *Anleitung*, *Handbuch* and *Dokumentation* are all defensible German but the shared lexicon bans alternating between them, and *Guide* is what the sidebar item will be. `config:pseudoTestHelpLink`. |
| release |  |  |
| changelog |  |  |
