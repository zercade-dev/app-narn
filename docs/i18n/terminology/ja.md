# NARN terminology — Japanese (`ja`)

Your locale's rendering of every term in the shared lexicon. **This file is yours alone** —
no other locale's file is touched by your work, and nobody else writes rows here.

Read it alongside two other files:

- [`../terminology.md`](../terminology.md) — what each term *means*, its part of speech,
  an example key, and what it must not be confused with. It is **frozen** for the duration
  of the backfill: you read it, you never edit it. See the freeze notice at its top.
- [`../style/ja.md`](../style/ja.md) — how Japanese is written here: register, punctuation,
  capitalization, plural and agreement rules. That one is yours to write too, for this
  locale only.

Fill a row when you meet the term, in the same change that introduces the wording — so
this file records decisions actually taken, never predictions. Use **Notes** for anything
the next translator would otherwise have to rediscover: a declension that forced a
different word, a term you deliberately left in English, an acronym you expanded, a
candidate you rejected and why.

Terms are in the order they appear in `../terminology.md`. Do not add, remove or reorder
rows: a term the lexicon lacks goes in the additive queue in [`README.md`](README.md).

**Quoting convention, so every claim here is checkable.** 「…」 marks a span quoted
**verbatim from a shipped value** — never truncated with an internal `…`, so it can be
verified by substring search. A rejected candidate, an English source phrase, or a bare
lexeme under discussion is written in plain text or *italics*, never in 「…」. The citation
guard did not read 「…」 as a citation at all until the wave fixed it, which is exactly how a
stale quote in the *pass rate* row survived batch 1's own review.

| Term | Rendering | Notes |
| --- | --- | --- |
| project | プロジェクト | Batch 1 (`config`). Verb is 作成 (`config:duplicateProject` is 「プロジェクトを複製」). |
| workspace | ワークスペース | Batch 1. Never abbreviated, and never the same word as プロジェクト — `config:batchGroupingDefaultOption` 「ワークスペース設定を使用」 sits directly opposite the project value. |
| entry | エントリ | Batch 1. **Counter is 件**, always (`config:orphanedCount` is 「孤立{{count}}件」). No trailing ー: -y/-ry loans drop the long-vowel mark here (エントリ・カテゴリ・メモリ) while -er/-or/-ar loans keep it (サーバー・プロバイダー) — see `style/ja.md`. Rejected 項目, which is reserved for **form fields and vault key slots** (`config:routing.promptOptionsOptional`, `config:credentialsMissing`), and 行, reserved for **CSV rows** (`config:rowsProcessed`, `config:malformedRows_other`). `config:routing.simpleHint` says *string* in English and means an entry: rendered エントリ per the canonical-term rule. Batch 2: where English itself says **row** rather than entry, Japanese follows English and counts 行, not 件 — `strings:bulk.rowsSelected_other` 「{{count}}行を選択中」 against `strings:compare.selectedCount` 「{{count}}件を選択中」. The two English strings differ ("{{count}} rows selected" vs "{{count}} selected"), so the two renderings differ; this is faithfulness, not drift. **Batch 5 cashes in the lexicon's own canonical-term instruction, which had never been exercised at scale.** `terminology.md`'s *entry* row says the `logs` namespace narrates about this object with the English word *string* and that the *entry* rendering is used there anyway; `english-review-notes.md` item 3 records the same English inconsistency and leaves it deliberately unfixed. **Sixteen** `logs` keys now ship エントリ over an English *string* — counted, not estimated: the eleven `translation.*` keys (`start` 「エントリを{{language}}に翻訳中…」, `done`, `queued`, `queued_other`, `failedGeneric` and the three bare/`_other` `failed*` pairs), `glossaryGen.done`, `orphan.detected`/`detected_other` 「ソースファイルに存在しなくなったエントリが{{count}}件見つかりました。」, and `orphan.linked`/`deleted`, whose *orphaned string* takes 孤立エントリ. A seventeenth key contains the word for the ordinary reason — `logs:tm.variantDeleted`, whose own English says *entry*. The English is **not** aligned and must not be. **A second, different sense arrives with it: a *log entry*.** `console:empty` is 「ログエントリはまだありません。」 — the reservation on エントリ is a claim on the content-unit noun, and ログ disambiguates here exactly as runbook 2.9 describes, so no second word is invented. Its counter is 件 (see `style/ja.md`). |
| source text | 原文 | Batch 1. Deliberately **not** the ソース of ソース言語／ソースラベル: 原文 is the idiomatic Japanese for the original text and is what makes every LQA description read naturally (`config:lqa.checks.whitespace-parity.description`). Reserved for the *text* sense only. Batch 3: `review:sourceText` ("Source text") and the bare-"Source" column headers land on the same 原文 — `glossary:colSource` joins the two shipped siblings `strings:runs.judgeSourceLabel` and `strings:runs.detailsSourceColumn`, because that column holds the source-language form of the term. **A third English "Source" is a different word entirely:** `glossary:sourceLabel` ("Source:") is provenance — it introduces the link to the external sheet at `GlossaryTab.tsx:940-947` — and ships 「出典：」. Read the call site, not the word. |
| translation | 翻訳 | Batch 1. Noun and verb stem (翻訳する). Batch 2 settles the **tab label**: `strings:tabs.strings` is 「翻訳」, which `config:routing.categoriesConfiguredHint` 「カテゴリは翻訳タブで設定します。」 already named in batch 1 — the two agree. Japanese marks neither number nor the noun/verb split, so *Translations* and *Translation* land on the same 翻訳 (`strings:tabs.strings`, `strings:runs.judgeTargetLabel`, `strings:runs.typeTranslation`); that collapse is licensed. **English's *Translate* does NOT** — `strings:guide.groupTranslate` ships 「翻訳作業」, and `sidebar:groups.translate` (batch 4, byte-identical English) must copy **「翻訳作業」**, never 翻訳. It names the sidebar group that contains `strings:tabs.strings`, so 翻訳 there would put the heading directly above an identically-named child. See the group-heading section of `style/ja.md`; this row is the file batch 4 opens first, which is why the exception is stated here and not only there. **Batch 4 read this row first and shipped `sidebar:groups.translate` 「翻訳作業」 — the instruction worked as designed.** On the rail it now sits directly above 「翻訳」 (`strings:tabs.strings`), a licensed prefix rather than the equality a copy of 翻訳 would have produced. The dead name *Multi-language Text* appears nowhere. |
| source label | ソースラベル | Batch 1. `config:routing.labelSources` shortens to 「ソース」, matching English's own shortened column heading; `strings:filters.allSources` 「すべてのソース」 follows it. Batch 3 uses the same short form in `quality:bySource.title` 「ソース別の問題」 — **checked, not assumed**: `services/lqa-summary.ts:78` groups by `entry.sources`, so the dashboard's "source" really is this term and not the source text. Not to be confused with the *file*: `strings:columns.tooltipSource` is English "Source file" and ships 「ソースファイル」, and `strings:columns.tooltipAchievementSource` 「実績のソース」. Batch 5 ships the lexicon's own Example key: `generation:fieldSources` 「ソースラベル」, unchanged from batch 1's rendering. |
| achievement | 実績 | Batch 1. The word Japanese game platforms print (Xbox 実績). トロフィー is PlayStation-specific and would name a different platform's feature. |
| inline tag | インラインタグ | Batch 1. **Batch 5 ships the lexicon's own Example key, and it is the worked case for *a term row fixes the lexeme*.** `logs:translation.maskMismatch`'s English says *Formatting tags*, not *inline tags* — but `terminology.md` names this very key as the example of the **inline tag** term, so the object is fixed and the rendering is 「{{language}}への翻訳でインラインタグが保持されませんでした。」 Reading the label instead of the term row would have coined a second word for one thing. |
| placeholder | プレースホルダー | Batch 1, the double-brace-token sense only (`config:lqa.checks.mask-integrity.description`). The **input-hint** sense of the same English word is a different concept and is never rendered with this term — fold it into the label instead (`config:enableModulePlaceholder` is 「有効にするモジュールを選択…」, with no noun at all). |
| translator context | 翻訳者コンテキスト | Batch 1. `config:includeContext` is 「翻訳者コンテキストの列を含める」: the bare form without 翻訳者 would collide with コンテキストウィンドウ (`config:models.confidenceReason.prompt-near-context`), which the lexicon forbids. The shared コンテキスト root is licensed because the heads differ. **Batch 2 licenses the bare コンテキスト inside the Compare tab only** — `strings:compare.addContext` 「+コンテキスト」 and `strings:compare.emptyContextFilter` 「コンテキストが空」 are a chip and a filter label sitting beside `strings:compare.editContext` 「翻訳者コンテキストを編集」, which establishes the full term in the same toolbar, and no context-window concept exists anywhere in `strings`. Outside a surface that establishes it, write 翻訳者コンテキスト. **Batch 5 licenses the bare コンテキスト a second time, in `generation` only, and the grounds are the Compare ones plus one more.** `generation:contextLabel` 「含めるエントリのコンテキスト」 and `contextHint` establish the word at the top of the panel, and the checkbox under it is `fieldContext` 「コンテキストのメモ」 — the entry's translator-context field, but named *Context notes* by its own English, so runbook 2.6 forbids importing 翻訳者 from a sibling whose English has it. The extra ground is negative and checkable: no context-window concept exists anywhere in `generation` (the panel offers entry fields, languages and batch grouping), so the collision the lexicon guards against cannot arise here. |
| source language | ソース言語 | Batch 1. Pairs with ターゲット言語, which is why 原文言語 was rejected — it would have taken 原文 away from *source text*. |
| target language | ターゲット言語 | Batch 1. `config:targetLanguages` (plural) and `config:routing.labelTargetLanguage` (singular) are the same rendering: Japanese marks no plural. |
| reference language | 参照言語 | Batch 2. `strings:compare.translateUseReferenceNone` is 「参照言語をコンテキストとして使用する」 — the dictionary-form checkbox shape, not 体言止め. The Compare toolbar picker shortens to 「参照」 (`strings:compare.reference`), mirroring English's own short/long split at `translateUseReference` vs `translateUseReferenceNone`. Never ソース言語 — a reference is a reading aid, never a second source. |
| writable language | 編集可能な言語 | Batch 4. One adjective, used in all four homes the lexicon names: the Members table column (`collab:sharing.columnLanguages` 「編集可能な言語」, 7 characters against the soft 8-character column budget) and the three lock notices (`collab:locks.compareTargetsScoped`, `reviewLanguagesScoped`, `glossaryEditScoped`). The Not-list's point is that alternating is the bug, so 書き込み可能／許可された／割り当てられた are all rejected in favour of one word — and 編集可能 is the one that pairs with the permission's opposite, which is already shipped: `glossary:readOnly` 「読み取り専用」, reused verbatim by `collab:locks.readOnlyLanguage`. 割り当て is additionally unavailable: `glossary:unassignGlossary` 「用語集の割り当てを解除」 has it for glossary assignment. |
| Pseudo Test | 疑似テスト | Batch 1. The language code `pseudo-test` itself stays Latin, untranslated. |
| run | 実行 | Batch 1. Never ラン. Counted with 回, not 件 (`config:models.confidenceReason.batch-exceeds-reliable` is 「1回の実行で」). Batch 2 confirms the **noun-only** rule at the standalone label `strings:compare.run` 「実行」 (the Compare-toolbar run picker — the noun, per `english-review-notes.md`). One friction to know about: 実行中 is also the natural rendering of the *status* "Running" (`strings:runs.statusRunning`), so counting active runs as 実行中の実行 stutters; `strings:runs.activeRuns_other` ships 「進行中の実行{{count}}回」 and its sibling 「待機中の実行{{count}}回」 keeps the pair parallel. **Batch 5 ships the lexicon's own Example key**, `logs:translation.runQueued` 「翻訳の実行をキューに追加しました（順番：{{position}}）。」 — the noun, never a verb, exactly as batch 2's `strings:compare.run` settled. `batch:cancelRun` 「実行をキャンセル」 and `batch:savedRulesNotice` 「実行では保存済みの振り分けルールが使われます。」 use the same noun. 回 remains its counter, but batch 5 counts no runs. |
| Activity | アクティビティ | Batch 2 (`strings:tabs.runs`). **The kanji candidates are all on the lexicon's Not-list**: 履歴 is *history*, ログ is *log* (taken by the live server-log panel), 実行履歴 contains 履歴. アクティビティ is the surviving word and is what Japanese SaaS navigation calls this surface. The page title expands, deliberately: `strings:runs.title` is 「翻訳アクティビティ」. 履歴 stays free as an ordinary noun where English itself says *history* — `strings:tabPlaceholder.runs` and `strings:runs.emptyState` render "activity history" as 「アクティビティ履歴」. **Batch 5 ships ログ for the surface this row's Not-list reserves it for.** The live server-log panel is `console`/`logs`: 「コンソール」, 「ログを検索…」, 「ログをエクスポート」, 「ログを消去」, 「ログエントリはまだありません。」 and 「サーバーのログストリームに接続済み」. The Not-list's point — ログ is *not* available for Activity — is now backed by shipped strings rather than by a reservation. |
| batch | バッチ | Batch 1 — the request-packing sense, which is the only sense the lexicon defines (バッチモード, バッチサイズ, `config:batchGroupingLabel` 「バッチのグループ化」). **Batch 2: English's "Batch translate" at `strings:compare.translateDialogTitle` is not this term** — it is a user action over selected rows, which the lexicon tells you to keep distinct from *batch*, so it ships 「一括翻訳」 alongside `strings:bulk.bulkOperation` 「一括操作」. 一括 is the bulk-action root; バッチ stays the request-packing word. Batch 5: `logs:translation.batchFailed` 「翻訳{{count}}件のバッチが失敗しました（対象言語：{{languages}}）。」 — the request-packing sense, the run that contains it named separately in the sibling `queueStartFailed`. |
| batch grouping | バッチのグループ化 | Batch 1. Deliberately a **process** noun (…化) so it can never be confused with ルールグループ (*rule group*) or バッチモード (*batch mode*). |
| AI review | AIレビュー | Batch 2. `strings:runs.judgeBadge`, `strings:guide.topicAiReview`. Not チェック — that word is taken by *check* below, and `strings:runs.aiReviewChecksLabel` is a literal English "Checks" and correctly ships 「チェック」. The two tabs specialise it: 「原文AIレビュー」 (`strings:tabs`, review-source-ai) and 「翻訳AIレビュー」 (`strings:tabs`, review-translation-ai). Half-width AI, no 中黒, no space. |
| judge | 評価 | Batch 3 ships the one string where the lexicon's own example lives: `review:translationAi.description` 「完了した翻訳を、正確さ・流暢さ・用語・トーンの観点でAIに評価させます。」 — the verb, exactly as English uses it, with the four criteria copied verbatim from the shipped `strings:runs.judgeIssue*`. `review:translationAi.emptyHintRun` is the second. The noun "the judge" still appears nowhere. Batch 1, met only inside lists of run kinds (`config:batchGroupingDescription`). Evaluative sense; 裁く／裁判官 are the legal reading the lexicon warns about. **Batch 2 owns thirty-odd `strings:runs.judge*` keys and 評価 appears in none of them** — deliberately, per the lexicon's "do not introduce 'the judge' as a noun in a locale where English does not use it": the key names say *judge*, the values say AIレビュー, 提案, 指摘 and スコア. **Batch 5 owns the judge engine's four log lines and 評価 appears in none of them either** — the same outcome as batch 2, for the same reason: the key names say *judge*, the English values say *Review*. They ship 「レビューを採点しました（スコア：{{score}}・判定：{{verdict}}）。」 (the 判定 label added in the round-5 fix round — `english-review-notes.md` records that `{{verdict}}` is a raw English enum no locale can translate, so it is labelled as data rather than left as a bare word in the sentence), 「レビューが完了し、提案があります。」, 「レビューの結果、変更点はありませんでした。」 and 「提案を却下しました。」 採点 is the verb for English's *scored* and is not a rival rendering of this term; the noun *the judge* still appears nowhere. |
| source review | 原文レビュー | Batch 1. Batch 5: `logs:sourceReview.done` 「原文レビューが完了しました（指摘：{{findings}}）。」 |
| finding | 指摘 | Batch 3 owns the source-review panel where the term lives: `review:sourceAi.findingsTitle` 「指摘」, `allFindingsTitle` 「すべての指摘」 (byte-identical to the shipped `strings:runs.judgeAllFindingsTitle`, same English), `viewAll` 「すべての指摘を表示」, `loadingFindings`, and the five type labels, which are copied verbatim from the shipped `strings:runs.aiReviewCheck*` — 誤字・文法・用語・明確さ・不適切な表現. `review:sourceAi.runSummary` renders English's *flagged* as 「指摘あり」, matching `strings:runs.judgeSummary`, and is **not** the *flag* term. Batch 2 — the reservation is now shipped. `strings:runs.judgeAllFindingsTitle` 「すべての指摘」, `strings:runs.judgeViewAll` 「指摘を表示・未処理{{count}}件」. Distinct from 問題 (*issue*, a machine verdict) — the two are listed on the same entry. English's *flagged* in `strings:runs.judgeSummary` names the same object and ships 「指摘あり」, **not** the *flag* term below. **Batch 5's one term-level judgement call, decided against the English word and for the lexicon.** `logs:sourceReview.done` says *Source review found {{findings}} issues* — English's *issues* — but `terminology.md` puts **finding** and **issue** on each other's Not-lists precisely because they are listed against the same entry, and the object the source review reports is a finding (the token is even named `findings`). It ships 指摘, matching `review:sourceAi.findingsTitle`. Rendering English's *issues* as 問題 there would have put the LQA word on the AI-opinion object. |
| suggestion | 提案 | Batch 3 confirms the word across all three generators the lexicon names — `category:reviewTitle` ships **「提案をレビュー」 copied byte-for-byte from the shipped `strings:runs.reviewSuggestions`**, whose English is identical, and `glossary:generateNoSuggestions` / `category:noSuggestions` / `review:sourceAi.suggestion` 「提案された原文」 all use it. An earlier draft wrote 「提案の確認」 for `category:reviewTitle` on the argument that a dialog title takes 体言止め; that was wrong — the shipped sibling exists, and `config:deleteProject`/`config:confirmDeleteTitle` are the standing precedent that identical English takes one rendering across control classes. Batch 2. `strings:runs.judgeSuggestionLabel` 「提案」. The action on it is 適用 (*apply*), never 承認 — `strings:runs.judgeApproveAll` says *Approve all suggestions* in English for the very same action and ships 「すべての提案を適用」, per the lexicon's known-copy-bug instruction. Refusing one is 却下 (*discard*, sense 2). Batch 5: `logs:categoryGen.done` 「カテゴリの生成が完了しました（提案：{{suggestions}}）。」, `logs:glossaryGen.saveSuggestionsFailed` 「用語集の提案を保存できませんでした。」 and `logs:glossaryGen.done` 「提案した用語：{{suggested}}」 — one word across all three generators, as the lexicon requires. |
| discard | 破棄 | Batch 3 confirms the prediction this row made: `glossary:generateDiscard` ("Discard result") is sense 2 and ships 「結果を却下」. **Two senses, two words, both now shipped — this column can only hold one, and it holds sense 1.** Sense 1, unsaved edits — 破棄 (`config:discard`, `strings:editor.discard`, both the ghost button beside Save; **batch 4 adds the third key the lexicon names, `vault:discard` 「破棄」**, and it is the sense-1 case in its purest form: `VaultEditorDialog.tsx:351` calls `deleteRow`, which drops an unsaved row from the form and touches no stored credential, while the *existing*-row button beside it is `vault:delete` 「削除」, which really does null the stored key on save. The two render **in the same row list at the same time**, one per row depending on `row.existing`, so the pair had to read differently — and `vault:discard` is also that button's `aria-label`, where a screen reader would otherwise announce a destroy verb for a form-only action). Sense 2, refusing something offered — **却下** (`strings:runs.judgeDiscard`, `strings:runs.judgeDiscardSuccess` 「提案を却下しました」); 破棄 there would read as destroying the suggestion's subject rather than declining it. Batch 3's `glossary:generateDiscard` and batch 6's `colorText:assistant.discard` are sense 2 and take 却下. `config:discardUntranslatable` is a **third** English sense — omitting rows from an export — and takes 除外 (queued in `README.md`). Batch 5 adds a fourth sense-2 key: `logs:judge.suggestionDiscarded` 「提案を却下しました。」 It is the log line for the operation whose toast is the shipped `strings:runs.judgeDiscardSuccess`; the two differ only by the full stop English gives one and not the other, which is faithfulness per key rather than drift. |
| needs review | 要レビュー | Batch 2. One rendering across all three surfaces the lexicon names: the filter (`strings:filters.needsReview`, `strings:compare.needsReviewFilter`), the deliberately lowercase cell badge (`strings:compare.cellNeedsReviewBadge` — Japanese has no case, so the badge is byte-identical to the filter and that is licensed, not drift), and the bulk action `strings:compare.flagAllNeedsReview` 「すべてを要レビューにする」. Japanese needs no gender agreement, so the whole es/fr "reviewed" family question does not arise here. |
| flag | 保留 | Batch 3 (`review:filterFlagged` 「保留」 the state, `review:flag` 「保留にする」 the button, `review:flaggedToast` 「翻訳を保留にしました」). Elimination, against the three words already taken: 検出 is the LQA sense (`config:lqa.checks.overflow.description` 「…を検出します」), **要レビューにする is the mark-as-needs-review action** (`strings:compare.flagAllNeedsReview`) which this term *clears*, and 指摘 is *finding*. マーク says nothing and collides with レビュー済みにする's *mark*; フラグを立てる is jargon on a button, and the bare noun フラグ is already the UI-marker use (`strings:filters.clearNewFlags` 「新規フラグを解除（{{count}}）」) rather than this term. **And the survivor means this:** 保留 is "hold this aside and decide later", which is exactly the lexicon's "a disposition, not an alarm". Two consequences. (1) **The verb/participle split DOES have to survive here, and the first draft of this row was wrong to say it did not.** Batch 3 shipped both keys as the bare 「保留」 and licensed the collapse; the fix round found they co-render — `ReviewTab.tsx:755` always paints the 保留 filter tab and `:1073` paints the button under a filter-is-not-flagged guard, i.e. exactly when that tab is visible and inactive. The button therefore takes the ～にする shape (`style/ja.md`'s row for a state with no サ変 action noun, the same shape as `strings:compare.flagAllNeedsReview` 「すべてを要レビューにする」) and the filter keeps the bare state. The lexeme is unchanged; only the shape differs. (2) **Never write the bare 「保留中」**: that is `strings:runs.statusPending` ("Pending"), a run status. `review:emptyFlaggedTitle` therefore ships the longer 「保留中の翻訳はありません」, which does not collide. **English's other two "flagged"s are not this term** — `review:sourceAi.runSummary` ships 「指摘あり」 (the *finding* sense, matching `strings:runs.judgeSummary`) and `glossary:flaggedTitle` ships 「未完成の用語{{count}}語」. **The reason first given for that last one was false and is corrected here rather than quietly rewritten:** batch 3 called it "the LQA-style detection sense" and rendered it 検出. Nothing detects anything — `GlossaryTab.tsx:587-594` is a plain partition of the term list on an is-complete predicate over the configured target languages, and it only runs at all for a read-only glossary — so 検出 asserted a check the product never performs. English's parenthetical "(incomplete)" exists precisely because *flagged* is opaque; Japanese states the gloss directly and drops the bracket. The counter 語 and the decision not to reuse 保留 were both right. |
| Review (the sidebar group) | レビュー | Batch 2 (`strings:guide.groupReview`); `sidebar:groups.review` must copy it. Elimination: 校閲 claims the group for the human member, 検証／確認 claim it for the machine-checking member (*Quality*), 品質管理 claims it for Quality outright. The survivor レビュー is a loanword with no strong sub-sense in Japanese, so it stays the umbrella while each member specialises it — 原文AIレビュー／翻訳AIレビュー／手動レビュー — exactly as English's own vague "Review" does. **Batch 4 shipped `sidebar:groups.review` 「レビュー」, copied from `strings:guide.groupReview` without re-deciding**, and re-ran the nesting check on the rail it actually renders in (`Sidebar.tsx:773` over `:788`): the heading is a prefix of three of its four children and clear of the fourth (品質) — the same licensed prefix English has (Review > AI Review). |
| review queue | レビューキュー | Batch 3 (`review:title`, `review:emptyTitle` 「レビューキューは空です」, `review:filterLabel` 「レビューキューを絞り込む」). Built from the レビュー root the lexicon asks for, so 要レビュー / 手動レビュー / レビューキュー read as one feature. **Not 「手動レビュー」** — that is the tab label (`strings:tabs`, review-manual) and a different English string; the split is English's own and Japanese keeps it. 待ち行列 was rejected as the mathematics/telecom register; キュー is the ordinary Japanese software word (印刷キュー). `review:retranslateQueued` reuses it: 「再翻訳をキューに追加しました」. |
| module | モジュール | Batch 1. |
| module instance | インスタンス | Batch 1. Long form モジュールインスタンス where the sentence has not yet established モジュール (`config:routing.deletedInstanceWarning`). Batch 5: `logs:module.instanceRegistered` 「{{instance}}の設定を追加しました。」 — English says *configuration*, not *instance*, so the value says 設定 and not インスタンス. Runbook 2.6: match the key's own English, not the sibling term. |
| provider | プロバイダー | Batch 1. Trailing ー per the -er rule. Kept even in the three English strings that mislabel a module picker as *Provider* — `config:routing.simplePlaceholder` ships 「プロバイダーを選択」, per the lexicon's instruction to translate those three as written. |
| model | モデル | Batch 1. Never used for *template*, which takes テンプレート. |
| prompt | プロンプト | Batch 1. Distinct from リクエスト (`config:requestTimeoutLabel`) and from 検索 (`config:models.noMatches`). |
| reasoning effort | 推論の強度 | Batch 1. 努力 rejected: it reads as effort expended by a person. |
| routing rule | 振り分けルール | Batch 1. ルーティング in Japanese reads as **network** routing, which the lexicon forbids; 振り分け is content distribution. The same root carries `config:routing.routesTo` 「振り分け先」 and `config:routing.modeAriaLabel` 「振り分けエディターのモード」. |
| rule group | ルールグループ | Batch 1. |
| credential vault | 保管庫 | Batch 1. The word Japanese password managers ship (1Password / Bitwarden 保管庫). Checked against all four required frames before deciding: 認証情報の保管庫 / 保管庫はロック中 (`config:credentialsVaultLockedChip`) / 保管庫のロックを解除 (`config:credentialsUnlockButton`) / 保管庫のパスワード. **Batch 4 ships all four for real and the deferred full form with them.** `vault:statusLabel` is 「認証情報の保管庫」, byte-identical to the shipped `strings:guide.topicVault` as the surface table requires; `vault:statusLocked` 「ロック中」 and `vault:unlock` 「ロックを解除」 keep batch 1's wording; `vault:changePasswordTitle` is 「保管庫のパスワード変更」. **The long form is used where the term is being named, the short form where it has been established**: the three dialog titles take it (`vault:unlockTitle` 「認証情報の保管庫のロック解除」, `createTitle`, `editorTitle`) because a dialog opens without context, while everything inside them is 保管庫. That split is English's own — it writes *Credential vault* in the titles and bare *vault* in the body. Batch 5 copies batch 1's chip verbatim where English is byte-identical: `console:vaultLocked` 「保管庫はロック中」 is `config:credentialsVaultLockedChip`'s value, and `logs:action.unlockVault` 「保管庫のロックを解除」 is `config:credentialsUnlockButton`'s — both pairs are the same English string in two homes. `logs:vault.unlocked` 「保管庫のロックを解除しました。」 and `logs:vault.passwordChanged` 「保管庫のパスワードを変更しました。」 narrate the same two events. |
| credential | 認証情報 | Batch 1. `config:credentialsMissing` calls the vault's key slots 項目, never エントリ. **Batch 4 owns the namespace and keeps both halves.** 認証情報 for the secret itself (`vault:addKey` 「認証情報を追加」, `vault:noKeys`, `vault:manageOnVaultPage`), and 項目 for the slot it occupies — `vault:editorDescription` 「認証情報の項目を追加・更新・削除します。…」 and `vault:keysCount` 「保管庫に{{count}}項目」, where 項目 is also the **counter** (the unit is the counter, like 文字／バイト／トークン). English is itself split here — `keysCount` says *key* while its own siblings `addKey`/`noKeys` say *credential* for the same object — and runbook 2.6 says to follow each key's own English, which is why the count line names the slot and the buttons name the secret. The literal key names stay untranslated: `vault:keyPlaceholder` ships English's "KEY_NAME" byte-identical, as a format template for an ASCII identifier field (see the guard escalation in `style/ja.md`). Batch 5: `logs:vault.credentialsEvicted` 「保存されていた認証情報がメモリから消去されました。」 — 消去 because an in-memory store is wiped, per the four-verb split in `style/ja.md`. |
| LQA | LQA | Batch 1. Kept as the industry acronym — Japanese localization vendors use LQA untranslated, so no expansion is recorded. |
| quality gate | ゲート | Batch 1. English also says bare *the gate* once *quality checks* has been said, and Japanese does the same: `config:lqa.description` carries 「ブロッキングの問題はゲートを通過できず」 after it has said 「品質チェック」. 門／関門 are the physical-door readings the lexicon warns about. Batch 5 narrates the gate's verdicts rather than naming it: `logs:lqa.passed` 「品質チェックに合格しました。」 and `logs:lqa.failed` 「品質チェックに不合格でした。」 The bare ゲート is not used in this batch. |
| check | チェック | Batch 1. One word across 品質チェック, LQAチェック and every individual check name. Batch 3 ships the 27 `quality:checkLabels.*`, and the load-bearing rule there is **runbook 2.6, matched to each key's own English rather than to its `config` sibling's rendering**: `config:lqa.checks.tag-equality.name` is "**Inline** tag equality" 「インラインタグの一致」, while `quality:checkLabels.tag-equality` is the bare "Tag equality" and ships 「タグの一致」 with no インライン. Every other label whose English is identical to its `config` sibling copies that sibling byte-for-byte. `review:sourceAi.checksLabel` is the literal English "Checks" and ships 「チェック」, as `strings:runs.aiReviewChecksLabel` already does. Batch 5: 品質チェック across `logs:lqa.passed`, `logs:lqa.failed`, `logs:lqa.checkError` 「品質チェックを実行できませんでした。」, `logs:translation.lqaRetry` and `logs:translation.tmRejected` 「品質チェックに合格しませんでした。」 — one word, as batch 1 settled. |
| issue | 問題 | Batch 3 puts the two words on one screen and they stay apart: `review:lqaTitle` 「LQAの問題」 and `review:sourceAi.findingsTitle` 「指摘」 are listed against the same entry, exactly as the lexicon warns. `quality:mostCommonIssues.title` 「最も多い問題」 and `quality:bySource.title` 「ソース別の問題」 follow. Batch 1. Distinct from エラー, and reserved against 指摘 (*finding*), which batch 2 now ships. `strings:row.lqaIssues_other` is 「LQAの問題{{count}}件」 — an LQA verdict is counted with 件. |
| severity | 重大度 | Batch 1. 重要度 rejected — that is priority, not how hard a check fails. The two **values** are fixed by *check*: ブロッキング / 警告. |
| notification severity | 警告 | **This column can only hold one rendering and the term is a set of three, so — following the *discard* row's precedent — it holds the one the lexicon actually constrains.** The three values shipped in batch 4 are 「情報」 (`account:notificationsSeverity.info`), 「警告」 (`.warning`) and 「重大」 (`.critical`); a slash-joined cell was tried first and `check-lexicon-citations` correctly rejected it, because no shipped value equals that string. **The reservation this row carried is honoured: 「警告」 is byte-identical with the shipped `config:lqa.severityWarning`**, so the product ships one word for one severity word, exactly as the lexicon demands. 重大 for *Critical* deliberately shares the root of *severity* 重大度 without colliding — they are a value and its dimension, never on one screen (an LQA severity setting lives in Global Config, a notification badge in Account) — and it stays clear of `config:lqa.severityBlocking` 「ブロッキング」, which is the LQA value set and must not drift here. 緊急 was rejected: it means *urgent*, a claim about how fast to act rather than how serious the message is. `settings:previewSamples.warn` is a **third** home for the same English word (a theme-preview status chip) and ships 「警告」 too. **Batch 5 adds a fourth home for 「警告」 and it is a log level, not a severity value.** `console:filter_warn` (*Warn*) is one of the console's five filter tabs — 「すべて」「エラー」「警告」「情報」「デバッグ」 — and 情報 likewise repeats `account:notificationsSeverity.info`, whose English is the identical *Info*. The collapse over *Warn* / *Warning* is licensed and recorded in `style/ja.md`: the two English strings name the same concept and differ only by an abbreviation, which carries nothing into Japanese, and 警告 is the standard Japanese log-level word. エラー stays clear of both, as `terminology.md`'s *check* row requires. |
| assertion | アサーション | Batch 1. A fourth word was genuinely needed: 条件 belongs to routing conditions, チェック to the LQA checks, ルール to routing rules. |
| pattern | パターン | Batch 1, regex sense only. No collision with テンプレート. |
| overflow | オーバーフロー | Batch 1. Kept clearly apart from 長さ制限 — both appear in the same checks list, and batch 3 puts a third neighbour beside them: `quality:checkLabels.too-long` 「長すぎる」 (copied from the shipped `strings:filters.tooLong`). The three read distinctly at 「長さのオーバーフロー」／「長さ制限」／「長すぎる」. `review:overflowIssue` is the only string in batches 1–3 that writes a counter directly against a non-`count` token — 「{{ratio}}倍」 — see the numeral section of `style/ja.md` for why it was kept. |
| length limit | 長さ制限 | Batch 1. Covers the character cap, the UTF-8 byte cap **and** the routing condition (`config:routing.labelMaxLength` 「エントリの長さ制限」). 文字数制限 rejected because it silently excludes the byte bound. |
| pass rate | 合格率 | Batch 3 — the reservation is now shipped: `quality:columns.passRate` 「合格率」, `quality:overallStat.passRateLabel` 「全体の合格率」, `quality:byLanguage.title` 「言語別の合格率」, and the three legend tiers 「合格率が高い（90%以上）」／「合格率が中程度（70〜89%）」／「合格率が低い（70%未満）」. **The trap is real in Japanese too**: `config:health.successRate` is a different metric and ships 「成功：{{rate}}%」, so 成功率 must never be used for *pass rate*. The 合格 root was already established by `config:tm.lqaPassedBadge` 「LQA合格」 and `strings:runs.judgeVerdictPass` 「合格」, and `quality:legend.passed`／`legendFailed` copy it exactly. **合格/不合格 is not the run-status word**: `strings:runs.statusFailed` ("Failed") is 「失敗」 — a run that errored — while `quality:legend.failed` ("Failed") is 「不合格」, a verdict. Same English, two Japanese words, deliberately. Batch 5 exercises the 合格/不合格 pair as a **check verdict** for the first time (`logs:lqa.passed`/`failed` above). Note the deliberate split inside one namespace: a check's verdict is 合格/不合格, while a translation that *didn't pass* is written as a negated verb — `logs:translation.tmRejected` says 合格しませんでした — because its own English says *didn't pass the quality check*, not *failed*. |
| glossary | 用語集 | Batch 1. Batch 3: **`glossary:glossaries` ("Glossaries") is also 「用語集」** — Japanese marks no plural, so the glossary-list heading is byte-identical to `strings:tabs.glossary`. Licensed and faithful: English names the tab "Glossary" and the panel inside it "Glossaries" for the same thing, and both are on screen together in the same way the Translation Memory guide pair is. Counter is **件** (`glossary:generateAccept` 「用語集{{count}}件を作成」), which the batch-1 row under *entry* already set at `config:glossariesSkipped_other`. |
| glossary term | 用語 | Batch 1. Never エントリ — `config:lqa.checks.forbidden-terms.name` is 「禁止用語」 and `config:tm.clearAllSuccess_other` counts エントリ in the same file. Counter is 語, never 件. Batch 2: the same lexeme carries English's literal "Terminology" at `strings:runs.aiReviewCheckTerminology` and `strings:runs.judgeIssueTerminology` 「用語」, and the guide group `strings:guide.groupContent` 「用語」 — one word family, not a rival rendering. Where the English means *words in the source text* rather than a glossary row, 用語 is wrong and the batch ships 語句: `strings:order.presortHint` 「原文の語句が似ているエントリをまとめ…」. **Batch 3 ships the 語 counter that batch 1 reserved and batch 2 never met**: `glossary:toastPushed` 「用語{{count}}語をDeepLに送信しました」, `glossary:generateSuggestionCount` 「用語{{count}}語」, `glossary:toastTermsTranslated`, `glossary:bulkMarkConstant`, `glossary:flaggedTitle`. 件 there would count records, and a term is a word. Batch 5: `logs:glossaryGen.done` renders English's *terms suggested* as 「提案した用語：{{suggested}}」 — the count sits behind a colon, so no counter is written and the 語 counter does not appear in this batch. |
| constant | 定数 | Batch 3 (`glossary:colConstant`, `review:glossaryConstant`, `review:sourceAi.glossaryConstant`, `glossary:bulkMarkConstant` 「{{count}}語を定数にする」, `glossary:bulkClearConstant` 「{{count}}語の定数を解除」). Elimination: 固定 is the banned *fixed*; 不変 the banned *unchangeable*; 読み取り専用 is the banned *read-only* **and is already taken by the near-miss two columns away** (`glossary:readOnly` 「読み取り専用」); 翻訳しない is the banned *do-not-translate*. **And the survivor means this:** a 定数 is a value fixed in advance and carried through unchanged, which is exactly what the flag does — the term is masked before the model sees it and restored verbatim. The lowercase badge is byte-identical to the column header because Japanese carries no case; licensed. Setting and clearing the flag use the ～にする／～を解除 pair from `style/ja.md`, not 削除. |
| match | 一致 | Batch 1. One root for the noun and the verb: `config:models.noMatches` 「検索条件に一致するモデルがありません」, `config:lqa.regexModeMustMatch` 「一致する」, `config:lqa.checks.number-parity.name` 「数字の一致」. **Batch 3 owns the glossary-match noun and it needs a head noun: 一致箇所** — `glossary:matchesPanel` 「一致箇所」, `glossary:matchResultsCount` 「一致箇所{{count}}件」, `glossary:matchNoResults` 「一致箇所が見つかりません。」, `glossary:confirmEnableApplyConfirm` 「一致箇所を適用」. A bare 一致 as a panel title is the substantivized-adjective shape reviewer-rubric item 4 rejects; 箇所 names what a match actually is — one *place* in an entry's source text where the term turned up — and keeps the noun on the same root as the verb. Counter is **件**. Do not confuse it with 用語: a 用語 is what is searched for, a 一致箇所 is where it was found. `glossary:glossaryTermsTitle`'s sibling `review:glossaryTermsTitle` ("Matching terms") is the attributive verb and stays 「一致した用語」. |
| translation memory | 翻訳メモリ | Batch 1. トランスレーションメモリ rejected on length (twelve characters against five) and on idiom; メモリ alone would read as RAM. Batch 5: `logs:tm.cleared` 「翻訳メモリを消去しました。」 and `logs:tm.variantDeleted` 「翻訳メモリのエントリを削除しました。」 — two different verbs on purpose, matched to English's own *cleared* / *Removed* and to what the code does: 消去 wipes the whole store (as `config:tm.clearAll` already ships), while `routes/tm.ts:51` deletes one stored variant, which is a destroy and takes 削除. |
| approve | 承認 | Batch 3 note first, because it is the one a later batch will doubt: **`review:approve` / `approveAll` / `approveUnchangedPassing` really are this term** — `ReviewTab.tsx:418` POSTs the project approve endpoint, the same one `StringTable.tsx:638` calls behind `strings:bulk.approveSelected`, so the review queue's Approve *is* the promotion into translation memory. Opened both call sites rather than reading the label. **`review:sourceAi.approve` is a different operation with the same English** — `SourceAiReviewTab.tsx:451` calls approveEntry(), which only marks a source-review record reviewed — and it also ships 「承認」, because this row's reservation is scoped to the *term*, not to the lexeme, and inventing a second word where English has one is the error class runbook 2.6 names. The two never co-render. Batch 1 (`config:tm.browserEmpty`). Distinct from 適用 (*apply*), レビュー済みにする (*mark as reviewed*) and 保存 (*save*). **Batch 2 is the bulk bar where all four meet, and all four are visibly different there.** 承認 is not a movement verb in Japanese, so 「翻訳メモリに承認」 is ungrammatical; `strings:bulk.approveSelected` ships 「承認して翻訳メモリに追加」, keeping 承認 in first position so the promotion — not the storing — is what the user reads first. 追加 is the app's generic *add* and carries no reserved sense. |
| category | カテゴリ | Batch 1. No trailing ー, per the -y rule under *entry*. Batch 3: `category:title` ("Categories", the page title) is the same 「カテゴリ」 as `strings:tabs.category` ("Category", the tab) — English's deliberate singular/plural split carries no information into Japanese, so the pair is identical and licensed. **The verbs do not collapse, though.** *Delete category* destroys the category (`category:deleteCategory` 「カテゴリを削除」) and *remove from category* only unassigns it (`category:removeFromCategory` 「カテゴリから外す」, `category:deleteConfirmBody_other` 「…エントリ{{count}}件から外し…」). 外す is the un-assign verb; 削除 stays the destroy verb. Counter is 件, set in batch 2. Batch 5: `logs:categoryGen.queued` 「カテゴリの生成をキューに追加しました。」, `generation:fieldCategories` 「割り当て済みのカテゴリ」 and `generation:skipCategoriesLabel` 「スキップするカテゴリ」. |
| tone | トーン | Batch 1. The acoustic readings (音色・調子) are the trap; 文体 would read as the model's writing style. |
| orphan | 孤立エントリ | Batch 1. The state alone is 孤立 (`config:orphanedCount` 「孤立{{count}}件」, `config:importModeFullReplaceHint` 「孤立として扱われます」); the surface name adds エントリ. Note `config:fullReplaceOrphanNotice` says *Relink tab* in English, which does not exist — it ships 「孤立エントリタブ」. **Batch 5 ships the log lines this row's Not-list demands read like the tab.** 「孤立エントリ」 in `logs:orphan.linked` and `logs:orphan.deleted` 「孤立エントリを削除しました。」, and the state phrased as a relative clause in `logs:orphan.detected`, whose English never says *orphan* at all. |
| relink | 再リンク | Batch 1. **Batch 5's second judgement call against the English word, and it was decided by opening the code.** `logs:orphan.linked`'s English is *Linked an orphaned string*, but the event is emitted by `M11-orphan-manager.ts:147`, at the end of the *relinkOrphan* method — this is the relink operation, and the lexicon reserves one verb for it. It ships 「孤立エントリを再リンクしました。」 A bare リンクしました would also have read in Japanese as *created a hyperlink*, which is not the effect the English has on its reader. |
| backup | バックアップ | Batch 1. The verb is バックアップを作成; kept separate from エクスポート and スナップショット. **Batch 5 fixes this term's log-line rendering and thereby binds batch 6.** `logs:backup.restored` 「バックアップを復元しました。」 and `logs:backup.pruneFailed` 「古いバックアップを整理できませんでした。」 `backup:toastRestoreSuccess` is **byte-identical in English** (*Backup restored.*), so batch 6 copies 「バックアップを復元しました。」 onto it rather than re-deciding. |
| snapshot | スナップショット | Batch 1. `config:templatesDescription` uses the English word loosely for a saved configuration rather than a restore point; the same rendering is correct there. |
| template | テンプレート | Batch 1. モデル is the dangerous near-miss the lexicon names and is already taken by *model*. |
| collaborator | 共同作業者 | Batch 2 (`strings:runs.projectTotalCollaborators`, `strings:runs.manualRecordingPaused`). Elimination: the lexicon's Not-list bans *editor*, which rules out the otherwise-obvious 共同編集者 — and the collision is live rather than theoretical, because `strings:runs.manualEditedByColumn` ("Edited by") ships 「編集者」 in this same namespace; コラボレーター is an unnecessary loan where a kanji compound is idiomatic. The survivor 共同作業者 means "a person who works on it jointly", which is what a collaborator is — and it leaves メンバー free for *member*, which batch 4 needs as a separate word. **Batch 4 cashes that in and the two words are visibly different on one page**, exactly as the lexicon predicted: `collab:sharing.membersTitle` 「メンバー」 heads the table while `collab:sharing.auditToggleHelp` 「共同作業者がこのプロジェクトで行った手動のテキスト編集を記録します。」 and `collab:invites.errorNicknameRequired` 「共同作業者を招待する前に…」 sit above and below it. Ten batch-4 keys use 共同作業者; none of them says メンバー and no メンバー key says 共同作業者. |
| member | メンバー | Batch 4, on the word batch 2 reserved for it when *collaborator* took 共同作業者. `collab:sharing.membersTitle` ("Members") and `collab:sharing.columnMember` ("Member") are **both** 「メンバー」 and they **co-render, nested**: `MembersSection.tsx:154` paints the card title directly above the table whose first column header is `:168`. That is an exact equality on one screen, and it is licensed on the same grounds as `glossary:glossaries` over `strings:tabs.glossary` — English's difference is **number only**, which carries nothing into Japanese, and inventing a second word for the column would name one thing twice. Distinct from 共同作業者 throughout: `collab:invites.description` 「このプロジェクトで共同作業する人を招待します。」 and `collab:sharing.auditToggleHelp` use 共同作業者 in the same page as the メンバー table, exactly as English uses both words there. |
| nickname | ニックネーム | Batch 4 (`collab:nickname.title`, `collab:nickname.claimButton` 「ニックネームを取得」). Elimination against the Not-list: ユーザー名 is *username* — the one the lexicon singles out, and the app does have a separate account identity; 表示名 is *display name*; アカウント名 is *account name*; 別名／筆名 are *alias*／*pen name*. 愛称 is the kanji compound and is **rejected against `style/ja.md`'s own kanji-over-katakana preference**, which only binds where both are idiomatic: 愛称 is a literary word for a term of endearment and names nothing to a reader of a web handle. **And the survivor means this:** ニックネーム is what Japanese services call exactly this — a chosen public handle — so it is the established word, not a transliteration standing in for one. |
| claim | 取得 | Batch 4. The lexicon demands all four forms and they are shipped: button `collab:nickname.claimButton` 「ニックネームを取得」, progressive `collab:nickname.claiming` 「取得中…」, success `collab:nickname.claimSuccess` 「ニックネームを取得しました。」, immutability hint `collab:nickname.immutableHint` 「一度取得したニックネームは変更できません。」, plus three error strings and two prompts on the same root. Elimination: 予約 is the banned *reserve* **and is needed two strings away** — `collab:errors.nickname_reserved` ships 「その名前は予約されています。別の名前を選んでください。」, which is precisely the adjacent message the lexicon says *claim* must stay distinguishable from; 登録 is the banned *register*; 占有 is *occupy*; 申請 is *request*. **And the survivor means this:** 取得 is to obtain something for oneself and keep it, which is what claiming a nickname permanently is, and it is the verb Japanese services use for taking an account name. One English string in the family deliberately does **not** use it: `collab:invites.nicknameRequiredLink` says *Set up your nickname*, not *Claim*, and ships 「ニックネームを設定」 — faithful to its own English. |
| invite | 招待 | Batch 4. Noun for the record (`collab:invites.title` 「招待」 — English pluralises and Japanese does not) and root of the verb (`collab:invites.description` 「…人を招待します。」). The code is 「招待コード」 (`collab:invites.codeDialogTitle`, `collab:join.codePlaceholder`). **The four statuses are one set, one part of speech** — all past-state noun phrases, as the lexicon requires: 「保留中」／「使用済み」／「取り消し済み」／「期限切れ」. 保留中 is byte-identical to the shipped `strings:runs.statusPending`, same English and the natural Japanese for an outstanding invite; the two live in different tabs and cannot co-render. Do not read the *flag* row's «never write the bare 保留中» as forbidding this — that ban is scoped to the **flag** term, where 保留中 would collide with a run status; here the English **is** *Pending*, so the collision is the point. The verb is 取り消す (`collab:invites.revoke`), never 削除: revoking marks the invite revoked (`revokedAt`), it does not delete the row — `InvitesSection.tsx:224-254` still lists it, greyed. |
| recording | 記録 | Batch 2 (`strings:runs.manualRecordingPaused` 「記録を一時停止中：参加中の共同作業者がいません」). 記録 does not collide with エントリ, so the lexicon's warning ("Recording paused" must not read as "Entry paused") is satisfied by construction in Japanese. Batch 4 ships the owner-side control on the same root: `collab:sharing.auditToggleCheckboxLabel` 「手動編集を記録する」 (the dictionary-form checkbox shape, because English's *Record manual edits* is a verb phrase) and `collab:sharing.auditToggleHelp` 「…手動のテキスト編集を記録します。」. The feature's **name** is a separate noun phrase — `collab:sharing.auditToggleLabel` ("Manual-edit audit") is 「手動編集の監査」 — so 監査 names the feature and 記録 names the capture being on, which is the split English makes too. |
| stage | ステージ | Batch 2 — met as the tab label `strings:tabs` (stage-details) 「ステージ詳細」, which `stage-details:title` must copy verbatim. The gaming word. 段階／工程 are the process readings the lexicon calls the single most likely mistranslation in the app; ステージ carries the playable-level sense unambiguously in Japanese game copy. Batch 5 reuses batch 2's tab label verbatim in the three run log lines: `logs:stageDetails.queued` 「ステージ詳細をキューに追加しました。」, `done` and `failed`. The surface name is unchanged; what the fix round added to two of the three is the head noun 実行 — see the *run* row and `style/ja.md`. |
| Text Styler | テキスト装飾 | Batch 2 (`strings:tabs`, color-text); `colorText:title` and `sidebar:colorText` must copy it exactly. Elimination: スタイルエディター is the lexicon's banned *style editor*; テキストエディター／書式ツール are the banned *text editor*／*formatter*; テキストスタイラー is a bare transliteration that names nothing to a Japanese reader, against `style/ja.md`'s standing preference for the kanji compound wherever both are idiomatic. **The length budget did not decide this and must not be cited as if it had** — an earlier version of this row argued テキストスタイラー was too long, which was wrong twice over: it is 9 full-width equivalents against a container that fits 14, and the container had been mis-described as a scrolling tab bar when it is in fact the sidebar. Both candidates fit comfortably; the choice is the kanji-over-katakana rule and the lexicon's Not-list, nothing else. The survivor テキスト装飾 means "text decoration/styling", which is exactly what the tool does — apply colour and formatting tags to game text. **Batch 4 shipped `sidebar:colorText` 「テキスト装飾」**, copied verbatim; `colorText:title` (batch 6) still owes the same value. |
| element |  |  |
| theme | テーマ | Batch 4 (`settings:theme`, `settings:themes.*`). **The four names are the lexicon's highest-risk duplication and batch 6 must copy them byte-for-byte into `welcome:themeChooser.names.*`**: 「クラシック」（Classic）／「ピクセル」（Pixel）／「テクノ」（Techno）／「ミニマル」（Minimal）. All four are the ordinary Japanese renderings of the four English words, katakana because each is a style name rather than a describable object — the kanji-over-katakana preference in `style/ja.md` binds only where both are idiomatic, and 古典風／画素／技術／最小 all name something else. 「テクノ」 is the music/aesthetic loan, not 「テクノロジー」. The row is repeated in the surface table of `style/ja.md` so batch 6 meets it whichever file it opens. |
| guide | ガイド | Batch 1 (`config:pseudoTestHelpLink`). One word — ヘルプ／マニュアル／ドキュメント must not alternate with it. Batch 2 ships the whole `strings:guide.*` topic tree; the word *guide* itself does not appear in any of those values, so nothing there re-decides this row. Batch 4 ships the sidebar item that opens it, `sidebar:guide` 「ガイド」 — the one place the term is the whole string. |
| release |  |  |
| changelog | 変更履歴 | Batch 4 (`sidebar:changelog`, the rail item; the page itself is batch 6's `common:changelog*`). The Not-list bans *history* as a rendering, and the bare 履歴 would indeed be it — but 変更履歴 is the established Japanese compound for a changelog, with 変更 carrying the distinction, and the *Activity* row above already records that 履歴 stays free as a compound element where English's own sense is historical (`strings:tabPlaceholder.runs` 「アクティビティ履歴」). Rejected: お知らせ (*news*), 更新情報 (*updates*), リリースノート (*release notes*), ログ (taken by *log*). **Checked against the whole sidebar rail**, where it is painted at the same moment as 「アクティビティ」 and 「バックアップ」 — no collision with either. |
