# NARN terminology — Turkish (`tr`)

Your locale's rendering of every term in the shared lexicon. **This file is yours alone** —
no other locale's file is touched by your work, and nobody else writes rows here.

Read it alongside two other files:

- [`../terminology.md`](../terminology.md) — what each term *means*, its part of speech,
  an example key, and what it must not be confused with. It is **frozen** for the duration
  of the backfill: you read it, you never edit it. See the freeze notice at its top.
- [`../style/tr.md`](../style/tr.md) — how Turkish is written here: register, punctuation,
  capitalization, plural and agreement rules. That one is yours to write too, for this
  locale only.

Fill a row when you meet the term, in the same change that introduces the wording — so
this file records decisions actually taken, never predictions. Use **Notes** for anything
the next translator would otherwise have to rediscover: a declension that forced a
different word, a term you deliberately left in English, an acronym you expanded, a
candidate you rejected and why.

Terms are in the order they appear in `../terminology.md`. Do not add, remove or reorder
rows: a term the lexicon lacks goes in the additive queue in [`README.md`](README.md).

**Renderings are given in the bare citation form** (nominative singular, or the bare verb
stem for a verb). Turkish is agglutinative, so the shipped strings almost always carry the
word with case, possessive or plural suffixes on it — “proje” appears as “projeyi”,
“projenin”, “projeden”. Consistency here means **the same stem**, never the same letters.

| Term | Rendering | Notes |
| --- | --- | --- |
| project | proje | Verb is “oluştur”, never “aç” (open). |
| workspace | çalışma alanı | Deliberately two words, so it can never collapse into “proje”. Attributive use keeps the izafet: “çalışma alanı ayarları”. |
| entry | girdi | The unit the app counts. “kayıt” is refused — it is the obvious Turkish for *record*, and the *recording* term reserves that family. “satır” (row) is the CSV row (`config:rowsProcessed`), a different object. The reservation binds the **content unit only**: a disambiguated compound such as “kasa girdisi” (`config:credentialsMissing`, vault key entries) is licensed, exactly as the runbook allows for a log entry. **Batch 5 is where the lexicon's own instruction about `logs` is spent, and it is not this locale's choice:** `../terminology.md`'s *entry* row states that the `logs` namespace narrates about the same object with the English word *string* and that the occurrence takes your **entry** rendering, so the UI counts one thing and not two. Every such line therefore ships “girdi” — `translation.start` / `done` / `failedGeneric`, `translation.failedNoRoute` and its two module siblings, `glossaryGen.done`, `orphan.detected` / `linked` / `deleted` — with **one deliberate exception**, `logs:translation.queued`, which is the very key the frozen row quotes. See the end of this cell. The disambiguated compounds arrive in the same batch and stay distinct by their qualifier: `console:empty` is “Henüz günlük girdisi yok.” (a *log* entry — the console's own rows) and `logs:tm.variantDeleted` is “Bir çeviri belleği girdisi kaldırıldı.” (a translation-memory entry). `console:membersNotShown` (“{{count}} girdi daha gösterilmiyor”) is the one bare “girdi” that means a log entry rather than content: it renders only inside an expanded console group, under the log rows it counts, and English writes the bare *entries* there too. **The one exception, and the corrected version of a claim this cell got wrong in batch 5.** The batch first wrote that *three* of these counters count **jobs** (entry × target language) rather than entries, and that a job count labelled “girdi” *under*-describes a multi-language run. **Both halves were wrong, and the review caught them.** The count is **one**, not three: the three `failed*` siblings aggregate on target language **and** reason together, so inside a group the language is fixed and `count` really is distinct entries for that one language — the English is correct there and so is “girdi”, and the non-aggregated path passes no count at all and defaults to 1. And the direction is the opposite: a job count labelled “girdi” **over**-states the entries — 30 for a ten-entry, three-language run. **Only `logs:translation.queued` has the mismatch** (its token is the routing-decision count, one per entry × language), and it is why that key ships “{{total}} çeviri sıraya alındı.” — the job head noun, the same one `batch:toTranslateCount` uses for the identical computation — instead of the *entry* rendering the frozen row's illustration asks for. **That deviation is deliberate and escalated, not an oversight:** the frozen row reserves this word for the object, the object being counted there is not an entry, and following the illustration ships a number that is three times wrong in front of the user. The controller has been asked to amend the illustration; do not re-broaden this correction to the `failed*` siblings, which is exactly the mistake being undone here. |
| source text | kaynak metin | Same “kaynak” as source language and source label; the head noun disambiguates all three. |
| translation | çeviri | Verb “çevirmek”. The tab label is the plural “Çeviriler” — see the surface-name table in `../style/tr.md`. **Batch 6 ships two more keys whose English is byte-identical to that tab label, and both take the same plural**: `stage-details:translationsHeading` (a section heading over the per-language fields) and `orphans:columns.translations` (a table column header). Neither is the Translations tab, so the verbatim *surface-name* rule is not what decides them — the word is simply the only correct Turkish for what each names, and English shows the identical three-way identity. The sidebar is always painted, so the tab label really does co-render with both; that is not a defect here because all three name the same kind of object, unlike the recorded Japanese failure where one word served two *different* concepts. **The one place this batch deliberately does not use the noun is `stage-details:translate`**, a button, which takes the bare stem “Çevir” — the pre-flight reports it against `sidebar:groups.translate` “Çeviri” as a same-English/different-rendering pair, and the control shape is what licenses it. |
| source label | kaynak etiketi | The routing column shortens it to the bare “Kaynaklar” (`config:routing.labelSources`), exactly as English shortens it to “Sources”. |
| achievement | başarım | The settled Turkish game-platform word. “başarı” (success/achievement in the everyday sense) is refused: it is already the health metric in `config:health.successRate`, and the two appear in the same product. |
| inline tag | satır içi etiket | Umbrella over placeholders, formatting tags and “\n” escapes. Shares “etiket” with source label; the qualifier separates them. **Batch 3 ships the bare head, on purpose:** `quality:checkLabels.tag-equality` is “Etiket eşitliği” and `checkLabels.tag-mismatch` is “Etiket uyuşmazlığı”, because *their* English is the bare *Tag equality* / *Tag mismatch* — matching `config:lqa.checks.tag-equality.name` (“Satır içi etiket eşitliği”, whose English adds the word **Inline**) by rendering rather than by English would put a word into a string whose own source lacks it. That pair is the runbook's own worked example of rubric item 8; it is now shipped in this locale. |
| placeholder | yer tutucu | The interpolation token inside game text only — the double-brace kind the mask-integrity check protects (`config:lqa.checks.mask-integrity.description`). The input-hint sense of the English word is a different concept and takes “ipucu metni” if it is ever needed — never this. |
| translator context | çevirmen bağlamı | Shipped in batch 2, which is where the concept itself first appears (`strings:compare.editContext`, `contextPlaceholder`, `compare.emptyContextFilter`, `compare.addContext`). The qualifier is load-bearing and **not droppable**: bare “bağlam” is the banned rendering, because the model's context window is “bağlam penceresi” and the CSV column is a third thing. That is why the “+ Context” chip ships as the fuller “+ Çevirmen bağlamı” rather than “+ Bağlam” — English clips it, this locale may not. |
| source language | kaynak dil |  |
| target language | hedef dil | Pairs with “kaynak dil”; the two sit side by side in Config. |
| reference language | referans dil | Pairs with “kaynak dil” / “hedef dil” on the same head noun, which is what makes the three read as one family (`strings:compare.translateUseReferenceNone`, `compare.reference`). The loanword is deliberate: a Turkish coinage (“başvuru dili”) would collide with the request/application senses of “başvuru”. Never a pivot reading — the app does not translate through it. |
| writable language | yazılabilir dil | **Met in batch 4**, and the lexicon's warning is the whole point of the row: the same permission is named in a table column (`collab:sharing.columnLanguages` “Yazılabilir diller”) and in three lock messages (`collab:locks.compareTargetsScoped`, `reviewLanguagesScoped`, `glossaryEditScoped`), and all four ship the identical adjective. Elimination: “düzenlenebilir” (editable) is the one a Turkish writer reaches for first, and it is refused **because its opposite is already shipped** — `glossary:readOnly` is “Salt okunur” and `collab:locks.readOnlyLanguage` is “salt okuma erişimi”, so *writable* has to be the direct antonym of *readable*, not of *locked*; “atanan” (assigned), “izin verilen” (permitted) and “kullanılabilir” (available) are the interchangeable set the lexicon bans. “yazılabilir” survives and is the ordinary Turkish for *writable*. |
| Pseudo Test | Pseudo Test | Kept as a proper noun, like the language code it names. A Turkish rendering (“sözde test”) would read as a value judgement about the test rather than a language name. |
| run | çalıştırma | Noun only — never a verb, per the shared lexicon. “koşu” is athletics; “işlem”, “görev” and “oturum” are the operation/task/session readings the lexicon bans. The verbs stay “çevir”, “başlat”, “üret”. **Scope of the “işlem” ban (runbook 2.9):** it bans *rendering this term* with that word, and nothing more — “işlem” is free elsewhere and batch 2 uses it correctly for *operation* (`strings:bulk.bulkOperation` “Toplu işlem”) and for a generic action inside prose (`strings:editor.deleteConfirm` “Bu işlem …”). The Activity table's **Action** column therefore takes “Eylem” (`strings:runs.actionColumn`), not “İşlem”, so that one word is not doing both jobs one column apart. Also note `strings:compare.run` — English's bare “Run” labelling the run-picker filter — is the noun and ships as “Çalıştırma”; it is this locale's largest single length ratio (3.33× over a three-character source), which is what a short English source does to a ratio, not a long rendering. |
| Activity | etkinlik | The tab that lists runs (`strings:tabs.runs`), its guide topic (`strings:guide.topicActivity`) and its page title, which English deliberately expands to “Translation Activity” → “Çeviri etkinliği” (`strings:runs.title`). Elimination: “geçmiş” is *history* and the lexicon bans it (it is also the word the empty state needs for “activity history”, which is where it belongs — inside the sentence, not as the surface name); “günlük” is taken by the live server-log panel; “işlem geçmişi” reads as a bank statement; “faaliyet” is bureaucratic register. “etkinlik” survives and means *the state of being active, things going on* — which is what the tab shows. Its secondary “an organized event” reading never surfaces here because nothing in this app schedules events. |
| batch | yığın | “toplu” is refused and **reserved**: it is Turkish for *bulk*, which the bulk-operations bar needs — and batch 2 spends that reservation (`strings:bulk.bulkOperation` “Toplu işlem”, `bulk.applyFailed` “Toplu değişiklik uygulanamadı”). “grup” is refused too — it is taken by rule group. English calls the Compare dialog “Batch translate” and its failure toast “batch translation”, so both keep this word: “Yığın çeviri” (`strings:compare.translateDialogTitle`) and “Yığın çevirisi başlatılamadı” (`strings:bulk.startFailed`). |
| batch grouping | yığın gruplaması | Distinct from batch mode (“yığın modu”) by the head noun, as the lexicon requires. |
| AI review | yapay zekâ incelemesi | Built on the same “inceleme” head as *source review*, which is what makes the two tabs read as one family: `strings:tabs.review-source-ai` is “Kaynak yapay zekâ incelemesi” and `review-translation-ai` is “Çeviri yapay zekâ incelemesi”. **Never rendered with “denetim”**, the *check* word — the lexicon separates a model's opinion from a deterministic rule, and this locale keeps that separation in the noun. The AI is always the full “yapay zekâ” with the circumflex, never “YZ”. |
| judge | değerlendirme | The evaluative sense. “yargı”, “hâkim”, “yargıç” are the courtroom readings and are refused outright. Appears in batch 1 only as a run type (`config:requestTimeoutDescription`, `config:batchGroupingDescription`). **Batch 3 is the first and only place the agent noun is licensed**, because it is the one string where English itself writes the noun: `review:translationAi.description` (“Have an AI judge score completed translations…”) ships as “Tamamlanmış çevirileri bir yapay zekâ değerlendiricisine doğruluk, akıcılık, terminoloji ve ton açısından puanlatın.” — “değerlendirici” on this same stem. The lexicon's rule is *do not introduce “the judge” as a noun where English does not use one*; here it does, so the noun is carried across rather than invented. Nowhere else. **In batch 2 the `strings:runs.judge*` keys are not this term:** every one of them says “AI review” in English, so they all take “yapay zekâ incelemesi” — the lexicon is explicit that the *feature* is called AI review and that “the judge” must not be introduced as a noun where English does not use one. Its two verdict labels are a deliberate pair: `judgeVerdictPass` / `judgeVerdictFail` ship as “Geçti” / “Geçmedi”, built on the same “geç-” root as “kalite geçidi”, rather than reusing “Başarılı/Başarısız” — those are already the *run status* and the LQA badge (`config:tm.lqaPassedBadge`, `strings:row.lqaPassed`, `strings:runs.statusFailed`), and one verdict must not read as the other. |
| source review | kaynak incelemesi | Built from “kaynak” + “inceleme”. “inceleme” is therefore reserved for **review**; the LQA check family takes “denetim” instead — see the *check* row. Shipped cold in batch 3 at `review:sourceAi.startSuccess` (“Kaynak incelemesi başlatıldı”), `startFailed` and `emptyTitle`; the tab title `review:sourceAi.configTitle` repeats batch 2's surface name verbatim, “Kaynak yapay zekâ incelemesi”. The separation holds inside one dialog: `review:sourceAi.checksLabel` is “Denetimler” and `noCheckHint` says “en az bir denetimi etkinleştirin”, three lines under a heading that says “inceleme”. |
| finding | bulgu | Reserved for it by the *issue* row and now shipped (`strings:runs.judgeAllFindingsTitle`, `judgeAllFindingsCount`, `judgeViewAll`). It is the ordinary Turkish for *a thing found on inspection*, which is exactly an AI opinion about one entry, and it stays visibly distinct from “sorun” (the machine verdict listed on the same entry). Batch 3 ships the source-review side of it on the same stem: `review:sourceAi.findingsTitle` is “Bulgular”, `allFindingsTitle` is “Tüm bulgular” (byte-identical in English to `strings:runs.judgeAllFindingsTitle`, so the verbatim rule binds), and `allFindingsCount` is “Bulgusu olan {{count}} girdi”. |
| suggestion | öneri | One word across the judge panel, the glossary generator and the category generator, as the lexicon requires. Elimination: “tavsiye” is *advice*, “ipucu” is *hint* and is already spoken for by the input-hint sense in the *placeholder* row, “teklif” is a commercial offer. The action on it is “uygula” (apply), never “onayla” — see the *approve* row and the English copy bug recorded there. Batch 3 adds the category generator's own suggestion surfaces (`category:noSuggestions`, `category:reviewTitle`) and the source review's single unified rewrite, which English calls “Suggested source” — `review:sourceAi.suggestion` is “Önerilen kaynak”, the participle on the same stem, because there the noun is *kaynak* and *önerilen* qualifies it. **Batch 6 meets the frozen row's *the action is apply, not accept* rule in the one place English breaks it outside the recorded copy bug:** `backup:triggerPreAccept` is English *Automatic — before accepting AI suggestions*, and it ships as “Otomatik — yapay zekâ önerileri uygulanmadan önce” — the *apply* verb, not an *accept* one. The frozen row bars *accept* as a rendering of the action on a suggestion, and the frozen *snapshot* row describes this very trigger with the same English word, so nothing here is a second action. The assistant panels in the same batch keep the pair intact on their own buttons: `colorText:assistant.apply` is “Uygula” and `assistant.discard` is “Reddet”. |
| discard | vazgeç | **Batch 6 ships the last sense-2 site, and it is the one the frozen lexicon names by key**: `colorText:assistant.discard` (*Discard*) is “Reddet”, refusing the styled text the assistant just produced. The frozen *discard* row lists that exact key in its sense-2 group alongside `strings:runs.judgeDiscard` and `glossary:generateDiscard`, so the split was settled before this batch read it — which matters, because the byte-identical English also matches three sense-1 keys that ship “Vazgeç”, and a translator matching on English alone would have taken the wrong one. There is no Save button beside it; there is an Apply button, which is the sense-2 tell. The pre-flight reports *Discard* as a same-English/different-rendering group with both renderings in it, and that group is the term row working. **Batch 5 adds a third sense-2 site and it is the terse one:** `logs:judge.suggestionDiscarded` (*Suggestion discarded.*) ships “Öneri reddedildi.” — the produced-result sense, on the same stem as `strings:runs.judgeDiscard`, narrated in the passive because the log reports what already happened. Sense 1, the ghost button beside Save (`config:discard`, `strings:editor.discard`). Sense 2, refusing a produced result, ships as **“reddet”** (`strings:runs.judgeDiscard`, and its toasts `judgeDiscardSuccess` / `judgeDiscardFailed`) — the reservation made in batch 1 is now a shipped pair, and the pre-flight reports the two as a licensed same-English/different-rendering collision. “iptal” is unavailable: it is Cancel (`config:cancelImport`). `config:discardUntranslatable` is a third, non-term use — “leave out of the export” — and is rendered by meaning (“dışarıda bırak”), not with this word. Batch 3 adds one more sense-2 site, and it is the clearest one in the app: `glossary:generateDiscard` (“Discard result”) is “Sonucu reddet” — a produced result being refused, beside a “Yeniden dene” button. **Batch 4 adds a sense-1 site and the code is what settles it**: `vault:discard` (“Discard”) sits on a credential row that has never been saved and only drops the row from the form (`VaultEditorDialog.deleteRow`), while its sibling `vault:delete` sits on an existing row and marks a real key for removal, which the save handler then applies by sending that key as null. So the pair ships as “Vazgeç” / “Sil” — byte-identical to `config:discard` for the same English, and the destructive one is the one that says *sil*. |
| needs review | inceleme gerekli | One wording across all three surfaces the lexicon names: the filter label (`strings:filters.needsReview`, `strings:compare.needsReviewFilter`), the deliberately lowercase cell chip (`strings:compare.cellNeedsReviewBadge`) and the bulk action (`strings:compare.flagAllNeedsReview` — “Tümünü inceleme gerekli olarak işaretle”). Casing is the only difference between them; do not let it become two renderings. Turkish carries no gender here, so the es/fr agreement problem the shared lexicon describes does not arise — but the **quoted-token** rule does: `strings:contextMenu.clearNeedsReview` quotes the chip verbatim, “inceleme gerekli”, and `clearReviewed` quotes the status token “incelendi”, matching `strings:compare.cellReviewedBadge`. Elimination: “gözden geçirilmeli” would be a second word for *review* beside “inceleme” and is refused for that reason alone. Batch 3 adds the fourth surface, the review queue's own filter: `review:filterNeedsReview` is “İnceleme gerekli”, the same wording again. |
| flag | ayır | **Met in batch 3.** The disposition, not an alarm: `review:flag` is “Ayır”, and English's own empty state glosses it as *set aside* (“Translations you set aside with Flag”), which is exactly the Turkish idiom “bir kenara ayırmak”. Elimination: “işaretle” is barred by the lexicon *and* already spent by batch 2 on setting *needs review* and on the new-from-import flag — and this action **clears** the needs-review flag, so one verb cannot set and unset the same state; “beklemeye al” / “beklemede” was rejected because “Beklemede” is already the run status *Pending* (`strings:runs.statusPending`); “raporla”/“bildir” is the report reading (`config:lqa.checks.*` already uses “bildirir”). “ayır” survives, is free, and means *set apart*. **Two shape notes the next translator needs.** (a) The bare past tense “ayrıldı” also reads as *departed*, so the toast names the destination rather than leaving the verb bare — `review:flaggedToast` is “Çeviri bir kenara ayrıldı”, not “Çeviri ayrıldı”. The filter chip is the nominalised plural, `review:filterFlagged` “Ayrılanlar”, and the empty state is `review:emptyFlaggedTitle` “Ayrılmış çeviri yok”. (b) **The empty-state hint glosses the term with a second verb on purpose, and it must not be “harmonized” away:** `review:emptyFlaggedHint` is “Ayır ile bir kenara aldığınız çeviriler burada görünür.” — *aldığınız*, not *ayırdığınız*, because “Ayır ile bir kenara ayırdığınız” repeats the stem twice in five words. English does the same thing, glossing its own button *Flag* as *set aside*. The whole-language sweep's same-rendering pass will see two verbs for one term here; this is why. **Scope (runbook 2.9): this reserves the verb for *this disposition only*, and three shipped strings prove the scope is doing real work rather than being a formality.** The root stays free where English is not this term — batch 1's “ayırt edebilmeniz” (*tell apart*, `config:instances.slugHelp`) and batch 3's “bu sekmeden ayrılabilir” (*leave this tab*, `category:genBackgroundHint`, the *departed* reading of this very stem) are both correct and neither is a collision. **And the mirror image, inside `review.json` itself:** `review:sourceAi.runSummary` (“…{{flagged}} tanesi işaretlendi…”) and `review:sourceAi.approvedToast` (“Onaylandı olarak işaretlendi”) use the *marking* verb three keys away from `review:flag` “Ayır”. That is not a contradiction of the reservation — their English is the generic *flagged*/*marked*, not the queue disposition, and they render on the Source AI review tab while “Ayır” renders in the manual review queue. Do not reopen it. `glossary:flaggedTitle` is the same case one namespace over: English “{{count}} flagged (incomplete)” is the generic marking, and it ships as “{{count}} işaretli (eksik)”. |
| Review (the sidebar group) | inceleme | Shipped as the guide's group heading (`strings:guide.groupReview`); **`sidebar:groups.review` shipped it verbatim in batch 4**, along with the other four inherited headings (`groups.project` “Kurulum”, `groups.translate` “Çeviri”, `groups.content` “Terminoloji”, `groups.maintenance` “Bakım”), copied from `strings:guide.group*` and not re-decided. The nesting is the one the runbook warns about and it is clean here: the heading “Çeviri” sits over the child “Çeviriler” (`strings:tabs.strings`), the general over the specialised, and “İnceleme” sits over three children that all carry “inceleme” as their head plus “Kalite”, which sits outside it in both languages. The lexicon's warning is against a word derived from **one member** — and this is the opposite: “inceleme” is the shared head of three of the four members (“kaynak incelemesi”, “çeviri yapay zekâ incelemesi”, “elle inceleme”), exactly the relationship English's own vague “Review” has to them, with Kalite sitting outside it in both languages. It is not the specific word for any one of them: the AI passes are always the full “yapay zekâ incelemesi”, human review is “elle inceleme”, and the deterministic rules are “denetim”, a different word entirely. |
| review queue | inceleme kuyruğu | **Met in batch 3**, built on the “inceleme” root exactly as the lexicon asks, so the queue and the *needs review* state read as one feature: `review:title` is “İnceleme kuyruğu”, `emptyTitle` is “İnceleme kuyruğu boş” and `filterLabel` is “İnceleme kuyruğunu filtrele”. “kuyruk” is the settled Turkish computing word for a queue and is free — nothing else in this app is called one; “sıra” is refused because batch 2 already spends it on a run's position in the queue (`strings:runs.statusQueued` “Sıraya alındı”, `queuedCount`), and the two would then differ only by context. The page title is deliberately **not** the tab label: `strings:tabs.review-manual` is “Elle inceleme”, exactly as English pairs *Manual review* with *Review queue*. |
| module | modül | The loanword, settled. “birim” (unit) is refused — it says nothing about what the thing does, and the app has no other “unit”. |
| module instance | modül örneği | “örnek” is the settled Turkish computing word for *instance*; its other reading (*example*) never collides here because every occurrence sits beside a module. “kopya”, “profil”, “yapılandırma” are all on the lexicon's ban list. **The izafet suffix rides on “örnek”, never on the module name** — see the placeholder section of `../style/tr.md`. |
| provider | sağlayıcı | The outside company behind a module. Kept strictly apart from “modül”, including in the three English strings that mislabel a module picker *Provider* (`config:routing.simplePlaceholder`, which ships “Bir sağlayıcı seçin”), which are translated as written. **Batch 6 ships the other two of those three and follows the same instruction:** `colorText:assistant.instanceLabel` and `stage-details:chatInstanceLabel` are both “Sağlayıcı”, and the placeholder under the first is `colorText:assistant.pickInstance` “Bir sağlayıcı seçin…”, matching the routing one. The frozen row is explicit that these are translated *as written* and must not drag the rendering of *module* toward this word anywhere else — and they do not: the two namespaces also ship `stage-details:module` and `orphans:relink.aiModuleLabel` (“Yapay zekâ modülü”) on the module word, three lines from a *Provider* label in one case. Where the surrounding prose in those panels says *provider* it stays this word too (`colorText:assistant.noCredentials` “Asistan sağlayıcısı API kimlik bilgileri gerektiriyor.”), because English's own sentence does. |
| model | model | Loanword. This is why *template* may not be “model”: see that row. |
| prompt | istem | The settled Turkish AI term. **Watch the near-collision with “istek” (request)** — one letter apart, and both live in the same settings panel (`config:requestsPerSecondLabel`, `config:requestTimeoutLabel`). “komut”, “sorgu”, “yönerge” are the command/query/instruction readings the lexicon bans. |
| reasoning effort | akıl yürütme çabası | The provider's own parameter. Not “çaba” alone, which reads as work done. |
| routing rule | yönlendirme kuralı | Content routing, not network routing — “rota” and “yol” are refused for that reason. |
| rule group | kural grubu | “grup” is reserved here, which is one reason *batch* is “yığın”. Not “küme”, not “profil”, not “önayar”. |
| credential vault | kimlik bilgisi kasası | **Promoted from the clip “kasa” in batch 2, which is the batch that first names the vault cold** — `strings:guide.topicVault` (“Credential Vault”) ships the full form, so it is now attested and the Rendering column can hold the term itself. Bare “kasa” remains licensed as the **clip**, and only where the surrounding string already establishes credentials: every `config` site does (`credentialsVaultLockedChip`, `credentialsUnlockButton`, `credentialsVaultLocked`, `credentialsMissing`). A cold, standalone “kasa” is the safe/strongbox reading the frozen lexicon's Not-list bans — which still binds `vault:statusLabel` in batch 4, the second cold naming, and it takes the full form too. All four required phrasings read naturally on this stem: kasa kilitli / kasanın kilidini aç / kasa parolası / kimlik bilgisi kasası. **The mechanism that held this row at the clip for a round is worth keeping, because it applies to every row in this file:** the citation guard (`scripts/check-lexicon-citations.mjs`) requires this column's significant words to be attested in the shipped `tr` corpus, so a term's full form **cannot be recorded ahead of the batch that ships it**, and every unshipped row is biased toward the clipped wording by construction. Read an unpromoted row as “the longest form shipped so far”, not as “the term”, and promote it the moment your batch ships the fuller one. Bending a string to make a citation attestable is the wrong fix and was refused here. **Batch 3 adds one more clip site and it is licensed, not a third cold naming:** `glossary:toastGenerateVaultLocked` opens with English's own “Vault locked —”, byte-identical to `config:credentialsVaultLockedChip`, so it ships the same two words, “Kasa kilitli — …”. The reading is fixed by the situation (an AI run just stopped for want of credentials) and by the verbatim rule, not by the sentence naming credentials. A *cold* naming is a standalone label of the feature itself, which is still only `vault:statusLabel` in batch 4. **Batch 4 has now shipped it, and the transferred half of the debt is discharged:** `vault:statusLabel` is “Kimlik bilgisi kasası” — the full form, cold, exactly as this row required a batch in advance. Three more keys in the same namespace take the full form for the same reason, because English writes it out in all three: `vault:unlockTitle` (“Kimlik bilgisi kasasının kilidini açma”), `createTitle` and `editorTitle`; `account:devicesDescription` and `devicesEmpty` do too. **Batch 5 adds four more clip sites and they do not all rest on the same licence, which is why this row now says which.** Three carry both licences at once — the string establishes the vault *and* English clips it too: `console:vaultLocked` “Kasa kilitli” (byte-identical to `config:credentialsVaultLockedChip`), `logs:vault.unlocked` “Kasanın kilidi açıldı.” and `logs:vault.passwordChanged` “Kasa parolası değiştirildi.”, all mirroring an English that says *Vault*, not *credential vault*. `errors:http.vaultLocked` (“Kasanız kilitli. Kilidini açıp yeniden deneyin.”) carries only the second: its English is *Your vault is locked* on a generic HTTP-error toast that can fire anywhere in the product, so nothing in the surrounding sentence establishes credentials — it is the frozen row's own *shortened to “vault” where the context is unambiguous* that licenses it, and the possessive is what makes it unambiguous. A **cold** naming of the feature still takes the full form. **Batch 6 is the last batch to touch it, and it splits the same way: two full forms and two clips, each on its own licence.** The full form is cold at `welcome:setupVaultBody` (“…kimlik bilgisi kasanızı kurun.”) and in the two assistant messages whose English also writes it out — `colorText:assistant.vaultLocked` and `stage-details:chatVaultLocked`, both “…kimlik bilgisi kasası kilitli.” The clips are `welcome:setupVaultButton` “Kasayı kur”, which sits directly under the sentence that just named the term *and* mirrors an English that clips it too, and the pair `colorText:assistant.unlockVault` / `stage-details:chatUnlockVault`, which are byte-identical in English to `logs:action.unlockVault` and therefore fixed by the verbatim rule at “Kasanın kilidini aç” before any licence question arises — this was written into `../style/tr.md` as a batch-5 prescription and is a citation there now. **The clip “kasa” survives in batch 4 wherever the string itself establishes the vault** — `nameLabel` “Kasa adı”, `goToVault` “Kasa sayfasına git”, `passwordHelp` “kasa parolanızı”, `keysCount` “Kasada {{count}} anahtar” — and every one of those mirrors an English that clips it to “vault” too, so the clip is English's decision here rather than this locale's. |
| credential | kimlik bilgisi | Usually plural in the UI (“Kimlik bilgileri eksik”). Not “parola”, not “hesap”. Vault key names themselves are never translated. **Batch 4 ships the whole vault namespace on it** (`vault:addKey` “Kimlik bilgisi ekle”, `noKeys`, `editorDescription`, `manageOnVaultPage`) and keeps it strictly apart from “parola”, which is the *password* the same dialog asks for — the two appear in one form, three lines apart, and English separates them the same way. `vault:keyPlaceholder` is the one exception in kind: English's “KEY_NAME” is a shape shown in an empty field, not a value to type, so it ships translated as “ANAHTAR_ADI” rather than as an untranslatable literal. |
| LQA | LQA | Kept as the industry acronym; Turkish localization practice uses it untranslated. Used attributively with no suffix on the acronym: “LQA denetimleri”, “LQA geçidi”. |
| quality gate | kalite geçidi | The process-control sense. “kapı” is refused — it is a literal door, and its uppercase form “KAPI” also collides with the do-not-translate term API in the guard's own term check. “eşik” (threshold) is refused because a threshold is a number, and this file already has severity and limit vocabulary. |
| check | denetim | One named LQA rule. **Batch 5 adds the compound English calls a *quality check*, and it was inherited rather than decided:** `config:lqa.description` already ships “Kalite denetimleri her çeviride çalışır.”, so the four `logs` lines that name it repeat that compound — `logs:lqa.passed` “Kalite denetimi başarılı oldu.”, `lqa.failed`, `lqa.checkError` (“Bir kalite denetimi çalıştırılamadı.”) and `translation.lqaRetry`. **The verb splits with English's own verb, deliberately:** where English says *failed* the line takes “başarısız”, matching the LQA row badge family (`strings:row.lqaPassed` “LQA başarılı”); where English says *didn't pass* it takes the “geç-” root, so `logs:translation.tmRejected` ends “kalite denetimini geçemedi”. Two English verbs, two Turkish verbs, one term — not drift. The quality dashboard's own verdicts (“Geçen” / “Geçmeyen”) are a third family and are fixed by the *pass rate* row; nothing in this batch touches them. Elimination: “kontrol” is the generic Turkish for *control* and reads as a UI control; “sınama” is *test*; “doğrulama” is *validation* and is taken by **assertion**; “inceleme” is taken by **review**. “denetim” survives, and it means an applied rule that passes or fails — which is what a check is. Batch 3 ships the full check-label set at `quality:checkLabels.*`. Two things to know about it. **(1) Read each label against its own English, not against `config:lqa.checks.*`.** Thirteen labels have byte-identical English in both namespaces and are byte-identical here too, but `quality:checkLabels.tag-equality` is the bare English *Tag equality* where `config:lqa.checks.tag-equality.name` is *Inline tag equality* (the extra word is **Inline**), so this one ships as “Etiket eşitliği” against config's “Satır içi etiket eşitliği” — same term, each faithful to its own key. **(2) The eleven extra `checkLabels` are issue codes, not checks**: they name the verdict a check filed (`number-missing` “Sayı eksik”, `whitespace-leading` “Baştaki boşluk”, `mask-mismatch` “Maske uyuşmazlığı”), and they take the plain descriptive noun phrase, never “denetim”. `too-long` reuses batch 2's filter wording exactly — “Çok uzun”, as at `strings:filters.tooLong`. |
| issue | sorun | One LQA verdict, at blocking or warning severity. **Batch 5 ships the term at `logs:sourceReview.done`** (“Kaynak incelemesi {{findings}} sorun buldu.”) — English says *issues* there, not *findings*, so the line follows its own English exactly as `review:sourceAi.noFindings` did in batch 3. **And it spends the word once outside the term, which the reservation permits:** `errors:http.server` (English *The server ran into a problem.*) ships “Sunucuda bir sorun oluştu.” — the ordinary Turkish noun for *a problem*, on a network-error toast that can never co-render with an LQA verdict. Scope, per runbook 2.9: the row bans **“hata” as a rendering of this term**; it does not reserve “sorun” against the everyday sense, and “hata” stays free for English's own *error* (`console:filter_error` “Hata”). “hata” (error) is refused by the lexicon; “bulgu” is deliberately left free for **finding**, which is listed on the same entry. Batch 3 ships both on one screen and they stay apart: `quality:mostCommonIssues.title` is “En sık görülen sorunlar” and `review:lqaTitle` is “LQA sorunları”, while the source review's own list is `review:sourceAi.findingsTitle` “Bulgular”. `review:sourceAi.noFindings` (English “No issues found in this run.”) follows its **own** English and says “sorun”, not “bulgu” — the key name is not the term. |
| severity | önem derecesi | The two values are “Engelleyici” and “Uyarı” and are fixed by the *check* row, not re-decided here. “seviye”/“düzey” (level) and “öncelik” (priority) are refused. |
| notification severity | önem derecesi | **Met in batch 4** as a value set only — the term itself has no label in the UI, so what ships is the three values at `account:notificationsSeverity.*`: “Bilgi” / “Uyarı” / “Kritik”. **“Uyarı” is not re-decided here**: the lexicon fixes it from the *check* row's severity values, and `config:lqa.severityWarning` already ships “Uyarı”, so the two render identically by rule rather than by coincidence. “Bilgi” is the ordinary noun; “Kritik” is the loanword and is free (nothing else in this app is called critical). The head noun is shared with the LQA *severity* row deliberately — the two sets are separated by what they grade, exactly as English separates them, and neither set's values leak into the other (“Engelleyici” never appears on a notification). |
| assertion | doğrulama | A user-written regex the translation must or must not match. Elimination: “koşul” is routing conditions, “kural” is routing rules, “denetim” is the LQA check, “iddia”/“sav” are the claim reading, “ifade” is *statement*. “doğrulama” survives, is free (nothing else in this app is called *validation*), and means what an assertion does. Batch 3 repeats it in the quality dashboard's own labels: `quality:checkLabels.regex-assertions` is “Regex doğrulamaları” (matching `config:lqa.checks.regex-assertions.name`) and the issue code `regex-assertion-failed` is “Regex doğrulaması başarısız” — the singular, because one assertion failed. |
| pattern | desen | The regex sense only. Never “kalıp” or “şablon” — the latter is template. |
| overflow | taşma | Relative to the source, via the overflow ratio. Kept clearly apart from “uzunluk sınırı”, which is absolute. Batch 3 puts the two side by side in one list and they stay apart: `quality:checkLabels.overflow` is “Uzunluk taşması” and `checkLabels.length-limit` is “Uzunluk sınırı”. The per-entry verdict `review:overflowIssue` ships as “Taşma: çeviri, kaynak uzunluğunun {{ratio}} katı.” — device 1, with “kat” carrying the suffix so nothing is welded to `{{ratio}}`; English's “×” is dropped because Turkish expresses the multiple with the noun, not the sign. |
| length limit | uzunluk sınırı | Hard per-language cap set by the game editor. Also the routing condition on source length (`config:routing.labelMaxLength`, “Girdi uzunluk sınırı”). **Batch 6 meets a neighbouring English phrase that is NOT this term, and keeping them apart is rubric item 8 rather than a style preference.** `stage-details:maxLengthLabel` is English's *Max length*, not *length limit*, so it ships as “En fazla uzunluk” — the same *Max* wording batch 1 already uses at `config:maxBackupsLabel` and `config:routing.templateMeta` — while the button that adds one, `stage-details:addLimit` (*Add limit*), takes the term's head noun: “Sınır ekle”. Rendering the label with the full term would have put a word into a string whose own English lacks it, which is the standing example this locale already carries for the *inline tag* row. |
| pass rate | geçme oranı | **Met in batch 3**, and the reservation batch 1 wrote held: `config:health.successRate` is a different metric and still ships as “Başarı”, so this one takes its own word. Shipped at `quality:columns.passRate` (“Geçme oranı”), `quality:overallStat.passRateLabel` (“Genel geçme oranı”) and the three legend tiers. Built on the same “geç-” root as *quality gate* (“kalite geçidi”) and as batch 2's verdict pair `strings:runs.judgeVerdictPass` / `judgeVerdictFail` (“Geçti” / “Geçmedi”), which is why the dashboard's own bare `quality:legend.passed` / `failed` are **“Geçen” / “Geçmeyen”** and not “Başarılı” / “Başarısız”: those two are already the run status (`strings:runs.statusFailed`) and the LQA row badge (`strings:row.lqaPassed`), whose English is the qualified “LQA passed”, not the bare word. The pre-flight reports “Failed” → Geçmeyen / Başarısız as a licensed same-English/different-rendering collision for exactly this reason. |
| glossary | sözlükçe | Elimination: “sözlük” alone is *dictionary*, which the lexicon bans; “terim tabanı” is a termbase, a different CAT artefact; “kelime listesi” is the word-list reading. “sözlükçe” is the attested Turkish for *glossary*, is one word (it has to fit a tab label), and leaves “terim” free for the row below. Batch 3 translates the whole tab on this stem and it carries every suffix the namespace needs without strain: `glossary:glossaries` “Sözlükçeler”, `newGlossary` “Yeni sözlükçe”, `matchAssignmentWithout` / `matchAssignmentWith` “Sözlükçesiz” / “Sözlükçeli”, `unassignGlossary` “Sözlükçe atamasını kaldır”. It never touches “çeviri belleği” (translation memory), which is the collision the lexicon warns about. |
| glossary term | terim | Shortened to the bare “terim” inside the Glossary tab, as English does. Never “girdi” — that is a content entry, and the app counts those constantly. Batch 3 ships the tab itself and holds the split throughout: `glossary:totalTerms` is “Toplam terim:” and `glossary:generateSuggestionCount` is “{{count}} terim”, while every counted content unit in the same file stays “girdi” (`glossary:toastGenerated`, `generateScopedHint_one`). The two appear in adjacent strings, which is exactly the collision the lexicon's Not-list is about. |
| constant | sabit | **Met in batch 3.** The masking flag on a glossary term: a column header (`glossary:colConstant` “Sabit”), a bulk pair (`bulkMarkConstant` / `bulkClearConstant`) and a deliberately lowercase badge in both review panels (`review:glossaryConstant`, `review:sourceAi.glossaryConstant`, both “sabit” — English lowercases them and so does this locale, per the cell-chip rule). Elimination: “değişmez”, “kalıcı” and “salt okunur” are all on the lexicon's own Not-list, and the last is also **taken** — `glossary:readOnly` is “Salt okunur”, a different flag two columns away in the same table, which is precisely the near-miss the lexicon warns about. “sabit” survives, is the ordinary Turkish computing word for a constant, and is free. |
| match | eşleşme | Noun. The verb is “eşleşmek” on the same root, which keeps the search and routing strings (`config:models.noMatches`, `config:lqa.regexModeMustMatch`) visibly the same word as the glossary noun, exactly as the lexicon asks. Batch 3 ships the Matches panel it was defined for: `glossary:matchesPanel` is “Eşleşmeler”, `matchResultsCount` is “{{count}} eşleşme”, `matchNoResults` is “Eşleşme bulunamadı.” and the confirm pair `confirmEnableApplyConfirm` is “Eşleşmeleri uygula”. The noun and the verb stay visibly one word, and neither is ever “terim”. |
| translation memory | çeviri belleği | The established Turkish CAT term. Bare “bellek” is never used alone — it reads as RAM. |
| approve | onayla | Storing a translation into translation memory. This forces one consequence worth knowing: **confirm buttons may not also be “Onayla”**, so they take the explicit two-step Turkish form instead (“Evet, sil” at `config:instances.deleteConfirm` and `strings:editor.deleteConfirmYes`, “Eminim” at `config:routing.removeConfirm`). “kabul et” (accept), “doğrula” (validate) and “kaydet” (save) are all refused or taken. Batch 2 ships the bulk control as **“Çeviri belleğine onayla”** (`strings:bulk.approveSelected`): the destination has to be named, because bare “belleğe” reads as RAM. Its toast reorders rather than repeating the word — “Çeviri belleğine {{count}} çeviri onaylandı”, not “{{count}} çeviri çeviri belleğine…”. And `strings:runs.judgeApproveAll`, whose English says “Approve all suggestions” for the apply action, ships as “Tüm önerileri **uygula**” — the known English copy bug, not a second use of this word. **Batch 3 ships the term proper and one licensed second use.** The review queue's approve really is this term — `ReviewTab.tsx` posts to `/projects/:id/approve`, the same route as the bulk bar — so `review:approve` is “Onayla” and `review:approveAll` is “Tümünü onayla”. Their English is the **bare** word, not batch 2's “Approve to memory”, so the destination is *not* named here: naming it would import words this key's own English does not have (runbook 2.6). The second use is `review:sourceAi.approve` (“Approve”, `approvedBadge` “Onaylandı”), which records a disposition on a source finding rather than writing to memory — English writes the same word for both, and mirroring that is faithful; splitting it would invent a distinction the app does not have. The two are on different tabs and never co-render. *Confirm* is still barred from “Onayla” and still takes the explicit two-step form — **and batch 6 adds the fourth member of that family, `backup:confirmConfirm` (*Yes, restore*) “Evet, geri yükle”, plus the one place the bar is escaped by a noun rather than by the two-step form** (`orphans:relink.confirmTitle`, a title; see the *relink* row). |
| category | kategori | The loanword, and it never collides: *rule group* is “kural grubu”, *batch* is “yığın”, *source label* is “kaynak etiketi”. Batch 3 owns the tab: the page title is the plural `category:title` “Kategoriler”, against batch 2's deliberately singular tab label `strings:tabs.category` “Kategori” — English's own split, kept. The confirm body is the standing device-1 example for this namespace, `category:deleteConfirmBody_one` — “Bu işlem, “{{category}}” kategorisini {{count}} girdiden kaldırır …” — where the head noun “kategori” carries the accusative so nothing is welded to the token, and English's curly quotes are kept because this locale uses the same pair. |
| tone | ton | An authoring instruction on an entry. “üslup” is refused **as a rendering of this term**: it is the writing-style reading the lexicon warns about, and it would read as the model's style rather than an instruction to it. Scope (runbook 2.9): the ban is on this term only, so “üslup” stays free — and correct — where English itself says *style*, which batch 2 uses at `strings:compare.translateExamplesLabel` (“Örnek girdiler (üslup referansı)”) and `translateExamplesHint`. The two words next to each other are the disambiguation, not a collision. |
| orphan | yetim | The figurative noun, used identically in the tab title, the count chip and the import notices. “artık”, “sahipsiz” and “kayıp” are the leftover/ownerless/lost readings the lexicon bans. Adjectival “orphaned” is “yetim olarak işaretlenmiş” or simply “yetim”. **Batch 6 ships the namespace and the frozen row's *one noun everywhere* instruction is what it is tested by**: `orphans:title` repeats batch 2's tab label “Yetimler”, the confirm dialog keeps the bare noun (`orphans:confirmDelete.title` “Yetim silinsin mi?”, matching `config:orphanedCount` “{{count}} yetim”), and every toast stays on it (`orphans:toast.deleted` “Yetim silindi”, `toast.bulkDeleted` “{{count}} yetim silindi”). **The finding worth carrying forward is about English's *other* noun, not this one.** `orphans:empty` and `orphans:confirmDelete.body` call the object an *orphaned record*, where the rest of the app says *entry* and `logs` says *string*. All three are one object, so both ship the **entry** rendering — “Yetim girdi yok — …” and “…yetim girdiyi ve çevirilerini kalıcı olarak siler.” — and **not** “kayıt”, which is the first Turkish word a translator reaches for here and which the *recording* row owns. Had *entry* taken “kayıt”, this namespace would have had no word left. **One key in the namespace fires from two paths and the reason first recorded for it was wrong — the correction is left visible because the string did not change.** `orphans:toast.deleteError` (*Failed to delete orphan*) is raised by the single delete **and** by the bulk delete, and `english-review-notes.md` now files it as singular-only English and asks for a count-neutral frame. The shipped “Yetim silinemedi” is defensible, but **not** for the reason first given — *a counted noun does not inflect* is the post-numeral rule, and there is no numeral here, so it cannot license anything. The real ground is narrower: a **bare** Turkish noun is unmarked for number and reads generically, so the sentence is true of one orphan or twenty. That the distinction is genuinely available two keys away is what makes the reason matter — `orphans:toast.loadError` ships the explicit plural “Yetimler yüklenemedi” and `toast.candidatesError` uses a deverbal-noun frame — so a later sweep preferring the filed prescription has the namespace's own shape to reach for rather than a plural that would then be wrong on the single path. |
| relink | yeniden bağla | One verb for the row button, the dialog title, the confirm step and the import warning (`config:importModeFullReplaceHint`). Not “yeniden eşle”, not “taşı”. **Batch 6 spends it across all five shapes the frozen row demands and they all sit on this stem**: the row button and the confirm button (`orphans:actions.relink` and `relink.confirmSubmit`, both “Yeniden bağla”), the dialog title as a deverbal noun (`orphans:relink.title` “Yetimi yeniden bağlama”), the confirm-step title as a noun phrase (`relink.confirmTitle` “Yeniden bağlama onayı”) and the toasts (`toast.relinkError` “Yetim yeniden bağlanamadı”). **The confirm-step title is where the *approve* reservation bites and the escape is the noun:** English writes *Confirm relink*, and this locale bars “Onayla” from a confirm control because *approve* owns that verb — but the **noun** “onay” is free and already shipped (`account:tokenPlaceholder` “Onay kodu”), so the title takes it rather than reaching for the two-step “Evet, …” form, which is a button shape and not a title shape. |
| backup | yedek | Countable noun (“proje başına en fazla yedek”). The **surface** is “Yedekleme” — see the surface-name table in `../style/tr.md`; do not use one where the other belongs. The verb is “yedek oluştur”. **Batch 5 ships the countable noun twice in `logs`** — `logs:backup.restored` is “Yedek geri yüklendi.” and `logs:backup.pruneFailed` is “Eski yedekler temizlenemedi.” — reusing batch 2's restore verb from `strings:compare.undoRestore` (“Geri yükle”). **This created a prescription for batch 6, and there was no freedom in it:** `backup:toastRestoreSuccess` is byte-identical in English (*Backup restored.*), so the verbatim rule fixed it before the guard ever looked at it. **Batch 6 discharged it — the key ships “Yedek geri yüklendi.” and this sentence is a citation now, checked on every run.** **Batch 6 is also the namespace this row was written for, and the object/process split it names is what shapes the whole file.** The countable object stays “yedek” (`backup:createButton` “Yedek oluştur”, `backup:toastDeleteSuccess` “Yedek silindi.”, `backup:backupsListSection` “Kayıtlı yedekler”) while the page title takes the process noun and expands on the tab label exactly as *Activity*'s does: `strings:tabs.backup` is “Yedekleme” and `backup:title` is “Yedekleme ve geri yükleme”. **The runbook names this namespace's section headings as the place Russian got the shape wrong, so they were resolved as controls rather than from this row:** `backup:createSection` is the noun phrase “Yedek oluşturma” and its button one line below is the bare stem “Yedek oluştur”, which the pre-flight duly reports as a same-English/different-rendering pair — that pair is the rule working, not drift, and the two are siblings on screen precisely so the shapes must differ. `backup:restoreSection` (“Yedekten geri yükleme”) takes the same shape. The restore verb is still batch 2's, from `strings:compare.undoRestore`. |
| snapshot | anlık görüntü | An automatic pre-operation restore point. Distinct from “yedek”, which matters because `config:importSnapshotNote` uses both in one sentence. Not “ekran görüntüsü”, which is a screenshot. **Batch 6 met English's *other* name for this object and did not coin a second Turkish one.** `backup:itemPreImport` and `itemPreRetranslate` label the automatic backups *Restore point from just before…*, and the frozen row states in its own definition that English also calls these restore points — so both ship on this term: “{{date}} tarihli içe aktarmadan hemen önce alınan anlık görüntü”. The tempting rendering is the very familiar “geri yükleme noktası”, which every Turkish Windows user knows; it is refused because it would put two Turkish names on one object in a namespace that already distinguishes “yedek” from this term three lines away, and because `config:importSnapshotNote` has already named the object once for the reader. The head noun also carries the placeholder here — “{{date}} tarihli …” — so nothing is welded to the token. |
| template | şablon | Never “model” — that is the AI model, and this is exactly the collision the shared lexicon warns Romance and Slavic languages about. Turkish has the same trap through “kalıp”, which is also refused because it is the natural word for a regex *pattern*. |
| collaborator | ortak çalışan | Shipped at `strings:runs.projectTotalCollaborators` and `strings:runs.manualRecordingPaused`. **“işbirlikçi” is refused outright and this is the one to know about:** it is the dictionary translation of *collaborator*, and in Turkish its dominant reading is the wartime one — a person who collaborates with an enemy. Shipping it would call every invited teammate a traitor. “katılımcı” (participant), “katkıda bulunan” (contributor) and “kullanıcı” (user) are on the lexicon's own ban list. “ortak çalışan” survives, is two words so it can never collapse into *member*, and means precisely *someone working on it jointly*. **Batch 4 is the namespace the word was reserved for and it holds throughout** — `collab:join.description` (“…ortak çalışan olarak katılmak…”), `invites.nicknameRequiredBody`, `errorNicknameRequired`, `sharing.auditToggleHelp`, `errors.project_full` (“ortak çalışan sınırına”) — and never once alternates with “üye”, which is now shipped as *member* in the same file. |
| member | üye | **Met in batch 4**, and the prediction the previous batch left here held: “üye” was free, and it is the row type of the Members table (`collab:sharing.membersTitle` “Üyeler”, `columnMember` “Üye”, `activity.formerMember` “Eski üye”). The two words do sit in adjacent strings exactly as the lexicon warns — `collab:sharing.auditToggleUnsharedHint` says “ortak çalışanla” three keys after `membersEmpty` says “üye” — and they stay two words. “ortak çalışan” is never shortened to “üye” and “üye” is never widened to “ortak çalışan”: the owner is a member and not a collaborator, which is precisely what `collab:sharing.membersDescription` (“Bu projeye erişimi olan herkes.”) says. |
| nickname | takma ad | **Met in batch 4.** The settled Turkish for a self-chosen public handle (`collab:nickname.title` “Takma ad”). Elimination: “kullanıcı adı” (username) is the one the lexicon singles out and is refused — the app already has an account identity; “rumuz” is attested but reads as a forum/chat pseudonym of the 2000s; “görünen ad” (display name) is the account-profile concept the nickname deliberately is not. Two words, so it can never collapse into the account's own name. |
| claim | edin | **Met in batch 4**, and the row exists because the UI needs the whole family off one stem: `collab:nickname.claimButton` “Takma ad edin” (the bare stem, per the short-button rule), `claiming` “Ediniliyor…” (the passive progressive, per the status rule), `claimSuccess` “Takma ad edinildi.” and the immutability hint `immutableHint` “Takma adlar bir kez edinildikten sonra değiştirilemez.” Elimination: “al” (take), “rezerve et” (reserve), “kaydol/kaydet” (register), “iste” (request) are the lexicon's own Not-list; “sahiplen” was rejected because its dominant Turkish reading is appropriating something that was somebody else's. “edinmek” survives and means *to acquire for oneself*, which is what claiming a nickname is. **It must stay distinguishable from “that name is reserved”**, and it does: `collab:errors.nickname_reserved` ships “Bu ad rezerve edilmiş.” — deliberately NOT “ayrılmış”, which would collide, one word for one word, with the *flag* term (`review:flag` “Ayır”, `review:emptyFlaggedTitle` “Ayrılmış çeviri yok”). |
| invite | davet | **Met in batch 4.** Noun and verb on one stem: `collab:invites.title` “Davetler”, `invites.description` “…davet edin.”, `join.codePlaceholder` “Davet kodu”. “davetiye” (an invitation card) is refused — it names a physical/social object, not a one-time code; “bağlantı” (link), “jeton” (token) and “erişim kodu” (access code) are on the lexicon's Not-list. **The four statuses are one set, `invites.status.*`: “Beklemede” / “Kullanıldı” / “Geçersiz kılındı” / “Süresi doldu”** — all state predicates, matching the shape batch 2 shipped for run statuses (`strings:runs.status*`), which are also mixed morphologically because English's own set is. “Beklemede” is byte-identical to `strings:runs.statusPending` and that is correct: same English word, same state. **“Revoke” is “geçersiz kıl”, and the elimination matters** — “iptal et” is Cancel (`config:cancelImport`, and every `cancel` key in this batch), “geri al” is Undo (`vault:undoRemove`), “geri çek” sits one letter from “geri al” in a product that already has an “istem”/“istek” near-miss, and “kaldır” is Remove. “geçersiz kıl” is free, and it says exactly what revoking a code does to it. |
| recording | kayıt | **The reservation this row made against *entry* was collected in batch 6, in a namespace neither row mentions.** `orphans` calls its objects *orphaned records* twice in English, and the batch rendered both with “girdi” rather than “kayıt” — see the *orphan* row. Nothing in `orphans` is a recording, and had *entry* been allowed to take this family the batch would have had to invent a word for one of the two. The reservation binds the **noun for the capture**, not the verb: `colorText:assistant.verboseHint` and `stage-details:chatVerboseHint` both render English's *Records timings…* as “…kaydeder.”, which is the ordinary verb doing its own job, exactly as `collab:sharing.auditToggleCheckboxLabel` already does. Shipped at `strings:runs.manualRecordingPaused` (“Kayıt duraklatıldı — etkin ortak çalışan yok”). **Batch 4 ships the switch that turns it on, on the same stem**: `collab:sharing.auditToggleLabel` (English “Manual-edit audit”) is “Elle düzenleme kaydı”, `auditToggleCheckboxLabel` is “Elle düzenlemeleri kaydet”, and both reuse batch 2's “elle düzenleme” from `strings:runs.viewManual` (“Elle düzenlemeler”), so the toggle, the paused chip and the view read as one feature. **English's “audit” is deliberately NOT rendered with “denetim”**: that word is the *check* term (one named LQA rule), and an “Elle düzenleme denetimi” would announce a quality check on the Sharing tab. The feature is a recording, this row owns the word for a recording, and the qualifier separates it from the server log the way the *changelog* row's does. The *entry* row reserved this family for exactly this term, and the reservation paid: “kayıt” is also the first Turkish word for a content record, so had *entry* taken it, “Recording paused” would read as “Entry paused”. Names the capture being on or off, never the captured rows. |
| stage | bölüm | A playable level, not a phase — `strings:tabs.stage-details` is “Bölüm ayrıntıları”, and `strings:runs.typeStageDetailsTranslation` / `typeChatStageDetails` repeat it. **Batch 6 owns the namespace itself and repeats the surface name verbatim at `stage-details:title`**, then carries the stem through every field label and message on the tab: `fields.stageDescription.label` is “Bölüm açıklaması”, `fields.name.placeholder` is “Bölümün adı”, `fields.gameplayDetails.label` is “Oynanış ayrıntıları” (English's *Gameplay details* — “oynanış” is the settled Turkish for gameplay and is free), and `stage-details:translateQueued` is “Bölüm ayrıntıları çevirisi sıraya alındı”. Nothing in the namespace reads as a process step, which is what the frozen row calls the single most likely mistranslation in the app. “aşama” and “evre” are the process readings the lexicon calls the single most likely mistranslation in the app, and both are refused; “sahne” is the theatre stage; “seviye” is a difficulty or a numeric level, not a piece of game content. “bölüm” is what Turkish games themselves call a level (“Bölüm 1-1”). Its other reading (*section, department*) never surfaces here because every occurrence sits beside game content. |
| Text Styler | metin biçimlendirici | The tool's name, settled once for the tab label (`strings:tabs` color-text), the sidebar item and the tool's own title. Sentence case, not Title Case: this locale writes even named surfaces in sentence case (`config:tm.policyTitle` “Çeviri belleği” for English's Title-Cased “Translation Memory”). The lexicon's Not-list bans *alternating* between “formatter”, “style editor” and “colour tool”, not the formatter reading itself — so one word, used everywhere, is what it asks for. “metin editörü” is refused: it promises a text editor. **`sidebar:colorText` shipped it in batch 4**, byte-identical to batch 2's tab label; **`colorText:title` shipped it in batch 6, the third and last site, byte-identical again — the surface is now closed.** (The sidebar key has no live call site today — `PAGE_ITEMS` in `Sidebar.tsx` does not list it and the tool is reached through `strings:tabs.color-text` — so it is translated for parity and for the day it is wired up, not because it renders.) |
| element | element | **Met in batch 6**, and the row splits in two because the namespace does. The **group heading** is translated — `colorText:groupElements` is “Elementler” — because it is ordinary chrome naming a swatch group, and “element” is the word Turkish gaming already uses for an elemental type (“öğe”, the generic Turkish for a UI *element*, is refused: it names a widget, not a game element). **The seven swatch names are NOT translated**: `colorText:swatches.hydro` / `pyro` / `anemo` / `electro` / `dendro` / `cryo` / `geo` ship exactly as English spells them. That is the frozen lexicon's own instruction — *use the names the game itself ships in your language, and if it ships them untranslated, leave them* — and these games ship no Turkish, so the Turkish player community uses the English names verbatim. `english-review-notes.md` says the same thing about the same nine keys. **Do not “finish” this row by inventing Turkish for the Greek roots**, which the frozen row bans outright. **Nothing mechanical protects the seven, and the reason is worth writing down rather than rediscovering:** the identical-value rule only fires on a *substantial* English source, which for a spaced locale such as this one means three words or more, and every one of the seven is a single word — so the gate is silent about them in `tr` whether they are right or wrong. It is silent for the unspaced locales too, for a different reason: the longest of them, *Electro*, is seven characters against that threshold's eight. Read the guard's own `isSubstantial` before assuming either half of that. |
| theme | tema | **Met in batch 4** (`settings:theme` “Tema”). **The four names are Turkicized in spelling, all four or none: “Klasik”, “Piksel”, “Tekno”, “Minimal”.** All four are ordinary Turkish loanwords, so half-translating the set (English *Pixel* beside Turkish “Klasik”) would read as an oversight rather than as a naming choice. **This is a prescription for batch 6**: `welcome:themeChooser.names.*` must ship these four byte-identical, which is the duplication the lexicon calls the highest-risk in the app — es ships the English spelling in one place and a Spanish one in the other. The descriptions are not names and are translated freely (`themes.techno.description` “Neon siberpunk terminal.”). **NOTHING MECHANICAL ENFORCED THIS PRESCRIPTION — batch 6 had to copy the four names out of `locales/tr/settings.json` by hand.** Checked against the guard's own code rather than assumed, and it fails *twice over*: (1) the function that builds a lexicon row's candidate list takes only the **Rendering** cell, so this Notes cell yields **zero** citations and the four names are never checked against anything, even after `welcome` ships; (2) even if they were, the guard proves a rendering is attested *somewhere in the locale*, never that it is attached to the right key — and all four words are already attested from `settings.json`, so a `welcome` that shipped the English spelling *Techno* would go green anyway. That is precisely the defect this row cites Spanish for, and the guard cannot see it. **Batch 6 discharged it by copying, and then by diffing the two files key-for-key rather than by re-reading this cell:** `welcome:themeChooser.names.*` now ships “Klasik” / “Piksel” / “Tekno” / “Minimal”, byte-identical to `settings:themes.*.name`, and the equality was asserted mechanically before the batch was reported. The taglines are not names and were translated freely, on the spelling this row fixes — `welcome:themeChooser.keepDefault` (*Keep Techno*) is “Tekno'da kal”, taking the proper-noun apostrophe because the name is a literal one the reader can see in the string. |
| guide | kılavuz | One word for the sidebar item and every “read the guide” link (`config:pseudoTestHelpLink`). Not “yardım”, “belgeler” or “el kitabı”. **The sidebar item itself shipped in batch 4** — `sidebar:guide` is “Kılavuz”, the same word the prose links already used. **Batch 6 adds the first-run screen, which is where a reader meets the word before anything else:** `welcome:guidesHeading` is the plural “Kılavuzlar” over the six guide group headings, and `welcome:browseGuides` is the button “Kılavuzlara göz at”. The heading standing over children that specialise it is the licensed nesting shape, and it is a proper substring rather than an equality — the group headings under it are “Kurulum”, “Çeviri”, “İnceleme”, “Terminoloji”, “Bakım” and the translation-memory one, none of which is this word. |
| release | sürüm | **Met in batch 6**, at the two `common` keys the Changelog page owns. Elimination: “girdi” is refused by the frozen row itself, which reserves *entry* for a content entry and names this term as the thing that must not take it; “kayıt” is refused because the *recording* row owns that family; “güncelleme” (update) and “yapı” (build) are on the frozen Not-list; “versiyon” is the loanword for the version *number*, which the frozen row also separates from this term. “sürüm” survives and is the settled Turkish for a published software release. **One same-rendering relation this creates, and it is licensed rather than overlooked:** batch 2 ships the same word for a *translation's* saved version — `strings:compare.cellUndoAria` is “{{language}} için önceki sürümler”. English separates *release* from *version*; Turkish does not, and coining a second word to keep the two apart is the error the *Etkin* bullet in `../style/tr.md` warns about. The two cannot co-render: the Changelog is a workspace page and the version history is a per-entry popover inside the Compare grid, and neither ever paints the other. **The consequence for `common:changelogEntryError` is that its rendering names the release rather than the entry:** English's *this changelog entry* means one release's entry, so the string ships as “Bu sürümün değişiklik günlüğü yüklenemedi.” — checked at **both** its call sites in `ChangelogView.tsx`, which are each keyed by a version and each report one release's own block failing to load, so the rendering is true at both (rubric item 6). Version numbers themselves are never translated. |
| changelog | değişiklik günlüğü | **Met in batch 4** at `sidebar:changelog` (“Değişiklik günlüğü”, 18 characters against the hard 28-character sidebar budget). It is the established Turkish for the artefact, and the qualifier is load-bearing: bare “günlük” is the live server-log panel — the *Activity* row above reserved it — so the head is shared and the heads-differ rule licenses it, exactly as “bağlam penceresi” and “çevirmen bağlamı” share “bağlam”. Elimination: “sürüm notları” (release notes), “yenilikler” (news/updates), “geçmiş” (history) are the lexicon's own Not-list; bare “Değişiklikler” was rejected because it drops the *log* and collides with `strings:runs.manualChangeColumn` “Değişiklik”. Version numbers are never translated. **Batch 6 names it in prose for the first time, and the stem is repeated rather than re-decided:** `common:changelogEntryError` is “Bu sürümün değişiklik günlüğü yüklenemedi.” The *release* row settles the other half of that sentence and explains why the word for the failing thing is “sürüm” rather than an entry word. |
