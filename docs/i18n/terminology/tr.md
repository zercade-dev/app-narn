# NARN terminology — Turkish (`tr`)

Your locale's rendering of every term in the shared lexicon. **This file is yours alone** —
no other locale's file is touched by your work, and nobody else writes rows here.

Read it alongside two other files:

- [`../terminology.md`](../terminology.md) — what each term *means*, its part of speech,
  an example key, and what it must not be confused with. It is **frozen** for the duration
  of the backfill: you read it, you never edit it. See the freeze notice at its top.
- [`../style/tr.md`](../style/tr.md) — how Turkish is written here: register, punctuation,
  capitalization, plural and agreement rules. That one is yours to write too, for this
  locale only.

Fill a row when you meet the term, in the same change that introduces the wording — so
this file records decisions actually taken, never predictions. Use **Notes** for anything
the next translator would otherwise have to rediscover: a declension that forced a
different word, a term you deliberately left in English, an acronym you expanded, a
candidate you rejected and why.

Terms are in the order they appear in `../terminology.md`. Do not add, remove or reorder
rows: a term the lexicon lacks goes in the additive queue in [`README.md`](README.md).

**Renderings are given in the bare citation form** (nominative singular, or the bare verb
stem for a verb). Turkish is agglutinative, so the shipped strings almost always carry the
word with case, possessive or plural suffixes on it — “proje” appears as “projeyi”,
“projenin”, “projeden”. Consistency here means **the same stem**, never the same letters.

| Term | Rendering | Notes |
| --- | --- | --- |
| project | proje | Verb is “oluştur”, never “aç” (open). |
| workspace | çalışma alanı | Deliberately two words, so it can never collapse into “proje”. Attributive use keeps the izafet: “çalışma alanı ayarları”. |
| entry | girdi | The unit the app counts. “kayıt” is refused — it is the obvious Turkish for *record*, and the *recording* term reserves that family. “satır” (row) is the CSV row (`config:rowsProcessed`), a different object. The reservation binds the **content unit only**: a disambiguated compound such as “kasa girdisi” (`config:credentialsMissing`, vault key entries) is licensed, exactly as the runbook allows for a log entry. |
| source text | kaynak metin | Same “kaynak” as source language and source label; the head noun disambiguates all three. |
| translation | çeviri | Verb “çevirmek”. The tab label is the plural “Çeviriler” — see the surface-name table in `../style/tr.md`. |
| source label | kaynak etiketi | The routing column shortens it to the bare “Kaynaklar” (`config:routing.labelSources`), exactly as English shortens it to “Sources”. |
| achievement | başarım | The settled Turkish game-platform word. “başarı” (success/achievement in the everyday sense) is refused: it is already the health metric in `config:health.successRate`, and the two appear in the same product. |
| inline tag | satır içi etiket | Umbrella over placeholders, formatting tags and “\n” escapes. Shares “etiket” with source label; the qualifier separates them. |
| placeholder | yer tutucu | The interpolation token inside game text only — the double-brace kind the mask-integrity check protects (`config:lqa.checks.mask-integrity.description`). The input-hint sense of the English word is a different concept and takes “ipucu metni” if it is ever needed — never this. |
| translator context | çevirmen bağlamı | Shipped in batch 2, which is where the concept itself first appears (`strings:compare.editContext`, `contextPlaceholder`, `compare.emptyContextFilter`, `compare.addContext`). The qualifier is load-bearing and **not droppable**: bare “bağlam” is the banned rendering, because the model's context window is “bağlam penceresi” and the CSV column is a third thing. That is why the “+ Context” chip ships as the fuller “+ Çevirmen bağlamı” rather than “+ Bağlam” — English clips it, this locale may not. |
| source language | kaynak dil |  |
| target language | hedef dil | Pairs with “kaynak dil”; the two sit side by side in Config. |
| reference language | referans dil | Pairs with “kaynak dil” / “hedef dil” on the same head noun, which is what makes the three read as one family (`strings:compare.translateUseReferenceNone`, `compare.reference`). The loanword is deliberate: a Turkish coinage (“başvuru dili”) would collide with the request/application senses of “başvuru”. Never a pivot reading — the app does not translate through it. |
| writable language |  |  |
| Pseudo Test | Pseudo Test | Kept as a proper noun, like the language code it names. A Turkish rendering (“sözde test”) would read as a value judgement about the test rather than a language name. |
| run | çalıştırma | Noun only — never a verb, per the shared lexicon. “koşu” is athletics; “işlem”, “görev” and “oturum” are the operation/task/session readings the lexicon bans. The verbs stay “çevir”, “başlat”, “üret”. **Scope of the “işlem” ban (runbook 2.9):** it bans *rendering this term* with that word, and nothing more — “işlem” is free elsewhere and batch 2 uses it correctly for *operation* (`strings:bulk.bulkOperation` “Toplu işlem”) and for a generic action inside prose (`strings:editor.deleteConfirm` “Bu işlem …”). The Activity table's **Action** column therefore takes “Eylem” (`strings:runs.actionColumn`), not “İşlem”, so that one word is not doing both jobs one column apart. Also note `strings:compare.run` — English's bare “Run” labelling the run-picker filter — is the noun and ships as “Çalıştırma”; it is this locale's largest single length ratio (3.33× over a three-character source), which is what a short English source does to a ratio, not a long rendering. |
| Activity | etkinlik | The tab that lists runs (`strings:tabs.runs`), its guide topic (`strings:guide.topicActivity`) and its page title, which English deliberately expands to “Translation Activity” → “Çeviri etkinliği” (`strings:runs.title`). Elimination: “geçmiş” is *history* and the lexicon bans it (it is also the word the empty state needs for “activity history”, which is where it belongs — inside the sentence, not as the surface name); “günlük” is taken by the live server-log panel; “işlem geçmişi” reads as a bank statement; “faaliyet” is bureaucratic register. “etkinlik” survives and means *the state of being active, things going on* — which is what the tab shows. Its secondary “an organized event” reading never surfaces here because nothing in this app schedules events. |
| batch | yığın | “toplu” is refused and **reserved**: it is Turkish for *bulk*, which the bulk-operations bar needs — and batch 2 spends that reservation (`strings:bulk.bulkOperation` “Toplu işlem”, `bulk.applyFailed` “Toplu değişiklik uygulanamadı”). “grup” is refused too — it is taken by rule group. English calls the Compare dialog “Batch translate” and its failure toast “batch translation”, so both keep this word: “Yığın çeviri” (`strings:compare.translateDialogTitle`) and “Yığın çevirisi başlatılamadı” (`strings:bulk.startFailed`). |
| batch grouping | yığın gruplaması | Distinct from batch mode (“yığın modu”) by the head noun, as the lexicon requires. |
| AI review | yapay zekâ incelemesi | Built on the same “inceleme” head as *source review*, which is what makes the two tabs read as one family: `strings:tabs.review-source-ai` is “Kaynak yapay zekâ incelemesi” and `review-translation-ai` is “Çeviri yapay zekâ incelemesi”. **Never rendered with “denetim”**, the *check* word — the lexicon separates a model's opinion from a deterministic rule, and this locale keeps that separation in the noun. The AI is always the full “yapay zekâ” with the circumflex, never “YZ”. |
| judge | değerlendirme | The evaluative sense. “yargı”, “hâkim”, “yargıç” are the courtroom readings and are refused outright. Appears in batch 1 only as a run type (`config:requestTimeoutDescription`, `config:batchGroupingDescription`). **In batch 2 the `strings:runs.judge*` keys are not this term:** every one of them says “AI review” in English, so they all take “yapay zekâ incelemesi” — the lexicon is explicit that the *feature* is called AI review and that “the judge” must not be introduced as a noun where English does not use one. Its two verdict labels are a deliberate pair: `judgeVerdictPass` / `judgeVerdictFail` ship as “Geçti” / “Geçmedi”, built on the same “geç-” root as “kalite geçidi”, rather than reusing “Başarılı/Başarısız” — those are already the *run status* and the LQA badge (`config:tm.lqaPassedBadge`, `strings:row.lqaPassed`, `strings:runs.statusFailed`), and one verdict must not read as the other. |
| source review | kaynak incelemesi | Built from “kaynak” + “inceleme”. “inceleme” is therefore reserved for **review**; the LQA check family takes “denetim” instead — see the *check* row. |
| finding | bulgu | Reserved for it by the *issue* row and now shipped (`strings:runs.judgeAllFindingsTitle`, `judgeAllFindingsCount`, `judgeViewAll`). It is the ordinary Turkish for *a thing found on inspection*, which is exactly an AI opinion about one entry, and it stays visibly distinct from “sorun” (the machine verdict listed on the same entry). |
| suggestion | öneri | One word across the judge panel, the glossary generator and the category generator, as the lexicon requires. Elimination: “tavsiye” is *advice*, “ipucu” is *hint* and is already spoken for by the input-hint sense in the *placeholder* row, “teklif” is a commercial offer. The action on it is “uygula” (apply), never “onayla” — see the *approve* row and the English copy bug recorded there. |
| discard | vazgeç | Sense 1, the ghost button beside Save (`config:discard`, `strings:editor.discard`). Sense 2, refusing a produced result, ships as **“reddet”** (`strings:runs.judgeDiscard`, and its toasts `judgeDiscardSuccess` / `judgeDiscardFailed`) — the reservation made in batch 1 is now a shipped pair, and the pre-flight reports the two as a licensed same-English/different-rendering collision. “iptal” is unavailable: it is Cancel (`config:cancelImport`). `config:discardUntranslatable` is a third, non-term use — “leave out of the export” — and is rendered by meaning (“dışarıda bırak”), not with this word. |
| needs review | inceleme gerekli | One wording across all three surfaces the lexicon names: the filter label (`strings:filters.needsReview`, `strings:compare.needsReviewFilter`), the deliberately lowercase cell chip (`strings:compare.cellNeedsReviewBadge`) and the bulk action (`strings:compare.flagAllNeedsReview` — “Tümünü inceleme gerekli olarak işaretle”). Casing is the only difference between them; do not let it become two renderings. Turkish carries no gender here, so the es/fr agreement problem the shared lexicon describes does not arise — but the **quoted-token** rule does: `strings:contextMenu.clearNeedsReview` quotes the chip verbatim, “inceleme gerekli”, and `clearReviewed` quotes the status token “incelendi”, matching `strings:compare.cellReviewedBadge`. Elimination: “gözden geçirilmeli” would be a second word for *review* beside “inceleme” and is refused for that reason alone. |
| flag |  | Still not met — but batch 2 **narrows what is left for it**, so read this before choosing. The LQA check descriptions' English “Flags …” is not this term (batch 1 renders it “bildirir”). Batch 2 now spends the generic marking verb **“işaretle”** on two other things: setting *needs review* (`strings:compare.flagAllNeedsReview`, `markAllReviewed`, `contextMenu.clearNeedsReview`) and the new-from-import flag (“yeni işareti”, `strings:filters.clearNewFlags`, `bulk.clearNewFlag`, `row.newTooltip`). The lexicon bans rendering *flag* with the mark verb precisely because this action **clears** the needs-review flag, so the disposition verb must be a third word — “ayır” / “beklemeye al” are free and unclaimed. Scope: this reserves the verb “işaretle” and the noun “işaret”, nothing else. |
| Review (the sidebar group) | inceleme | Shipped as the guide's group heading (`strings:guide.groupReview`); `sidebar:groups.review` in batch 4 repeats it verbatim. The lexicon's warning is against a word derived from **one member** — and this is the opposite: “inceleme” is the shared head of three of the four members (“kaynak incelemesi”, “çeviri yapay zekâ incelemesi”, “elle inceleme”), exactly the relationship English's own vague “Review” has to them, with Kalite sitting outside it in both languages. It is not the specific word for any one of them: the AI passes are always the full “yapay zekâ incelemesi”, human review is “elle inceleme”, and the deterministic rules are “denetim”, a different word entirely. |
| review queue |  | Not met. Note: build it on the “inceleme” root so the queue and the *needs review* state read as one feature, as the lexicon asks. |
| module | modül | The loanword, settled. “birim” (unit) is refused — it says nothing about what the thing does, and the app has no other “unit”. |
| module instance | modül örneği | “örnek” is the settled Turkish computing word for *instance*; its other reading (*example*) never collides here because every occurrence sits beside a module. “kopya”, “profil”, “yapılandırma” are all on the lexicon's ban list. **The izafet suffix rides on “örnek”, never on the module name** — see the placeholder section of `../style/tr.md`. |
| provider | sağlayıcı | The outside company behind a module. Kept strictly apart from “modül”, including in the three English strings that mislabel a module picker “Provider” (`config:routing.simplePlaceholder`), which are translated as written. |
| model | model | Loanword. This is why *template* may not be “model”: see that row. |
| prompt | istem | The settled Turkish AI term. **Watch the near-collision with “istek” (request)** — one letter apart, and both live in the same settings panel (`config:requestsPerSecondLabel`, `config:requestTimeoutLabel`). “komut”, “sorgu”, “yönerge” are the command/query/instruction readings the lexicon bans. |
| reasoning effort | akıl yürütme çabası | The provider's own parameter. Not “çaba” alone, which reads as work done. |
| routing rule | yönlendirme kuralı | Content routing, not network routing — “rota” and “yol” are refused for that reason. |
| rule group | kural grubu | “grup” is reserved here, which is one reason *batch* is “yığın”. Not “küme”, not “profil”, not “önayar”. |
| credential vault | kimlik bilgisi kasası | **Promoted from the clip “kasa” in batch 2, which is the batch that first names the vault cold** — `strings:guide.topicVault` (“Credential Vault”) ships the full form, so it is now attested and the Rendering column can hold the term itself. Bare “kasa” remains licensed as the **clip**, and only where the surrounding string already establishes credentials: every `config` site does (`credentialsVaultLockedChip`, `credentialsUnlockButton`, `credentialsVaultLocked`, `credentialsMissing`). A cold, standalone “kasa” is the safe/strongbox reading the frozen lexicon's Not-list bans — which still binds `vault:statusLabel` in batch 4, the second cold naming, and it takes the full form too. All four required phrasings read naturally on this stem: kasa kilitli / kasanın kilidini aç / kasa parolası / kimlik bilgisi kasası. **The mechanism that held this row at the clip for a round is worth keeping, because it applies to every row in this file:** the citation guard (`scripts/check-lexicon-citations.mjs`) requires this column's significant words to be attested in the shipped `tr` corpus, so a term's full form **cannot be recorded ahead of the batch that ships it**, and every unshipped row is biased toward the clipped wording by construction. Read an unpromoted row as “the longest form shipped so far”, not as “the term”, and promote it the moment your batch ships the fuller one. Bending a string to make a citation attestable is the wrong fix and was refused here. |
| credential | kimlik bilgisi | Usually plural in the UI (“Kimlik bilgileri eksik”). Not “parola”, not “hesap”. Vault key names themselves are never translated. |
| LQA | LQA | Kept as the industry acronym; Turkish localization practice uses it untranslated. Used attributively with no suffix on the acronym: “LQA denetimleri”, “LQA geçidi”. |
| quality gate | kalite geçidi | The process-control sense. “kapı” is refused — it is a literal door, and its uppercase form “KAPI” also collides with the do-not-translate term API in the guard's own term check. “eşik” (threshold) is refused because a threshold is a number, and this file already has severity and limit vocabulary. |
| check | denetim | One named LQA rule. Elimination: “kontrol” is the generic Turkish for *control* and reads as a UI control; “sınama” is *test*; “doğrulama” is *validation* and is taken by **assertion**; “inceleme” is taken by **review**. “denetim” survives, and it means an applied rule that passes or fails — which is what a check is. |
| issue | sorun | One LQA verdict, at blocking or warning severity. “hata” (error) is refused by the lexicon; “bulgu” is deliberately left free for **finding**, which is listed on the same entry. |
| severity | önem derecesi | The two values are “Engelleyici” and “Uyarı” and are fixed by the *check* row, not re-decided here. “seviye”/“düzey” (level) and “öncelik” (priority) are refused. |
| notification severity |  |  |
| assertion | doğrulama | A user-written regex the translation must or must not match. Elimination: “koşul” is routing conditions, “kural” is routing rules, “denetim” is the LQA check, “iddia”/“sav” are the claim reading, “ifade” is *statement*. “doğrulama” survives, is free (nothing else in this app is called *validation*), and means what an assertion does. |
| pattern | desen | The regex sense only. Never “kalıp” or “şablon” — the latter is template. |
| overflow | taşma | Relative to the source, via the overflow ratio. Kept clearly apart from “uzunluk sınırı”, which is absolute. |
| length limit | uzunluk sınırı | Hard per-language cap set by the game editor. Also the routing condition on source length (`config:routing.labelMaxLength`). |
| pass rate |  | Not met. Note for the quality batch: `config:health.successRate` is a **different** metric and already ships as “Başarı”, so pass rate needs its own word (“geçme oranı” is free). |
| glossary | sözlükçe | Elimination: “sözlük” alone is *dictionary*, which the lexicon bans; “terim tabanı” is a termbase, a different CAT artefact; “kelime listesi” is the word-list reading. “sözlükçe” is the attested Turkish for *glossary*, is one word (it has to fit a tab label), and leaves “terim” free for the row below. |
| glossary term | terim | Shortened to the bare “terim” inside the Glossary tab, as English does. Never “girdi” — that is a content entry, and the app counts those constantly. |
| constant |  |  |
| match | eşleşme | Noun. The verb is “eşleşmek” on the same root, which keeps the search and routing strings (`config:models.noMatches`, `config:lqa.regexModeMustMatch`) visibly the same word as the glossary noun, exactly as the lexicon asks. |
| translation memory | çeviri belleği | The established Turkish CAT term. Bare “bellek” is never used alone — it reads as RAM. |
| approve | onayla | Storing a translation into translation memory. This forces one consequence worth knowing: **confirm buttons may not also be “Onayla”**, so they take the explicit two-step Turkish form instead (“Evet, sil” at `config:instances.deleteConfirm` and `strings:editor.deleteConfirmYes`, “Eminim” at `config:routing.removeConfirm`). “kabul et” (accept), “doğrula” (validate) and “kaydet” (save) are all refused or taken. Batch 2 ships the bulk control as **“Çeviri belleğine onayla”** (`strings:bulk.approveSelected`): the destination has to be named, because bare “belleğe” reads as RAM. Its toast reorders rather than repeating the word — “Çeviri belleğine {{count}} çeviri onaylandı”, not “{{count}} çeviri çeviri belleğine…”. And `strings:runs.judgeApproveAll`, whose English says “Approve all suggestions” for the apply action, ships as “Tüm önerileri **uygula**” — the known English copy bug, not a second use of this word. |
| category | kategori |  |
| tone | ton | An authoring instruction on an entry. “üslup” is refused **as a rendering of this term**: it is the writing-style reading the lexicon warns about, and it would read as the model's style rather than an instruction to it. Scope (runbook 2.9): the ban is on this term only, so “üslup” stays free — and correct — where English itself says *style*, which batch 2 uses at `strings:compare.translateExamplesLabel` (“Örnek girdiler (üslup referansı)”) and `translateExamplesHint`. The two words next to each other are the disambiguation, not a collision. |
| orphan | yetim | The figurative noun, used identically in the tab title, the count chip and the import notices. “artık”, “sahipsiz” and “kayıp” are the leftover/ownerless/lost readings the lexicon bans. Adjectival “orphaned” is “yetim olarak işaretlenmiş” or simply “yetim”. |
| relink | yeniden bağla | One verb for the row button, the dialog title, the confirm step and the import warning (`config:importModeFullReplaceHint`). Not “yeniden eşle”, not “taşı”. |
| backup | yedek | Countable noun (“proje başına en fazla yedek”). The **surface** is “Yedekleme” — see the surface-name table in `../style/tr.md`; do not use one where the other belongs. The verb is “yedek oluştur”. |
| snapshot | anlık görüntü | An automatic pre-operation restore point. Distinct from “yedek”, which matters because `config:importSnapshotNote` uses both in one sentence. Not “ekran görüntüsü”, which is a screenshot. |
| template | şablon | Never “model” — that is the AI model, and this is exactly the collision the shared lexicon warns Romance and Slavic languages about. Turkish has the same trap through “kalıp”, which is also refused because it is the natural word for a regex *pattern*. |
| collaborator | ortak çalışan | Shipped at `strings:runs.projectTotalCollaborators` and `strings:runs.manualRecordingPaused`. **“işbirlikçi” is refused outright and this is the one to know about:** it is the dictionary translation of *collaborator*, and in Turkish its dominant reading is the wartime one — a person who collaborates with an enemy. Shipping it would call every invited teammate a traitor. “katılımcı” (participant), “katkıda bulunan” (contributor) and “kullanıcı” (user) are on the lexicon's own ban list. “ortak çalışan” survives, is two words so it can never collapse into *member*, and means precisely *someone working on it jointly*. |
| member |  | Not met. Note: “üye” is free and is the obvious pair for the row above — the two words must stay distinct, since “Members” and “collaborators” sit in adjacent strings on the Sharing page. |
| nickname |  |  |
| claim |  |  |
| invite |  |  |
| recording | kayıt | Shipped at `strings:runs.manualRecordingPaused` (“Kayıt duraklatıldı — etkin ortak çalışan yok”). The *entry* row reserved this family for exactly this term, and the reservation paid: “kayıt” is also the first Turkish word for a content record, so had *entry* taken it, “Recording paused” would read as “Entry paused”. Names the capture being on or off, never the captured rows. |
| stage | bölüm | A playable level, not a phase — `strings:tabs.stage-details` is “Bölüm ayrıntıları”, and `strings:runs.typeStageDetailsTranslation` / `typeChatStageDetails` repeat it. “aşama” and “evre” are the process readings the lexicon calls the single most likely mistranslation in the app, and both are refused; “sahne” is the theatre stage; “seviye” is a difficulty or a numeric level, not a piece of game content. “bölüm” is what Turkish games themselves call a level (“Bölüm 1-1”). Its other reading (*section, department*) never surfaces here because every occurrence sits beside game content. |
| Text Styler | metin biçimlendirici | The tool's name, settled once for the tab label (`strings:tabs` color-text), the sidebar item and the tool's own title. Sentence case, not Title Case: this locale writes even named surfaces in sentence case (`config:tm.policyTitle` “Çeviri belleği” for English's Title-Cased “Translation Memory”). The lexicon's Not-list bans *alternating* between “formatter”, “style editor” and “colour tool”, not the formatter reading itself — so one word, used everywhere, is what it asks for. “metin editörü” is refused: it promises a text editor. |
| element |  |  |
| theme |  |  |
| guide | kılavuz | One word for the sidebar item and every “read the guide” link (`config:pseudoTestHelpLink`). Not “yardım”, “belgeler” or “el kitabı”. |
| release |  |  |
| changelog |  |  |
